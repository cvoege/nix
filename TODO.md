# TODO.md
* Break down cw into some more purpose built commands. A worktree creator, a tmux session manager, and a workspace manager that uses the prior two to set up claude workspaces or general workspaces. Plus some cw fleet management concepts
* Restructure test folder to put all stack tests in a folder
* Write tests for cw using existing test fixture system
* Add support for --model on cw
* Make liberal use of writing one off scripts for validation. For example, if you are checking that two implementations do the same thing, write a quick script that runs both implementations with a variety of arguments to make sure they return the same thing.
* Code review skill 
* Figure out code review skill/command
* More agent reviewers?
