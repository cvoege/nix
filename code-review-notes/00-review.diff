diff --git a/apps/addin-components/src/components/ActionButtons/ActionButtonsWrapper.tsx b/apps/addin-components/src/components/ActionButtons/ActionButtonsWrapper.tsx
index 7eddb3f1b3..c293b33ea0 100644
--- a/apps/addin-components/src/components/ActionButtons/ActionButtonsWrapper.tsx
+++ b/apps/addin-components/src/components/ActionButtons/ActionButtonsWrapper.tsx
@@ -122,6 +122,7 @@ export const ActionButtonsWrapper = ({
     <>
       <ActionButtons
         onDrillDownClick={handleDrillDownClick}
+        // @ts-expect-error TYPETODO: useDrilldown needs to be typed
         getDrillDownByOptions={getDrillDownByOptions}
         renderTimelineItems={renderTimelineItems}
         onIsTrackingSelectionChange={(input) => {
diff --git a/apps/addin-components/src/hooks/useDrillDown.jsx b/apps/addin-components/src/hooks/useDrillDown.jsx
index cf90f6419f..8fe1d07490 100644
--- a/apps/addin-components/src/hooks/useDrillDown.jsx
+++ b/apps/addin-components/src/hooks/useDrillDown.jsx
@@ -122,9 +122,9 @@ const useDrillDown = ({ recordDrillDown, onDrillDownRecordingChange }) => {
             profileContext,
           });
           analytics.track('Drill Down Ended Succesfully');
-        } catch (e) {
-          analytics.track('Drill Down Ended With Failure', { error: e.message });
-          throw e;
+        } catch (error) {
+          analytics.track('Drill Down Ended With Failure', { error: error.message });
+          throw error;
         }
       },
     )();
diff --git a/apps/gs-addin/src/client/utils/formulaUtils.ts b/apps/gs-addin/src/client/utils/formulaUtils.ts
index 769a01580f..ecd55663e2 100644
--- a/apps/gs-addin/src/client/utils/formulaUtils.ts
+++ b/apps/gs-addin/src/client/utils/formulaUtils.ts
@@ -23,7 +23,7 @@ const ALEPH_FORMULA_REGEX = /ALEPH(.*?)GET(INTEGRATION)?DATA\((.*?)\)/g;
 
 export function isAlephFormula(formula: string) {
   const s = formula.toString();
-  return !!s.match(ALEPH_FORMULA_REGEX);
+  return Boolean(ALEPH_FORMULA_REGEX.test(s));
 }
 
 /**
@@ -210,14 +210,16 @@ export const parseAlephFormulaArraySize = (formula: string): RowColumnCounts | n
   const [_tableName, ...namesValues] = alephFormulaArgs;
 
   function argRowsColumns(arg: string) {
-    const m = arg.replace(/\$/g, '').match(RANGE_REGEX);
-    if (!m)
-      // Non-ranges are always a single cell
+    const m = arg.replaceAll('$', '').match(RANGE_REGEX);
+    if (!m) // Non-ranges are always a single cell
+    {
       return { nRows: 1, nColumns: 1 };
+    }
     const [_group0, startColumn, startRow, lastColumn, lastRow] = m;
-    if (!lastColumn)
-      // Single cell reference
+    if (!lastColumn) // Single cell reference
+    {
       return { nRows: 1, nColumns: 1 };
+    }
     // Range reference
     return {
       nRows: parseInt(lastRow, 10) - parseInt(startRow, 10) + 1,
@@ -294,10 +296,18 @@ export class RowExtent {
 }
 
 const compareRowExtentsLessThan = (a: RowExtent, b: RowExtent) => {
-  if (a.row < b.row) return true;
-  if (a.row > b.row) return false;
-  if (a.startColumn < b.startColumn) return true;
-  if (a.startColumn > b.startColumn) return false;
+  if (a.row < b.row) {
+    return true;
+  }
+  if (a.row > b.row) {
+    return false;
+  }
+  if (a.startColumn < b.startColumn) {
+    return true;
+  }
+  if (a.startColumn > b.startColumn) {
+    return false;
+  }
   return a.endColumn < b.endColumn;
 };
 
diff --git a/apps/server/src/operations/alephFunction.ts b/apps/server/src/operations/alephFunction.ts
index b89f108a2b..38f84a08be 100644
--- a/apps/server/src/operations/alephFunction.ts
+++ b/apps/server/src/operations/alephFunction.ts
@@ -55,10 +55,18 @@ export class AlephFunctionParseException extends Error {
  * the token was NOT quoted; numbers and booleans are the only valid unquoted forms.
  */
 const coerceArg = ({ value, isRef }: { value: string; isRef: boolean }): FormulaArgValue => {
-  if (!isRef) return value;
-  if (/^-?\d+(\.\d+)?$/.test(value)) return Number(value);
-  if (/^true$/i.test(value)) return true;
-  if (/^false$/i.test(value)) return false;
+  if (!isRef) {
+    return value;
+  }
+  if (/^-?\d+(\.\d+)?$/.test(value)) {
+    return Number(value);
+  }
+  if (/^true$/i.test(value)) {
+    return true;
+  }
+  if (/^false$/i.test(value)) {
+    return false;
+  }
   // Unquoted non-literal (a cell reference, or a string missing its quotes): unevaluable here.
   throw new AlephFunctionParseException(
     'NON_LITERAL_ARGUMENT',
@@ -91,7 +99,7 @@ export const parseAlephFunction = (rawFormula: string): FormulaArgValue[] => {
     throw new AlephFunctionParseException('MALFORMED_FORMULA', MALFORMED_FORMULA_MESSAGE);
   }
 
-  if (!parsed) {
+  if (!parsed || !parsed.alephFormula) {
     // `parsed` is undefined when:
     // - Not Aleph formula at all
     // - On some parsing errores like:
@@ -215,7 +223,9 @@ const runResolvedFunction = async (
   pullForCellsFn: ServerPullForCellsFn,
 ): Promise<{ value?: FormulaArgValue; error?: string }> => {
   const [outcome] = await runAlephFormulaBatch(pullForCellsFn, entry.table, entry.measures, [entry]);
-  if (CustomFunctionException.is(outcome)) return { error: outcome.message };
+  if (CustomFunctionException.is(outcome)) {
+    return { error: outcome.message };
+  }
   return { value: outcome };
 };
 
diff --git a/apps/web-ui/src/pages/Workbooks/Workbook/Workbook.tsx b/apps/web-ui/src/pages/Workbooks/Workbook/Workbook.tsx
index 5b96072928..30e88a874b 100644
--- a/apps/web-ui/src/pages/Workbooks/Workbook/Workbook.tsx
+++ b/apps/web-ui/src/pages/Workbooks/Workbook/Workbook.tsx
@@ -1008,6 +1008,7 @@ const Workbook = () => {
               <Box position="absolute" bottom="calc(40px + 12px)" right="calc(26px + 12px)">
                 <DrillDownButton
                   onDrillDownClick={handleDrillDownClick}
+                  // @ts-expect-error TYPETODO: getDrillDownByOptions is not typed yet
                   getDrillDownByOptions={handleGetDrillDownOptions}
                   onCompleteDrilldown={() => {}}
                 />
diff --git a/packages/addin-core/integration-tests/fixtures/dev.alephDB.fixturerecordings.json b/packages/addin-core/integration-tests/fixtures/dev.alephDB.fixturerecordings.json
index 5eba99d0d3..6a06b2d4df 100644
--- a/packages/addin-core/integration-tests/fixtures/dev.alephDB.fixturerecordings.json
+++ b/packages/addin-core/integration-tests/fixtures/dev.alephDB.fixturerecordings.json
@@ -141554,6 +141554,9 @@
           "sheetRangesValues": {
             "Sheet3": {
               "A7": [["=ALEPH.LOCAL.GETINTEGRATIONDATA(Sheet4!A1, \"Cons Or Accrual\", \"Test1\")"]]
+            },
+            "Sheet4": {
+              "A1": [["Sage Intacct (Native) | Profit and Loss"]]
             }
           },
           "activeRange": {
@@ -152434,6 +152437,9 @@
           "sheetRangesValues": {
             "Sheet3": {
               "A7": [["=ALEPH.LOCAL.GETINTEGRATIONDATA(Sheet4!A1, \"Cons Or Accrual\", \"Test1\")"]]
+            },
+            "Sheet4": {
+              "A1": [["Sage Intacct (Native) | Profit and Loss"]]
             }
           },
           "activeRange": {
diff --git a/packages/addin-core/src/data-access/index.ts b/packages/addin-core/src/data-access/index.ts
index f598124f01..6237c9c2d9 100644
--- a/packages/addin-core/src/data-access/index.ts
+++ b/packages/addin-core/src/data-access/index.ts
@@ -10,6 +10,7 @@ import {
   SheetRangesFormatPairsList,
   type CellValue,
   SpreadsheetService,
+  SheetWithoutActive,
 } from '@getaleph/global-common-utils';
 
 import { Cell, CellStyle, Configuration, DisconnectRange, Range } from '../entities';
@@ -109,7 +110,7 @@ export const parseA1 = async (
   };
 };
 
-type SheetRangePair = { sheet: Sheet; range: string };
+type SheetRangePair = { sheet: SheetWithoutActive; range: string };
 
 export const getOptimizedSheetRangesPairsList = (
   sheetRangePairsList: SheetRangePair[],
@@ -196,7 +197,7 @@ export const getRangesValues = async (sheetRangePairsList: SheetRangePair[], pro
   return processGetRangesValuesOutput(sheetRangePairsList, serviceResult, processValues);
 };
 
-export const getRangeValues = async (sheet: Sheet, range: string, processValues = true) => {
+export const getRangeValues = async (sheet: SheetWithoutActive, range: string, processValues = true) => {
   const batchedResult = await getRangesValues([{ sheet, range }], processValues);
 
   return batchedResult[0];
@@ -300,12 +301,12 @@ export const getCurrentSelection = async (): Promise<Cell | Range | DisconnectRa
 };
 
 type SheetDisconnectRangePair = {
-  sheet: Sheet;
+  sheet: SheetWithoutActive;
   disconnectRange: DisconnectRange;
 };
 
 type SheetBoundingBoxPair = {
-  sheet: Sheet;
+  sheet: SheetWithoutActive;
   range: string;
   rangeObject: Range;
 };
@@ -442,7 +443,7 @@ export const getDisconnectRangeValues = async (
 };
 
 type SelectionType = Cell | Range | DisconnectRange;
-type SheetSelectionPair = { sheet: Sheet; selection: SelectionType };
+type SheetSelectionPair = { sheet: SheetWithoutActive; selection: SelectionType };
 
 export const getSelectionsValues = async (
   sheetSelectionPairsList: SheetSelectionPair[],
@@ -634,7 +635,7 @@ export const getSelectionsValues = async (
  * @returns The values for the selection.
  */
 export const getSelectionValues = async (
-  sheet: Sheet,
+  sheet: SheetWithoutActive,
   selection: SelectionType,
   processValues = true,
   includeMergedAreas = false,
diff --git a/packages/addin-core/src/entities/Table.ts b/packages/addin-core/src/entities/Table.ts
index 64c9864569..0471e0c4bd 100644
--- a/packages/addin-core/src/entities/Table.ts
+++ b/packages/addin-core/src/entities/Table.ts
@@ -1,4 +1,4 @@
-import { OrderCriteria } from '@getaleph/global-common-utils';
+import { DimensionGroup, OrderCriteria } from '@getaleph/global-common-utils';
 
 import { normalizeName } from '../utils/names';
 
@@ -10,6 +10,7 @@ export type TableOptions = {
   prettyName: string;
   isMainTable?: boolean;
   dimensions: Dimension[];
+  dimensionGroups?: DimensionGroup[];
   isIntegrationTable?: boolean;
   // TYPETODO: This seems to be used in different ways in different places?
   orderByCriteria?: unknown[] | string[];
@@ -50,6 +51,9 @@ export class Table extends SelectableEntity {
 
   dimensions: Dimension[];
 
+  // Set by Object.assign in Entity#set from the server table payload.
+  dimensionGroups?: DimensionGroup[];
+
   dataWarehouseConfigurationId?: string;
 
   isIntegrationTable?: boolean;
diff --git a/packages/addin-core/src/operations/buildExplores/index.js b/packages/addin-core/src/operations/buildExplores/index.js
index 0cd1690df8..20f3fea2da 100644
--- a/packages/addin-core/src/operations/buildExplores/index.js
+++ b/packages/addin-core/src/operations/buildExplores/index.js
@@ -26,7 +26,6 @@ import { getConnectTableBoundingBoxRange } from '../connectsNew';
 import { getMetadataValues, parseSheet } from '../explores';
 import SpreadsheetUtils from '../SpreadsheetUtils';
 
-import { getColumnDimensionsNew, getFilterGroupNew, getRowDimensionsNew } from './newUtils.ts';
 import {
   dimensionsMatch,
   findConnectedTablesInRange,
@@ -44,6 +43,9 @@ import {
   splitAlephFormula,
   getSingleAlephFormula,
   findExploreInRange,
+  getColumnDimensionsNew,
+  getFilterGroupNew,
+  getRowDimensionsNew,
 } from './utils';
 
 export { isAlephFormula, buildAlephFormulasMap, getSingleAlephFormula };
diff --git a/packages/addin-core/src/operations/buildExplores/newUtils.ts b/packages/addin-core/src/operations/buildExplores/newUtils.ts
deleted file mode 100644
index d2dc4bb976..0000000000
--- a/packages/addin-core/src/operations/buildExplores/newUtils.ts
+++ /dev/null
@@ -1,155 +0,0 @@
-import { DrillDownError } from '@getaleph/frontend-common-utils';
-import { appendAndFilterToFilterGroup } from '@getaleph/global-common-utils';
-
-import { getSelectionValues } from '../../data-access';
-import { ConnectedTable, Dimension, DisconnectRange } from '../../entities';
-import Table from '../../entities/Table';
-import {
-  AddinCoreExploreFilter,
-  AddinCoreExploreFilterGroup,
-  addinCoreFilterToFilterGroup,
-  simpleExploreFilterToAddinCoreFilter,
-} from '../../transformers';
-import { parseConnectMeasure } from '../connects';
-
-type DimensionFilterMap = Record<string, string[]>;
-
-export function getFilterGroupNew(
-  table: Table,
-  addinFilterGroup: AddinCoreExploreFilterGroup | AddinCoreExploreFilter,
-  dimToValuesMap: DimensionFilterMap,
-) {
-  const initFilters = Object.entries(dimToValuesMap).map(([key, values]) => {
-    const valuesArray = Array.from(new Set(values));
-
-    if (valuesArray.length === 1) {
-      const value = valuesArray[0];
-
-      // using Equals here as wildcards aren't supported in IN filters
-      return {
-        dimension: table.getDimensionByAnyMatch(key)?.value ?? key,
-        value,
-        operator: 'Equals' as const,
-      };
-    }
-
-    return {
-      dimension: table.getDimensionByAnyMatch(key)?.value ?? key,
-      value: valuesArray,
-      operator: 'In' as const,
-    };
-  });
-
-  const cleanExploreFilters = addinCoreFilterToFilterGroup(addinFilterGroup);
-
-  const realFilters = appendAndFilterToFilterGroup(cleanExploreFilters, initFilters);
-
-  return simpleExploreFilterToAddinCoreFilter(realFilters, table);
-}
-
-export function getColumnDimensionsNew(table: Table, dimensionFilterMap: DimensionFilterMap): Dimension[] {
-  const columnDimensions: Dimension[] = [];
-  Object.entries(dimensionFilterMap).forEach((d) => {
-    const [dimension, values] = d;
-    const isVariableDimension = values.length > 1;
-    if (isVariableDimension) {
-      const columnDimension = table.getDimensionByValue(dimension);
-      if (columnDimension) {
-        columnDimensions.push(columnDimension);
-      }
-    }
-  });
-
-  return columnDimensions;
-}
-
-export function getRowDimensionsNew(table: Table, dimensionFilterMap: DimensionFilterMap): Dimension[] {
-  const parentDimensionTextsSet = new Set([]);
-  const filters = Object.keys(dimensionFilterMap).map((dimension) => table.getDimensionByAnyMatch(dimension));
-  // @ts-expect-error Table is yet not fully typed...
-  if (table.dimensionGroups) {
-    // @ts-expect-error Table is yet not fully typed...
-    const { dimensionGroups } = table;
-    filters.forEach((filter) => {
-      if (filter) {
-        const { text } = filter;
-        // @ts-expect-error Table is yet not fully typed...
-        const dimensionGroup = dimensionGroups.find(({ dimensions }) => dimensions.includes(text));
-        if (dimensionGroup) {
-          const dimensionIndex = dimensionGroup.dimensions.indexOf(text);
-          if (dimensionIndex > 0) {
-            dimensionGroup.dimensions
-              .slice(0, dimensionIndex)
-              // @ts-expect-error Table is yet not fully typed...
-              .forEach((parentDimension) => parentDimensionTextsSet.add(parentDimension));
-          }
-        }
-      }
-    });
-  }
-  const parentDimensionTexts = new Set(parentDimensionTextsSet);
-  return table.dimensions.filter(
-    ({ text, value }) =>
-      // @ts-expect-error Table is yet not fully typed...
-      !filters.find((dimension) => dimension.value === value) && !parentDimensionTexts.has(text),
-  );
-}
-
-export const processConnectMeasures = async (connectedTable: ConnectedTable) => {
-  if (!connectedTable.table) {
-    return [undefined];
-  }
-
-  const tableMeasures =
-    connectedTable.table && connectedTable.table.measures ? connectedTable.table.measures : [];
-
-  // If the connected table doesn't have measures defined, default to the first measure from the table
-  if (!connectedTable.measures?.range) {
-    return [tableMeasures[0]];
-  }
-
-  // Get the values from the measure range
-  const measureRange = DisconnectRange.parse(connectedTable.measures?.range);
-  const measureValuesGrid = await getSelectionValues(connectedTable.sheet, measureRange);
-
-  // Grab defined unique values only
-  const measureValues = Array.from(new Set(measureValuesGrid.flat().filter((value) => value !== undefined)));
-
-  // Fail if the connect brings multiple measures. We don't support this kind of drill down yet.
-  if (measureValues.length > 1) {
-    throw new DrillDownError(
-      'Drill down on connects that define multiple measures is not supported yet.',
-      'Drill down error',
-      {
-        shouldLog: false,
-      },
-    );
-  }
-
-  // Fail if the connect brings no measures. Weird case, just in case it happens.
-  if (measureValues.length === 0) {
-    throw new DrillDownError(
-      'This Connect was built with 1 measure. Since the table now has more, define 1 measure to drill down on it.',
-      'Drill down error',
-      { shouldLog: false },
-    );
-  }
-
-  // Parse the connect measure format into the measure pretty name and aggregator
-  const { measure, aggregator } = parseConnectMeasure(measureValues[0], {});
-
-  // Use the measure pretty name to bring the dimension object from the table
-  const matchingMeasure = connectedTable.table.getMeasureByText(measure);
-
-  // Fail if we couldn't find the measure in the table
-  if (!matchingMeasure) {
-    throw new DrillDownError(
-      "The measure you're trying to drill-down into no longer exists or is invalid, please re-check the measure names for this table and try again.",
-      'Drill down error',
-      { shouldLog: false },
-    );
-  }
-
-  // Return a copy of the match with the aggregator
-  return [new Dimension({ ...matchingMeasure, aggregator })];
-};
diff --git a/packages/addin-core/src/operations/buildExplores/utils.js b/packages/addin-core/src/operations/buildExplores/utils.ts
similarity index 66%
rename from packages/addin-core/src/operations/buildExplores/utils.js
rename to packages/addin-core/src/operations/buildExplores/utils.ts
index ed8442223d..e7fc8eb805 100644
--- a/packages/addin-core/src/operations/buildExplores/utils.js
+++ b/packages/addin-core/src/operations/buildExplores/utils.ts
@@ -1,5 +1,8 @@
 // oxlint-disable unicorn/prefer-regexp-test
+
 import { AlephError, DrillDownError } from '@getaleph/frontend-common-utils';
+import { appendAndFilterToFilterGroup } from '@getaleph/global-common-utils';
+import { CellValue, Connect, Sheet, SheetWithoutActive } from '@getaleph/global-common-utils';
 
 import { ERROR_MESSAGES } from '../../constants';
 import {
@@ -17,13 +20,24 @@ import {
   Range,
   Filter,
   FilterGroup,
+  Table,
+  Dimension,
+  Explore,
+  ConnectedTable,
 } from '../../entities';
 import { TABLE_ROW_START_INDEX } from '../../entities/NoCodeExplore';
+import { FilterValue } from '../../entities/types';
+import {
+  AddinCoreExploreFilter,
+  AddinCoreExploreFilterGroup,
+  addinCoreFilterToFilterGroup,
+  simpleExploreFilterToAddinCoreFilter,
+} from '../../transformers';
+import { SpreadsheetInput } from '../../transformers';
 import { normalizeName, arrayToKey } from '../../utils';
+import { parseConnectMeasure } from '../connects';
 import { getConnectWithModifiedDimensionRanges } from '../connects';
 
-import { processConnectMeasures } from './newUtils';
-
 const drilldownErrors = ERROR_MESSAGES.DRILLDOWN;
 
 // We know that the last \((.*?)\) is being lazy with the parenthesis match, so if some of our arguments has internal parenthesis like "table (v2) name"
@@ -35,10 +49,24 @@ const ALEPH_FORMULA_REGEX = /ALEPH(.*?)GET(INTEGRATION)?DATA\((.*?)\)/g;
 const ARRAY_ARGUMENT_REGEX = /\$?[A-Z]+\$?[0-9]+:\$?[A-Z]+\$?[0-9]+/g;
 const ARRAY_ARGUMENT_STRICT_REGEX = /^\$?[A-Z]+\$?[0-9]+:\$?[A-Z]+\$?[0-9]+$/;
 
-export function isAlephFormula(formula) {
+export function isAlephFormula(formula: string) {
   const s = formula.toString();
   return Boolean(s.match(ALEPH_FORMULA_REGEX));
 }
+type ParsedAlephFormulaWithoutArgs = {
+  nFormulas: number;
+  alephFormula?: undefined;
+  input?: undefined;
+  alephFormulaArgs?: undefined;
+};
+type ParsedAlephFormulaWithArgs = {
+  nFormulas: number;
+  alephFormula: string;
+  input: string;
+  alephFormulaArgs: string[];
+};
+
+type ParsedAlephFormula = ParsedAlephFormulaWithoutArgs | ParsedAlephFormulaWithArgs;
 
 /**
  * Extract custom function arguments from a string containing an aleph formula.
@@ -46,15 +74,8 @@ export function isAlephFormula(formula) {
  * Note extra return values because refactored from getSingleAlephFormula.
  * Exactly one formula is supported, if nFormulas is not 1 the rest of the
  * return values are undefined.
- *
- * @returns {{
- *   nFormulas: int,
- *   alephFormulaArgs: string[],
- *   input: string,
- *   alephFormula: int,
- * }}
  */
-export function splitAlephFormula(formula) {
+export function splitAlephFormula(formula: string): ParsedAlephFormula {
   /**
    * Excel spreadsheet service returns numbers as numbers instead of strings,
    * while google sheets and spreadjs returns numbers as strings.
@@ -71,7 +92,7 @@ export function splitAlephFormula(formula) {
   const alephFormulaArgsStr = arr[0][3]; // Pick out parameters to custom function call
 
   // Split the parameters on commas, skipping commas inside quoted text.
-  const alephFormulaArgs = [];
+  const alephFormulaArgs: string[] = [];
   const matches = Array.from(alephFormulaArgsStr.matchAll(quoteRegex));
   let latestIndex = 0;
   matches.forEach((elem, i) => {
@@ -105,26 +126,28 @@ export function splitAlephFormula(formula) {
   };
 }
 
-function hasArrayParameters(alephFormula) {
+function hasArrayParameters(alephFormula: string) {
   if (!alephFormula.toString().match(ARRAY_ARGUMENT_REGEX)) {
     return false;
   }
   const { alephFormulaArgs } = splitAlephFormula(alephFormula);
+  if (!alephFormulaArgs) {
+    throw new Error('Expected aleph formula to have args');
+  }
   return alephFormulaArgs.some((arg) => arg.match(ARRAY_ARGUMENT_STRICT_REGEX));
 }
 
+type AlephFormulaArgPiece = {
+  value: string;
+  isRef: boolean;
+};
+
 /**
  * Attempt to match basic custom function parameters (values and cell references),
  * fails for non-ALEPH formulas and non-trivial expressions.
  *
- * @param {string} formula
- * @returns {{
- *  alephFormula: string,
- *  alephFormulaArgs: { value: string, isRef: boolean }[],
- *  input: string
- * } | undefined}
  */
-export function getSingleAlephFormula(formula) {
+export function getSingleAlephFormula(formula: string) {
   // eslint-disable-next-line prefer-const -- alephFormulaArgs is modified
   let { nFormulas, alephFormulaArgs, input, alephFormula } = splitAlephFormula(formula);
   if (nFormulas === 0) {
@@ -134,25 +157,42 @@ export function getSingleAlephFormula(formula) {
     throw new DrillDownError(drilldownErrors.MULTIPLE_ALEPH_GETDATA_IN_FORMULA);
   }
 
-  function cleanPiece(value) {
+  function cleanPiece(value: string): AlephFormulaArgPiece {
     let isRef = true;
     // we know something is a ref if it is NOT inside quotes ;)
-    if (value[0] === '"' && value[value.length - 1] === '"') {
+    if (value.startsWith('"') && value.endsWith('"')) {
       value = value.slice(1, value.length - 1);
       isRef = false;
     }
     return { value: value.trim(), isRef };
   }
 
-  alephFormulaArgs = alephFormulaArgs.map((arg) => cleanPiece(arg));
+  if (!alephFormulaArgs) {
+    throw new Error('Expected aleph formula to have args');
+  }
+
+  const alephFormulaArgPieces = alephFormulaArgs.map((arg) => cleanPiece(arg));
 
   return {
     alephFormula,
-    alephFormulaArgs,
+    alephFormulaArgs: alephFormulaArgPieces,
     input,
   };
 }
 
+type ExploreRecording = {
+  data: {
+    input: {
+      spreadsheet: SpreadsheetInput;
+    };
+  };
+};
+
+type TableAndCellReference = {
+  table: string;
+  cellReferences: CellValue[];
+};
+
 /**
  *
  * Note this function defines "cellReference", it's just the value in
@@ -161,27 +201,29 @@ export function getSingleAlephFormula(formula) {
  * Note "clean" is an adjective, also meaning "cellReference" which
  * excludes references.  Or it's a verb, to replace the references
  * with values.
- *
- * @param {Array<{
- *  alephFormula: string,
- *  alephFormulaArgs: { value: string, isRef: boolean }[],
- *  input: string
- * }>} alephFormulas
- * @param {{ id: string, name: string, isActive: boolean }} activeSheet
- * @returns {Array<{
- *  table: string,
- *  cellReferences: string[]
- * }>}
  */
-export async function getTablesAndCleanCellReferences(alephFormulas, activeSheet, recording) {
+export async function getTablesAndCleanCellReferences(
+  alephFormulas: AlephFormulaArgPiece[][],
+  activeSheet: Sheet,
+  recording: ExploreRecording,
+): Promise<TableAndCellReference[]> {
   //
   /*
    * Split parts of a sheet!cell reference, use the default if sheet not present.
-   * @param {str} arg where the string identifies a cell
-   * @param {{ id: string, name: string, isActive: boolean }} sheet
-   * @returns {{ cell: string, sheet: { id: string, name: string, isActive: boolean }}}
    */
-  function splitCellAndSheet(arg, sheet) {
+
+  let sheetsCache: Sheet[] | undefined;
+  // Make getting sheets lazy, so it doesn't fire if not necessary
+  const getSheetsCached = async () => {
+    if (!sheetsCache) {
+      sheetsCache = await getSheets();
+    }
+    return sheetsCache;
+  };
+  async function splitCellAndSheet(
+    arg: { value: string },
+    sheet: Sheet,
+  ): Promise<{ cell: string; sheet: Sheet }> {
     /*
      * The assumption here is that a sheet ref is represented by
      * sheetName!B3 where the exclamation mark (!) is the separator between the sheet and the cell
@@ -194,7 +236,12 @@ export async function getTablesAndCleanCellReferences(alephFormulas, activeSheet
       // Both sheet and cell are present.
       const [sheetName, val] = splitStr;
       cell = val;
-      internalSheet = getSheets().find((s) => s.name === sheetName);
+      const referencedSheet = (await getSheetsCached()).find((s) => s.name === sheetName);
+      if (!referencedSheet) {
+        // Let the caller fall back to refreshing the formula and reading the cache.
+        throw new AlephError(`Could not find sheet "${sheetName}" referenced in an Aleph formula`);
+      }
+      internalSheet = referencedSheet;
     } else {
       // Input identifies a cell only, use with the default sheet.
       [cell] = splitStr;
@@ -203,58 +250,70 @@ export async function getTablesAndCleanCellReferences(alephFormulas, activeSheet
   }
 
   // De-duplicate and request values for all references
-  const refRanges = [];
-  alephFormulas.forEach((args) => {
-    args.forEach((arg) => {
+  const refRanges: { sheet: Sheet; range: string }[] = [];
+  for (const args of alephFormulas) {
+    for (const arg of args) {
       if (arg.isRef) {
-        const { cell, sheet } = splitCellAndSheet(arg, activeSheet);
+        // No need to Promise.all since the only async call is cached
+        // oxlint-disable-next-line no-await-in-loop
+        const { cell, sheet } = await splitCellAndSheet(arg, activeSheet);
         refRanges.push({
           sheet,
           range: cell,
         });
       }
-    });
-  });
+    }
+  }
 
   const optimizedSheetRangesPairList = getOptimizedSheetRangesPairsList(refRanges);
   const spreadsheetService = Configuration.getSpreadsheetService();
   const serviceResult = await spreadsheetService.getRangesValues(optimizedSheetRangesPairList);
 
   if (recording) {
-    const idToSheetMap = recording.data.input.spreadsheet.sheets.reduce(
+    const idToSheetMap = (recording.data.input.spreadsheet.sheets ?? []).reduce<Record<string, Sheet>>(
       (acc, sheet) => ({ ...acc, [sheet.id]: sheet }),
       {},
     );
 
-    recording.data.input.spreadsheet.sheetRangesValues = Object.entries(serviceResult).reduce(
+    recording.data.input.spreadsheet.sheetRangesValues = Object.entries(serviceResult).reduce<
+      NonNullable<ExploreRecording['data']['input']['spreadsheet']['sheetRangesValues']>
+    >(
       (acc, [id, values]) => ({
         ...acc,
         [idToSheetMap[id].name]: { ...acc[idToSheetMap[id].name], ...values },
       }),
-      recording.data.input.spreadsheet.sheetRangesValues,
+      recording.data.input.spreadsheet.sheetRangesValues ?? {},
     );
   }
 
   // Build and return a new list with references replaced by values.
   // Note the outer list has one item per input formula.
-  return alephFormulas.map((args) => {
-    const cleanValues = args.map((arg) => {
-      // Note args are to ALEPH_GETINTEGRATIONDATA(tableName, dimension, value, ...)
-      if (arg.isRef) {
-        const { cell, sheet } = splitCellAndSheet(arg, activeSheet);
-        if (arg.isRef && serviceResult[sheet.id] && serviceResult[sheet.id][cell]) {
-          return serviceResult[sheet.id][cell][0][0];
-        }
-        return cell;
-      }
-      return arg.value;
-    });
-    const [tableName, ...cellReferences] = cleanValues;
-    return { table: tableName, cellReferences };
-  });
+  return Promise.all(
+    alephFormulas.map(async (args) => {
+      const cleanValues = await Promise.all(
+        args.map(async (arg) => {
+          // Note args are to ALEPH_GETINTEGRATIONDATA(tableName, dimension, value, ...)
+          if (arg.isRef) {
+            const { cell, sheet } = await splitCellAndSheet(arg, activeSheet);
+            if (serviceResult[sheet.id]?.[cell]) {
+              return serviceResult[sheet.id][cell][0][0];
+            }
+            return cell;
+          }
+          return arg.value;
+        }),
+      );
+      const [tableName, ...cellReferences] = cleanValues;
+      return { table: tableName as string, cellReferences };
+    }),
+  );
 }
 
-export async function parseAndCleanAlephFormula(activeSheet, formula, recording) {
+export async function parseAndCleanAlephFormula(
+  activeSheet: Sheet,
+  formula: string,
+  recording: ExploreRecording,
+) {
   const singleFormula = getSingleAlephFormula(formula);
   if (!singleFormula) {
     return null;
@@ -293,9 +352,9 @@ export async function parseAndCleanAlephFormula(activeSheet, formula, recording)
 /**
  * Copied directly from addin-components
  */
-export function getRowDimensions(table, dimensionFilterMap) {
-  const parentDimensionTextsSet = new Set([]);
-  const filters = Object.keys(dimensionFilterMap).map((dimension) => table.getDimensionByValue(dimension));
+export function getRowDimensions(table: Table, dimensionFilterMap: Record<string, unknown>) {
+  const parentDimensionTextsSet = new Set();
+  const filters = Object.keys(dimensionFilterMap).map((dimension) => table.getDimensionByValue(dimension)!);
   if (table.dimensionGroups) {
     const { dimensionGroups } = table;
     filters.forEach(({ text }) => {
@@ -320,8 +379,8 @@ export function getRowDimensions(table, dimensionFilterMap) {
 /**
  * Copied directly from addin-components
  */
-export function getColumnDimensions(table, dimensionFilterMap) {
-  const columnDimensions = [];
+export function getColumnDimensions(table: Table, dimensionFilterMap: Record<string, unknown[]>) {
+  const columnDimensions: (Dimension | undefined)[] = [];
   const dimensionValues = Object.keys(dimensionFilterMap);
   dimensionValues.forEach((value) => {
     const isVariableDimension = Object.keys(dimensionFilterMap[value]).length > 1;
@@ -336,7 +395,7 @@ export function getColumnDimensions(table, dimensionFilterMap) {
  * Returns cloned dimensions that are not hidden.
  * Used for drilldown options where hidden columns should be excluded.
  */
-export function getVisibleDimensions(dimensions) {
+export function getVisibleDimensions(dimensions: Dimension[]) {
   return dimensions.filter((dim) => !dim.hidden).map((dim) => dim.clone());
 }
 
@@ -346,7 +405,7 @@ export function getVisibleDimensions(dimensions) {
  * Given a dimensionFilterMap, that is a map where the keys are dimension values and the values are maps between
  * filter values and the actual filter, it builds the required structure for passing the filters to ExploreUtils.
  */
-export function getFilterGroup(dimensionFilterMap) {
+export function getFilterGroup(dimensionFilterMap: Record<string, Record<string, FilterGroup['body']>>) {
   let variableDimensionsCount = 0;
 
   const dimensionValues = Object.keys(dimensionFilterMap);
@@ -362,13 +421,13 @@ export function getFilterGroup(dimensionFilterMap) {
     const rootOperator = filterGroup.operator;
     const filterValuesFilterMap = dimensionFilterMap[dimensionValue];
     const isVariableDimension = Object.keys(filterValuesFilterMap).length > 1;
-    variableDimensionsCount += isVariableDimension;
+    variableDimensionsCount += isVariableDimension ? 1 : 0;
 
     if (variableDimensionsCount > 1) {
       throw new DrillDownError(drilldownErrors.MULTIPLE_VARIABLE_DIMENSIONS);
     }
 
-    const filterValues = [];
+    const filterValues: FilterGroup['body'] = [];
     Object.values(filterValuesFilterMap).forEach((values) => filterValues.push(...values));
 
     if (isVariableDimension) {
@@ -384,47 +443,58 @@ export function getFilterGroup(dimensionFilterMap) {
     } else if (rootOperator) {
       filterGroup.body.push(...filterValues);
     } else {
-      [filterGroup] = filterValues;
+      [filterGroup] = filterValues as [FilterGroup];
     }
   });
 
   return filterGroup;
 }
 
-const pushAllValuesToFilterGroup = (filterGroup, dimensionName, arrayOfValues) => {
+const pushAllValuesToFilterGroup = (
+  filterGroup: FilterGroup,
+  dimension: Dimension,
+  arrayOfValues: FilterValue[],
+) => {
   arrayOfValues.forEach((value) => {
     const filter = new Filter({
       operator: ComparisonOperator.createEqualsOperator(),
-      dimension: dimensionName,
+      dimension,
       value,
     });
     filterGroup.body.push(filter);
   });
 };
 
+type FilledTableAndCellReference = {
+  table: Table;
+  cellReferences: string[];
+};
+
 /**
  *
  * Given an array of formulas with they dimension and values, we check for varying dimensions and build
  * the required structure with the values for passing the filters to ExploreUtils.
  */
-export function getFilterGroupAndVaryingDimensionsForScalarFormulas(formulasWithPureDimensions) {
-  const dimensionAndValues = {};
-  const varyingDimensionNames = new Set();
-  const dimensionNameToDimension = {};
+export function getFilterGroupAndVaryingDimensionsForScalarFormulas(
+  formulasWithPureDimensions: FilledTableAndCellReference[],
+) {
+  const dimensionAndValues: Record<string, { valuesKeys: Set<string>; values: FilterValue[][] }> = {};
+  const varyingDimensionNames = new Set<string>();
+  const dimensionNameToDimension: Record<string, Dimension> = {};
 
   // For each formula we accumulate the values of each dimension, since we implemented wildcards
   // we can expect having multiple values for the same dimension on the formula, so both to recognize the varying
   // dimensions and then to properly build the filter group we need to keep the information about each individual
   // formula values to check against the rest.
   formulasWithPureDimensions.forEach(({ cellReferences, table }, formulaIndex) => {
-    const formulaDimensionsAndValues = {};
+    const formulaDimensionsAndValues: Record<string, string[]> = {};
 
     cellReferences.forEach((dimension, index) => {
       if (index % 2 === 0) {
         const value = cellReferences[index + 1];
         const dimensionOption =
           table.getDimensionByText(dimension, { ignoreCase: true }) ||
-          table.getDimensionByValue(dimension, { ignoreCase: true });
+          table.getDimensionByValue(dimension, { ignoreCase: true })!;
 
         const dimensionName = dimensionOption.value;
 
@@ -443,7 +513,7 @@ export function getFilterGroupAndVaryingDimensionsForScalarFormulas(formulasWith
 
       if (!dimensionAndValues[dimensionName]) {
         dimensionAndValues[dimensionName] = {
-          valuesKeys: new Set(),
+          valuesKeys: new Set<string>(),
           values: [],
         };
       }
@@ -503,7 +573,7 @@ export function getFilterGroupAndVaryingDimensionsForScalarFormulas(formulasWith
  *
  * Returns true iff all tables in the list have the same id.
  */
-export function tablesMatch(tables) {
+export function tablesMatch(tables: { table: { id: string } }[]) {
   const [firstTable] = tables;
   const allTablesMatch = tables.every((t) => t.table.id === firstTable.table.id);
   return allTablesMatch;
@@ -516,7 +586,7 @@ export function tablesMatch(tables) {
  * Returns true iff all tables in the list match (requesting identical
  * sets of valid dimension names).
  */
-export function dimensionsMatch(tables) {
+export function dimensionsMatch(tables: FilledTableAndCellReference[]) {
   const [firstTable] = tables;
   return tables.every((other) => {
     //
@@ -551,7 +621,11 @@ export function dimensionsMatch(tables) {
   });
 }
 
-function connectAreaContainsRange({ sheet, rows, columns }, activeSheet, range) {
+function connectAreaContainsRange(
+  { sheet, rows, columns }: Connect,
+  activeSheet: SheetWithoutActive,
+  range: Range,
+) {
   if (sheet.name !== activeSheet.name) {
     return false;
   }
@@ -572,8 +646,8 @@ function connectAreaContainsRange({ sheet, rows, columns }, activeSheet, range)
     const lastRowSubrange = DisconnectRange.parse(rowValues[rowValues.length - 1].range).subranges[0];
 
     const valueColumnCell = new Cell({
-      row: lastRowSubrange.cellStart.row - 1,
-      column: lastRowSubrange.cellStart.column + 1,
+      row: (lastRowSubrange.cellStart.row ?? NaN) - 1,
+      column: (lastRowSubrange.cellStart.column ?? NaN) + 1,
     });
 
     columnDisconnectRange = new DisconnectRange({
@@ -591,11 +665,15 @@ function connectAreaContainsRange({ sheet, rows, columns }, activeSheet, range)
   return isRowBetweenSubranges && isColumnBetweenSubranges;
 }
 
-const hasActiveSpecialConnectFilter = (connect) =>
+const hasActiveSpecialConnectFilter = (connect: Connect) =>
   Boolean(connect.rowSettings?.specialConnectFilter?.isOn) ||
   Boolean(connect.columnSettings?.specialConnectFilter?.isOn);
 
-export async function findConnectedTablesInRange(connectedTables, activeSheet, range) {
+export async function findConnectedTablesInRange(
+  connectedTables: Connect[],
+  activeSheet: SheetWithoutActive,
+  range: Range,
+) {
   const rawMatches = connectedTables.filter((connect) =>
     connectAreaContainsRange(connect, activeSheet, range),
   );
@@ -617,7 +695,9 @@ export async function findConnectedTablesInRange(connectedTables, activeSheet, r
       }
       try {
         const { error, connectedTableWithSettings } = await getConnectWithModifiedDimensionRanges(connect);
-        if (error.hasError) return undefined;
+        if (error.hasError) {
+          return undefined;
+        }
 
         return connectAreaContainsRange(connectedTableWithSettings, activeSheet, range) ? connect : undefined;
       } catch {
@@ -631,8 +711,8 @@ export async function findConnectedTablesInRange(connectedTables, activeSheet, r
   return narrowedMatches.filter(Boolean);
 }
 
-export const findExploreInRange = (explores, activeSheet, range) => {
-  const explore = explores.find((e) => e.sheet.id === activeSheet.id);
+export const findExploreInRange = (explores: Explore[], activeSheet: Sheet, range: Range) => {
+  const explore = explores.find((e) => e.sheet!.id === activeSheet.id);
   if (!explore) {
     return undefined;
   }
@@ -645,8 +725,8 @@ export const findExploreInRange = (explores, activeSheet, range) => {
       column: explore.originColumnIndex,
     }),
     cellEnd: new Cell({
-      row: start + explore.previousRowsCount,
-      column: explore.originColumnIndex + explore.previousColumnsCount,
+      row: start + explore.previousRowsCount!,
+      column: explore.originColumnIndex + explore.previousColumnsCount!,
     }),
   });
   const containsRows = exploreRange.containsRows(range);
@@ -660,7 +740,7 @@ export const findExploreInRange = (explores, activeSheet, range) => {
 /**
  * Copied directly from addin-components
  */
-export async function getProcessedConnectedTableForDrillDown(connectedTable, range) {
+export async function getProcessedConnectedTableForDrillDown(connectedTable: ConnectedTable, range: Range) {
   const processFilters = () =>
     Promise.all(
       Object.values(connectedTable.filters).map(async ({ range: filterRange, dimension }) => {
@@ -688,13 +768,13 @@ export async function getProcessedConnectedTableForDrillDown(connectedTable, ran
 
         const value = await getRangeValues(connectedTable.sheet, normalizedRange);
         return {
-          dimension: normalizeName(dimension.value),
+          dimension: normalizeName(dimension!.value),
           values: [value[0][0]],
         };
       }),
     );
 
-  const processRows = async () =>
+  const processRows = () =>
     Promise.all(
       Object.values(connectedTable.rows).map(async ({ range: rowRangeA1, dimension }) => {
         const rowRange = DisconnectRange.parse(rowRangeA1).subranges[0];
@@ -712,13 +792,13 @@ export async function getProcessedConnectedTableForDrillDown(connectedTable, ran
           }),
         );
         return {
-          dimension: normalizeName(dimension.value),
+          dimension: normalizeName(dimension!.value),
           values: rowValues.map((rowValue) => rowValue[0]),
         };
       }),
     );
 
-  const processColumns = async () =>
+  const processColumns = () =>
     Promise.all(
       Object.values(connectedTable.columns || {}).map(async ({ range: columnRangeA1, dimension }) => {
         const columnRange = DisconnectRange.parse(columnRangeA1).subranges[0];
@@ -736,7 +816,7 @@ export async function getProcessedConnectedTableForDrillDown(connectedTable, ran
           }),
         );
         return {
-          dimension: normalizeName(dimension.value),
+          dimension: normalizeName(dimension!.value),
           values: columnValues[0],
         };
       }),
@@ -752,9 +832,9 @@ export async function getProcessedConnectedTableForDrillDown(connectedTable, ran
   return { processedFilters, processedRows, processedColumns, processedMeasures };
 }
 
-export function buildAlephFormulasMap(formulasMap) {
+export function buildAlephFormulasMap(formulasMap: Record<string, string>) {
   // Filter (using reduce) and keep formulas that look like aleph custom function calls
-  const alephFormulasMap = {};
+  const alephFormulasMap: Record<string, string> = {};
 
   // eslint-disable-next-line no-restricted-syntax
   for (const [cellA1, cellFormula] of Object.entries(formulasMap)) {
@@ -772,3 +852,141 @@ export function buildAlephFormulasMap(formulasMap) {
 
   return alephFormulasMap;
 }
+
+type DimensionFilterMap = Record<string, string[]>;
+
+export function getFilterGroupNew(
+  table: Table,
+  addinFilterGroup: AddinCoreExploreFilterGroup | AddinCoreExploreFilter,
+  dimToValuesMap: DimensionFilterMap,
+) {
+  const initFilters = Object.entries(dimToValuesMap).map(([key, values]) => {
+    const valuesArray = Array.from(new Set(values));
+
+    if (valuesArray.length === 1) {
+      const value = valuesArray[0];
+
+      // using Equals here as wildcards aren't supported in IN filters
+      return {
+        dimension: table.getDimensionByAnyMatch(key)?.value ?? key,
+        value,
+        operator: 'Equals' as const,
+      };
+    }
+
+    return {
+      dimension: table.getDimensionByAnyMatch(key)?.value ?? key,
+      value: valuesArray,
+      operator: 'In' as const,
+    };
+  });
+
+  const cleanExploreFilters = addinCoreFilterToFilterGroup(addinFilterGroup);
+
+  const realFilters = appendAndFilterToFilterGroup(cleanExploreFilters, initFilters);
+
+  return simpleExploreFilterToAddinCoreFilter(realFilters, table);
+}
+
+export function getColumnDimensionsNew(table: Table, dimensionFilterMap: DimensionFilterMap): Dimension[] {
+  const columnDimensions: Dimension[] = [];
+  Object.entries(dimensionFilterMap).forEach((d) => {
+    const [dimension, values] = d;
+    const isVariableDimension = values.length > 1;
+    if (isVariableDimension) {
+      const columnDimension = table.getDimensionByValue(dimension);
+      if (columnDimension) {
+        columnDimensions.push(columnDimension);
+      }
+    }
+  });
+
+  return columnDimensions;
+}
+
+export function getRowDimensionsNew(table: Table, dimensionFilterMap: DimensionFilterMap): Dimension[] {
+  const parentDimensionTextsSet = new Set<string>();
+  const filters = Object.keys(dimensionFilterMap).map((dimension) => table.getDimensionByAnyMatch(dimension));
+  if (table.dimensionGroups) {
+    const { dimensionGroups } = table;
+    filters.forEach((filter) => {
+      if (filter) {
+        const { text } = filter;
+        const dimensionGroup = dimensionGroups.find(({ dimensions }) => dimensions.includes(text));
+        if (dimensionGroup) {
+          const dimensionIndex = dimensionGroup.dimensions.indexOf(text);
+          if (dimensionIndex > 0) {
+            dimensionGroup.dimensions
+              .slice(0, dimensionIndex)
+              .forEach((parentDimension) => parentDimensionTextsSet.add(parentDimension));
+          }
+        }
+      }
+    });
+  }
+  const parentDimensionTexts = new Set(parentDimensionTextsSet);
+  return table.dimensions.filter(
+    ({ text, value }) =>
+      // @ts-expect-error Table is yet not fully typed...
+      !filters.find((dimension) => dimension.value === value) && !parentDimensionTexts.has(text),
+  );
+}
+
+export const processConnectMeasures = async (connectedTable: ConnectedTable) => {
+  if (!connectedTable.table) {
+    return [undefined];
+  }
+
+  const tableMeasures =
+    connectedTable.table && connectedTable.table.measures ? connectedTable.table.measures : [];
+
+  // If the connected table doesn't have measures defined, default to the first measure from the table
+  if (!connectedTable.measures?.range) {
+    return [tableMeasures[0]];
+  }
+
+  // Get the values from the measure range
+  const measureRange = DisconnectRange.parse(connectedTable.measures?.range);
+  const measureValuesGrid = await getSelectionValues(connectedTable.sheet, measureRange);
+
+  // Grab defined unique values only
+  const measureValues = Array.from(new Set(measureValuesGrid.flat().filter((value) => value !== undefined)));
+
+  // Fail if the connect brings multiple measures. We don't support this kind of drill down yet.
+  if (measureValues.length > 1) {
+    throw new DrillDownError(
+      'Drill down on connects that define multiple measures is not supported yet.',
+      'Drill down error',
+      {
+        shouldLog: false,
+      },
+    );
+  }
+
+  // Fail if the connect brings no measures. Weird case, just in case it happens.
+  if (measureValues.length === 0) {
+    throw new DrillDownError(
+      'This Connect was built with 1 measure. Since the table now has more, define 1 measure to drill down on it.',
+      'Drill down error',
+      { shouldLog: false },
+    );
+  }
+
+  // Parse the connect measure format into the measure pretty name and aggregator
+  const { measure, aggregator } = parseConnectMeasure(measureValues[0], {});
+
+  // Use the measure pretty name to bring the dimension object from the table
+  const matchingMeasure = connectedTable.table.getMeasureByText(measure);
+
+  // Fail if we couldn't find the measure in the table
+  if (!matchingMeasure) {
+    throw new DrillDownError(
+      "The measure you're trying to drill-down into no longer exists or is invalid, please re-check the measure names for this table and try again.",
+      'Drill down error',
+      { shouldLog: false },
+    );
+  }
+
+  // Return a copy of the match with the aggregator
+  return [new Dimension({ ...matchingMeasure, aggregator })];
+};
diff --git a/packages/addin-core/src/transformers/explores.ts b/packages/addin-core/src/transformers/explores.ts
index f89671d9b9..db8d171ec4 100644
--- a/packages/addin-core/src/transformers/explores.ts
+++ b/packages/addin-core/src/transformers/explores.ts
@@ -158,7 +158,7 @@ export type SpreadsheetInput = {
   bindings?: { id: string; range: string }[];
   entitiesMap?: Record<string, unknown>;
   sheets?: Sheet[];
-  sheetRangesValues?: Record<string, Record<string, unknown[][]> | undefined>;
+  sheetRangesValues?: Record<string, Record<string, unknown[][] | undefined> | undefined>;
   sheetRangesStyles?: Record<string, Record<string, SheetCellStyle[][]> | undefined>;
   activeRange?: {
     sheet: Sheet;
diff --git a/packages/addin-core/tests/operations/buildExplores/findConnectedTablesInRange.test.ts b/packages/addin-core/tests/operations/buildExplores/findConnectedTablesInRange.test.ts
index 15ea078628..32d133afc5 100644
--- a/packages/addin-core/tests/operations/buildExplores/findConnectedTablesInRange.test.ts
+++ b/packages/addin-core/tests/operations/buildExplores/findConnectedTablesInRange.test.ts
@@ -1,3 +1,4 @@
+import { Connect } from '@getaleph/global-common-utils';
 import { describe, it, expect, vi, beforeEach } from 'vitest';
 
 import { DisconnectRange } from '../../../src/entities';
@@ -11,20 +12,21 @@ vi.mock('../../../src/operations/connects', async (importOriginal) => ({
   getConnectWithModifiedDimensionRanges: vi.fn(),
 }));
 
-const ACTIVE_SHEET = { name: 'Sheet1' };
+const ACTIVE_SHEET = { id: 'sheet1', name: 'Sheet1' };
 
 const selection = (a1: string) => DisconnectRange.parse(a1).getBoundingBox();
 
-const makeConnect = (overrides = {}) => ({
-  id: 'connect',
-  name: 'Connect',
-  sheet: { name: 'Sheet1' },
-  rows: { r1: { range: 'A2:A10' } },
-  columns: { c1: { range: 'B1:D1' } },
-  rowSettings: {},
-  columnSettings: {},
-  ...overrides,
-});
+const makeConnect = (overrides = {}): Connect =>
+  ({
+    id: 'connect',
+    name: 'Connect',
+    sheet: { name: 'Sheet1' },
+    rows: { r1: { range: 'A2:A10' } },
+    columns: { c1: { range: 'B1:D1' } },
+    rowSettings: {},
+    columnSettings: {},
+    ...overrides,
+  }) as unknown as Connect;
 
 const makeFilteredConnect = (overrides = {}) =>
   makeConnect({
@@ -32,7 +34,7 @@ const makeFilteredConnect = (overrides = {}) =>
     name: 'Push',
     rowSettings: { specialConnectFilter: { range: 'E2:E10', isOn: true } },
     ...overrides,
-  });
+  }) as unknown as Connect;
 
 beforeEach(() => {
   vi.mocked(getConnectWithModifiedDimensionRanges).mockReset();
diff --git a/packages/addin-core/tests/operations/buildExplores/newUtils.test.ts b/packages/addin-core/tests/operations/buildExplores/newUtils.test.ts
index 8d1723014d..416ba8827c 100644
--- a/packages/addin-core/tests/operations/buildExplores/newUtils.test.ts
+++ b/packages/addin-core/tests/operations/buildExplores/newUtils.test.ts
@@ -3,7 +3,7 @@ import type { Mock } from 'vitest';
 
 import { initialize } from '../../../src';
 import { Dimension, Table } from '../../../src/entities';
-import { getFilterGroupNew, processConnectMeasures } from '../../../src/operations/buildExplores/newUtils';
+import { getFilterGroupNew, processConnectMeasures } from '../../../src/operations/buildExplores/utils';
 import {
   addinCoreFilterToFilterGroup,
   simpleExploreFilterToAddinCoreFilter,
diff --git a/packages/frontend-common-components/src/components/ActionButtons/DrillDownButton.tsx b/packages/frontend-common-components/src/components/ActionButtons/DrillDownButton.tsx
index b6f455c031..676981c654 100644
--- a/packages/frontend-common-components/src/components/ActionButtons/DrillDownButton.tsx
+++ b/packages/frontend-common-components/src/components/ActionButtons/DrillDownButton.tsx
@@ -1,6 +1,6 @@
+import { Dimension } from '@getaleph/addin-core';
 import { AlephError } from '@getaleph/frontend-common-utils';
 import { icons, Fab, NotificationContent } from '@getaleph/ui-components';
-import { ServerTable } from '@getaleph/workbook-utils';
 import React, { useCallback, useState } from 'react';
 
 import ErrorModal from '../general/ErrorModal';
@@ -10,7 +10,7 @@ import DrillDownByOptionsModal from './DrilldownByOptionsModal';
 export type DrillDownButtonProps = {
   onDrillDownClick: (selectedDimensions: string[], discoveryContext?: unknown) => Promise<void>;
   getDrillDownByOptions: () => Promise<{
-    dimensions: ServerTable['dimensions'];
+    dimensions: Dimension[];
     suggestedDimensions: string[];
     discoveryContext?: unknown;
   }>;
@@ -26,7 +26,7 @@ const DrillDownButton = ({
   const [isDrillingDown, setIsDrillingDown] = useState(false);
   const [drillDownByModalState, setDrillDownByModalState] = useState({
     visible: false,
-    dimensions: [] as ServerTable['dimensions'],
+    dimensions: [] as Dimension[],
     suggestedDimensions: [] as string[],
     discoveryContext: undefined as unknown,
   });
@@ -42,8 +42,8 @@ const DrillDownButton = ({
           icon: icons.selected,
           content: 'Drill down completed',
         });
-      } catch (err) {
-        const safeError = AlephError.fromError(err);
+      } catch (error) {
+        const safeError = AlephError.fromError(error);
         setError(safeError);
       } finally {
         setIsDrillingDown(false);
@@ -65,8 +65,8 @@ const DrillDownButton = ({
         suggestedDimensions,
         discoveryContext,
       }));
-    } catch (err) {
-      const safeError = AlephError.fromError(err);
+    } catch (error) {
+      const safeError = AlephError.fromError(error);
       setError(safeError);
     } finally {
       setIsLoading(false);
diff --git a/packages/frontend-common-components/src/components/ActionButtons/DrilldownByOptionsModal.tsx b/packages/frontend-common-components/src/components/ActionButtons/DrilldownByOptionsModal.tsx
index 9acbd9af76..17e24d6bfc 100644
--- a/packages/frontend-common-components/src/components/ActionButtons/DrilldownByOptionsModal.tsx
+++ b/packages/frontend-common-components/src/components/ActionButtons/DrilldownByOptionsModal.tsx
@@ -1,4 +1,4 @@
-import { ServerTable } from '@getaleph/global-common-utils';
+import { Dimension } from '@getaleph/addin-core';
 import {
   Combobox,
   ComboboxProps,
@@ -18,7 +18,7 @@ import { createPortal } from 'react-dom';
 const MODAL_WIDTH = '280px';
 
 type DrillDownByOptionsModalProps = {
-  drillDownDimensions: ServerTable['dimensions'];
+  drillDownDimensions: Dimension[];
   suggestedDimensions: string[];
   onCancel: () => void;
   onDrillDown: (selectedDimensions: string[]) => void;
diff --git a/packages/global-common-utils/src/connectUtils.ts b/packages/global-common-utils/src/connectUtils.ts
new file mode 100644
index 0000000000..00ef679508
--- /dev/null
+++ b/packages/global-common-utils/src/connectUtils.ts
@@ -0,0 +1,61 @@
+import { SheetWithoutActive, type ServerTable } from './types';
+
+export type ConnectDirection = 'bidirectional' | 'pull' | 'push';
+
+export type ConnectSettings = {
+  direction: ConnectDirection;
+  pullEmptyAsZero: boolean;
+  pushEmptyAsZero: boolean;
+  specialConnectFilter?: {
+    isOn?: boolean;
+  };
+};
+
+export type ConnectRowSettings = {
+  pull: { type: string };
+  push: { type: string };
+  specialConnectFilter?: {
+    isOn?: boolean;
+  };
+};
+
+export type ConnectDimension = {
+  value: string;
+  text: string;
+  id: string;
+  name: string;
+  prettyName: string;
+  table: string;
+  integrationTable?: string;
+  integration?: string;
+  tenant: string;
+  virtual: boolean;
+  measure: boolean;
+  calculatedMeasure: boolean;
+  description?: string;
+  isPrimaryKey: boolean;
+  calculatedDimension: boolean;
+  tooManyValues: boolean;
+  isAlephTime: boolean;
+  timeType: string;
+  dataType: string;
+  dimensionGroupId: string;
+  type: string;
+  index: number;
+};
+
+export type Connect = {
+  _id: string;
+  columns: Record<string, { dimension: ConnectDimension; id: string; range: string }>;
+  rows: Record<string, { dimension: ConnectDimension; id: string; range: string }>;
+  dataTable: string;
+  filters: object;
+  id: string;
+  name: string;
+  sheet: SheetWithoutActive;
+  sheetId: string;
+  table: ServerTable;
+  settings?: ConnectSettings;
+  rowSettings?: ConnectRowSettings;
+  columnSettings?: ConnectRowSettings;
+};
diff --git a/packages/global-common-utils/src/index.ts b/packages/global-common-utils/src/index.ts
index d66bc31ce3..0480601fd8 100644
--- a/packages/global-common-utils/src/index.ts
+++ b/packages/global-common-utils/src/index.ts
@@ -40,6 +40,7 @@ export * from './asyncTracker';
 export * from './tiptap-parser';
 export * from './colorUtils';
 export * from './getA1Components';
+export * from './connectUtils';
 export * as Colors from './colorUtils';
 export * as Palette from './palettes';
 export * as TableThemes from './tableThemes';
diff --git a/packages/global-common-utils/src/types.ts b/packages/global-common-utils/src/types.ts
index f44ae9a7a7..3132a54056 100644
--- a/packages/global-common-utils/src/types.ts
+++ b/packages/global-common-utils/src/types.ts
@@ -99,6 +99,11 @@ export type DimensionGroup = {
   table: string;
   tooManyValues: boolean;
   hiddenDimensions: string[];
+  isHyperlink?: boolean;
+  // One array of custom calculated items per dimension in `dimensions`.
+  customCalculatedItems?: CustomCalculatedItem[][];
+  createdAt?: string;
+  updatedAt?: string;
   __v: number;
 };
 
@@ -366,8 +371,12 @@ export type Sheet = {
   id: string;
   isActive: boolean;
 };
+export type SheetWithoutActive = Omit<Sheet, 'isActive'> & {
+  isActive?: boolean;
+};
+
 export type SheetRangesPairsList = {
-  sheet: Sheet;
+  sheet: SheetWithoutActive;
   ranges: string[];
 };
 
diff --git a/packages/workbook-utils/src/connect.ts b/packages/workbook-utils/src/connect.ts
index a624777e21..de6462170e 100644
--- a/packages/workbook-utils/src/connect.ts
+++ b/packages/workbook-utils/src/connect.ts
@@ -1,55 +1,7 @@
-import { ServerTable } from './useWorkbookTables';
-
-export type ConnectDirection = 'bidirectional' | 'pull' | 'push';
-
-export type ConnectSettings = {
-  direction: ConnectDirection;
-  pullEmptyAsZero: boolean;
-  pushEmptyAsZero: boolean;
-};
-
-export type ConnectRowSettings = {
-  pull: { type: string };
-  push: { type: string };
-};
-
-export type ConnectDimension = {
-  value: string;
-  text: string;
-  id: string;
-  name: string;
-  prettyName: string;
-  table: string;
-  integrationTable?: string;
-  integration?: string;
-  tenant: string;
-  virtual: boolean;
-  measure: boolean;
-  calculatedMeasure: boolean;
-  description?: string;
-  isPrimaryKey: boolean;
-  calculatedDimension: boolean;
-  tooManyValues: boolean;
-  isAlephTime: boolean;
-  timeType: string;
-  dataType: string;
-  dimensionGroupId: string;
-  type: string;
-  index: number;
-};
-
-export type Connect = {
-  _id: string;
-  columns: Record<string, { dimension: ConnectDimension; id: string; range: string }>;
-  rows: Record<string, { dimension: ConnectDimension; id: string; range: string }>;
-  dataTable: string;
-  filters: object;
-  id: string;
-  name: string;
-  sheet: { id: string; name: string };
-  sheetId: string;
-  table: ServerTable;
-  settings?: ConnectSettings;
-  rowSettings?: ConnectRowSettings;
-  columnSettings?: ConnectRowSettings;
-};
+export type {
+  ConnectDirection,
+  ConnectSettings,
+  ConnectRowSettings,
+  ConnectDimension,
+  Connect,
+} from '@getaleph/global-common-utils';
