================================================================================
1. PROMPT RECEIVED (verbatim)
================================================================================
You are finder angle "SIMPLIFICATION" — one of 10 parallel finder agents in a maximum-effort code review. Your job is CLEANUP: flag unnecessary complexity the diff ADDS — redundant or derivable state, copy-paste with slight variation, deep nesting, dead code left behind — and name the simpler form that does the same job.

REVIEW SCOPE: the diff of git branch `colton/typ-797-convert-packagesaddin-coresrcoperationsbuildexploresutilsjs` vs `trunk`, in repo /Users/colton/code/monorepo.
- Full unified diff is saved at: /Users/colton/code/monorepo/code-review-notes/00-review.diff  (READ THIS FIRST, in full)
- Regenerate with: `git diff trunk...HEAD` run from /Users/colton/code/monorepo
- What the PR does: converts packages/addin-core/src/operations/buildExplores/utils.js to TypeScript, merges the deleted newUtils.ts into utils.ts, moves the `Connect` type to global-common-utils, adds `SheetWithoutActive`, and touches apps/gs-addin formulaUtils.ts, apps/server alephFunction.ts, addin-components, frontend-common-components.

Concrete hypotheses to investigate (confirm or refute each with file:line evidence, and give the simpler replacement):
1. `apps/gs-addin/src/client/utils/formulaUtils.ts` `argRowsColumns`: the reformatting produced the bizarre shape `if (!m) // Non-ranges are always a single cell\n{\n  return ...;\n}` — comment stranded between condition and brace, twice. Give the clean form.
2. `apps/gs-addin/src/client/utils/formulaUtils.ts` `Boolean(ALEPH_FORMULA_REGEX.test(s))` — `.test()` already returns boolean, so `Boolean(...)` is redundant. Also the preceding `const s = formula.toString()` on an already-`string`-typed parameter.
3. `packages/addin-core/src/operations/buildExplores/utils.ts`: `const parentDimensionTexts = new Set(parentDimensionTextsSet);` — a pointless copy of a Set that is never used again (appears in BOTH `getRowDimensions` and `getRowDimensionsNew`). Confirm and name the fix.
4. `utils.ts` `getRowDimensionsNew` and `getRowDimensions` are near-identical (one uses `getDimensionByAnyMatch`, the other `getDimensionByValue`); same for `getColumnDimensions`/`getColumnDimensionsNew`. Now that they live in the SAME file after the newUtils merge, quantify the copy-paste and propose the parameterised single function. Check whether the old non-`New` variants still have live callers (grep `getRowDimensions`, `getColumnDimensions`, `getFilterGroup` across apps/ and packages/, excluding node_modules and dist).
5. `utils.ts` `hasArrayParameters` and `getSingleAlephFormula` each contain the identical three-line block `if (!alephFormulaArgs) { throw new Error('Expected aleph formula to have args'); }`. Is there a single-place fix (e.g. making `ParsedAlephFormula` a proper discriminated union with `nFormulas: 1` on the with-args member so TS narrows)? Also check `apps/server/src/operations/alephFunction.ts` line ~102 `if (!parsed || !parsed.alephFormula)` — is that a third instance of the same workaround?
6. `utils.ts` three separate duplicate import statements from the same modules (`@getaleph/global-common-utils` twice, `../../transformers` twice, `../connects` twice). Confirm exact line numbers.
7. `utils.ts` `type ParsedAlephFormulaWithoutArgs` uses `alephFormula?: undefined; input?: undefined; alephFormulaArgs?: undefined;` — is the explicit `?: undefined` triple needed, or would a discriminated union / `Partial<>` be simpler?
8. `getTablesAndCleanCellReferences`: `splitCellAndSheet` is now called TWICE for every ref arg (once in the `for` loop building `refRanges`, once again in the final `Promise.all` map). Is the second pass derivable from the first (i.e. could `refRanges` carry the resolved `{cell, sheet}` and be reused)? Also: `splitCellAndSheet` is `async` but its only await is a cached call — could the cache be resolved once up front (`const sheets = await getSheets()`) so the function stays synchronous and the `for`-loop + `oxlint-disable no-await-in-loop` + `Promise.all(map(async …))` all collapse?
9. `packages/workbook-utils/src/connect.ts` is now a 7-line pure re-export file. Should it exist at all, or should consumers import from `@getaleph/global-common-utils` directly? Grep who imports `Connect` from `@getaleph/workbook-utils`.
10. Dead code left behind: `packages/addin-core/tests/operations/buildExplores/newUtils.test.ts` still named `newUtils.test.ts` and its top-level `describe('newUtils')` while the module is gone; check whether a `utils.test.*` already exists next to it that it should merge into. Also check whether `// eslint-disable-next-line prefer-const -- alephFormulaArgs is modified` in `getSingleAlephFormula` is now stale (the variable may no longer be reassigned).
11. `processRows`/`processColumns` in `getProcessedConnectedTableForDrillDown` lost their `async` keyword while `processFilters` kept `const processFilters = () =>` — check consistency and whether the wrappers are needed at all (they are each called exactly once).

MANDATORY NOTE-TAKING (explicit user request): You MUST write a detailed plain-text notes file at
/Users/colton/code/monorepo/code-review-notes/07-simplification.txt
using the Write tool, BEFORE returning your final answer. It MUST contain, in this order:
1. "PROMPT RECEIVED" — the verbatim full text of this prompt.
2. "WHAT I AM LOOKING FOR"
3. "GOALS"
4. "METHOD / HOW I ACCOMPLISHED IT" — chronological log: every command run, every file read (absolute path + line ranges), what you saw, what you concluded. Include dead ends and refuted hypotheses.
5. "CANDIDATES" — file, line, summary, failure scenario, quoted code evidence.

OUTPUT: return a JSON array of up to 8 candidate findings, each {"file","line","summary","failure_scenario"}. For this angle, `failure_scenario` must state the CONCRETE COST (what is redundant/dead/harder to maintain, and the simpler form) rather than a crash. RECALL mode — err on the side of surfacing. If you truly find nothing, return [].


================================================================================
2. WHAT I AM LOOKING FOR
================================================================================
Unnecessary complexity that THIS DIFF ADDS (not pre-existing debt unless the diff
moved/rewrote it), specifically:
  - Redundant state: variables/copies/collections derivable from something already
    in scope (Set copies, re-derived values, doubled computations).
  - Copy-paste with slight variation: near-identical functions/blocks that a single
    parameterised function would cover. Especially relevant here because the PR
    MERGED newUtils.ts into utils.ts, putting near-duplicate pairs side by side.
  - Scaffolding added to work around a self-inflicted constraint (e.g. making a
    function async forces a lazy promise cache + `no-await-in-loop` disables +
    nested Promise.all).
  - Type-level escape hatches added instead of fixing the root type: `as unknown as
    X`, `!`, `@ts-expect-error`, duplicated runtime `throw`s that exist only to
    narrow a badly-shaped union.
  - Dead code / stale artefacts left behind: files named after deleted modules,
    eslint-disable comments whose stated reason no longer holds, pure re-export
    shim files, unreachable guards.
  - Mechanical-reformat damage: brace/comment placement that reads worse than the
    original.
For every item: name the SIMPLER FORM that does the same job.


================================================================================
3. GOALS
================================================================================
G1. Read the whole diff (1536 lines) before forming opinions.
G2. Confirm or refute each of the 11 supplied hypotheses with file:line evidence
    from the WORKING TREE (not just the diff), so line numbers are accurate.
G3. For each confirmed item, state the concrete cost + the simpler replacement.
G4. Establish whether the "New"/non-"New" duplicate pairs can simply be deleted
    (i.e. are the old variants dead?) — this changes the recommendation from
    "delete one" to "parameterise into one".
G5. Rank and return <=8 findings; record the runners-up here so nothing is lost.


================================================================================
4. METHOD / HOW I ACCOMPLISHED IT (chronological)
================================================================================

STEP 1. Read /Users/colton/code/monorepo/code-review-notes/00-review.diff lines
1-1250 (truncated at the 25k-token cap), then lines 1251-1536 to finish it.
Observed the full change set:
  - apps/addin-components/.../ActionButtonsWrapper.tsx: +1 line, a NEW
    `// @ts-expect-error TYPETODO: useDrilldown needs to be typed`.
  - apps/addin-components/src/hooks/useDrillDown.jsx: pure `e` -> `error` rename.
  - apps/gs-addin/src/client/utils/formulaUtils.ts: `!!s.match` ->
    `Boolean(REGEX.test(s))`; `replace(/\$/g,'')` -> `replaceAll('$','')`;
    brace-insertion reformat in `argRowsColumns` and `compareRowExtentsLessThan`.
  - apps/server/src/operations/alephFunction.ts: brace-insertion reformat +
    `if (!parsed)` -> `if (!parsed || !parsed.alephFormula)`.
  - apps/web-ui/.../Workbook.tsx: +1 NEW `@ts-expect-error`.
  - packages/addin-core: data-access Sheet -> SheetWithoutActive; Table gains
    typed `dimensionGroups`; newUtils.ts DELETED (155 lines) and its 4 exports
    appended verbatim to utils.ts; utils.js -> utils.ts with annotations.
  - packages/global-common-utils: new connectUtils.ts (Connect & friends moved
    out of workbook-utils), types.ts gains SheetWithoutActive + DimensionGroup
    fields.
  - packages/workbook-utils/src/connect.ts: 55 lines of type defs -> 7-line
    pure re-export.
  - packages/frontend-common-components: DrillDownButton /
    DrilldownByOptionsModal switch `ServerTable['dimensions']` -> `Dimension`
    (the addin-core CLASS).
  - tests: findConnectedTablesInRange.test.ts gains `as unknown as Connect`
    casts; newUtils.test.ts import repointed to `./utils`.

STEP 2. Read /Users/colton/code/monorepo/packages/addin-core/src/operations/buildExplores/utils.ts
in full (lines 1-993). This is the file the PR owns. Key observations, with
working-tree line numbers:
  - L4 `import { appendAndFilterToFilterGroup } from '@getaleph/global-common-utils';`
    L5 `import { CellValue, Connect, Sheet, SheetWithoutActive } from '@getaleph/global-common-utils';`
    => DUPLICATE import from same module. CONFIRMS hypothesis 6 (part 1).
  - L30-35 `import {...} from '../../transformers';`
    L36 `import { SpreadsheetInput } from '../../transformers';`
    => DUPLICATE. CONFIRMS hypothesis 6 (part 2).
  - L38 `import { parseConnectMeasure } from '../connects';`
    L39 `import { getConnectWithModifiedDimensionRanges } from '../connects';`
    => DUPLICATE. CONFIRMS hypothesis 6 (part 3). All three are newly introduced
    by this diff (the old utils.js had one `../connects` import; newUtils.ts
    contributed the other halves and they were pasted in unmerged).
  - L52-55 `isAlephFormula(formula: string) { const s = formula.toString(); ... }`
    `.toString()` on a `string` param — also at L84 and L130. The doc comment at
    L79-83 says numbers arrive from Excel, so the `string` annotation is either
    wrong or those three `.toString()` calls are dead. (Noted; overlaps another
    angle's remit, kept as runner-up.)
  - L56-69 the ParsedAlephFormula union. `nFormulas: number` on BOTH members, so
    `nFormulas === 1` does NOT discriminate. This is the root cause of the two
    runtime throws below. Re: hypothesis 7 — the `?: undefined` triple is NOT
    gratuitous: without it, `const { alephFormulaArgs } = splitAlephFormula(...)`
    would not typecheck at all (property missing on one member). So hypothesis 7
    is REFUTED as written; the real defect is the missing discriminant, not the
    `?: undefined`.
  - L129-138 `hasArrayParameters`: contains
        `if (!alephFormulaArgs) { throw new Error('Expected aleph formula to have args'); }`
  - L150-181 `getSingleAlephFormula`: contains the IDENTICAL 3-line throw at
    L170-172. CONFIRMS hypothesis 5. Further: by L170 the code has already
    returned for `nFormulas === 0` (L153) and thrown for `nFormulas > 1` (L156),
    so `nFormulas === 1`, so `splitAlephFormula` returned the full object =>
    the L170 guard is PROVABLY UNREACHABLE. It exists only to appease TS.
  - L151-152:
        `// eslint-disable-next-line prefer-const -- alephFormulaArgs is modified`
        `let { nFormulas, alephFormulaArgs, input, alephFormula } = ...`
    Scanned L150-181: `alephFormulaArgs` is never reassigned any more (the diff
    replaced `alephFormulaArgs = alephFormulaArgs.map(...)` with a new const
    `alephFormulaArgPieces`). So both the `let` and the disable comment (and its
    stated reason) are STALE. CONFIRMS hypothesis 10 (part 2).
  - L215-222 the `sheetsCache` + `getSheetsCached` hand-rolled lazy promise cache.
  - L223-250 `splitCellAndSheet` is now `async` solely because of that cache.
  - L253-266 the ref-collection loop was converted from `.forEach/.forEach` to
    `for..of/for..of` + `// oxlint-disable-next-line no-await-in-loop` + a comment
    apologising for it ("No need to Promise.all since the only async call is
    cached").
  - L291-309 the final pass was converted from `.map(args => args.map(arg => ...))`
    to `Promise.all(alephFormulas.map(async args => await Promise.all(args.map(
    async arg => ...))))`, and calls `await splitCellAndSheet(arg, activeSheet)`
    a SECOND time for every ref arg (first time was L259). CONFIRMS hypothesis 8,
    both halves.
  - L355-377 `getRowDimensions` vs L907-933 `getRowDimensionsNew`: 22 vs 26 lines,
    byte-identical except (a) `getDimensionByValue(d)!` vs
    `getDimensionByAnyMatch(d)`, (b) `forEach(({text}) => ...)` vs
    `forEach(filter => { if (filter) { const {text} = filter; ...`, (c) the final
    predicate uses `!` in one and `@ts-expect-error` in the other. CONFIRMS 4.
  - L372 `const parentDimensionTexts = new Set(parentDimensionTextsSet);` and
    L927 the same line. `parentDimensionTextsSet` is not used after either. Pure
    copy. CONFIRMS hypothesis 3.
  - L928-932: `@ts-expect-error Table is yet not fully typed...` survived the
    conversion in `getRowDimensionsNew` even though `Table` IS now typed (the diff
    added `dimensionGroups?: DimensionGroup[]` to Table.ts and removed the other
    three copies of this comment). The remaining error is `dimension.value` on a
    possibly-undefined element — fixable with `filters.some(d => d?.value === value)`.
    The twin at L373-376 instead uses `!` at L357. Inconsistent, both avoidable.
  - L382-392 `getColumnDimensions` returns `(Dimension | undefined)[]`; L891-905
    `getColumnDimensionsNew` returns `Dimension[]` by skipping undefined. Same
    computation ("more than one value for this dimension") written two ways
    (`Object.keys(map[value]).length > 1` vs `values.length > 1`). CONFIRMS 4.
  - L424 `variableDimensionsCount += isVariableDimension ? 1 : 0;` — replacement
    for `+= isVariableDimension`. Fine/necessary; `if (x) count += 1` would read
    better. Minor, not reported.
  - L744/L777/L801 `processFilters`/`processRows`/`processColumns` are now all
    `() => Promise.all(...)`. Hypothesis 11 REFUTED: dropping `async` made the
    three CONSISTENT (previously processFilters was sync-arrow and the other two
    async-arrow). They are each called once at L825-830 inside a `Promise.all`;
    inlining is possible but the names document intent, so not worth flagging.

STEP 3. Read /Users/colton/code/monorepo/apps/gs-addin/src/client/utils/formulaUtils.ts
lines 1-60 and 195-324.
  - L24-27:
        export function isAlephFormula(formula: string) {
          const s = formula.toString();
          return Boolean(ALEPH_FORMULA_REGEX.test(s));
        }
    `.test()` returns boolean => `Boolean(...)` redundant; `formula` is typed
    `string` => `.toString()` redundant. Simpler: `return ALEPH_FORMULA_REGEX.test(formula);`
    CONFIRMS hypothesis 2. (Separately: the regex has the /g flag so `.test()` is
    stateful across calls — a real behaviour change vs `.match()`. That is a
    correctness finding, left to the correctness angle; noted here because it
    means the "simpler form" must also drop /g or keep `.match()`.)
  - L212-222: CONFIRMS hypothesis 1 verbatim:
        if (!m) // Non-ranges are always a single cell
        {
          return { nRows: 1, nColumns: 1 };
        }
        ...
        if (!lastColumn) // Single cell reference
        {
          return { nRows: 1, nColumns: 1 };
        }
    Two occurrences. Clean form: comment on its own line ABOVE the `if`, brace on
    the `if` line.
  - L298-312 `compareRowExtentsLessThan`: brace expansion of four one-line
    returns. Verbose but house-style-consistent and harmless. NOT reported.

STEP 4. `grep -rn "getRowDimensions\b|getRowDimensionsNew|getColumnDimensions\b|getColumnDimensionsNew|getFilterGroup\b|getFilterGroupNew"` over apps/ packages/ (excluding node_modules, dist).
Result: index.js:277 and :601 call `getRowDimensions`; :605 calls
`getColumnDimensions`; :593 calls `getFilterGroup`; :847 and :903 call
`getRowDimensionsNew`; :861 and :919 call `getColumnDimensionsNew`; :839 and :911
call `getFilterGroupNew`.
CONCLUSION for G4: BOTH families are live. "Delete the old one" is not available;
the fix is to parameterise into one function per concern.

STEP 5. `ls packages/addin-core/tests/operations/buildExplores/` ->
  findConnectedTablesInRange.test.ts, indentUtils.test.js, newUtils.test.ts,
  utils.test.js
And `grep -n "export const getSheets" packages/addin-core/src/data-access/index.ts`
-> L59 `export const getSheets = (): Promise<Sheet[]> => {`  (confirms the await
in splitCellAndSheet is a real promise, and that resolving it once up front is
possible).

STEP 6. Read /Users/colton/code/monorepo/packages/addin-core/tests/operations/buildExplores/newUtils.test.ts
(1-253) and head of utils.test.js.
  - newUtils.test.ts L6 now imports from `.../buildExplores/utils`, but the FILE
    is still called newUtils.test.ts and L29 is `describe('newUtils', ...)` — a
    module name that no longer exists anywhere in the repo.
  - utils.test.js (same directory) L1-8 imports `getSingleAlephFormula`,
    `splitAlephFormula`, `getVisibleDimensions` from the SAME
    `.../buildExplores/utils` module; its describes are L12, L194, L219.
  So there are now two test files for one module, one of them named after a
  deleted module. CONFIRMS hypothesis 10 (part 1). Simpler: fold newUtils.test.ts
  into utils.test.js (or rename to utils.new.test.ts and drop the outer
  `describe('newUtils')` wrapper, which adds a level of nesting for nothing).

STEP 7. `cat packages/workbook-utils/src/connect.ts` -> exactly 7 lines, a pure
`export type { ConnectDirection, ConnectSettings, ConnectRowSettings,
ConnectDimension, Connect } from '@getaleph/global-common-utils';`
`grep` for consumers:
  - packages/workbook-utils/src/index.tsx:26 `export * from './connect';`
  - packages/frontend-common-components/src/components/InThisFile/types.ts:2
    `import { Sheet, Connect } from '@getaleph/workbook-utils';`
  - packages/frontend-common-components/src/components/InThisFile/CopyConnectedTableModal.tsx:13
    same.
So the shim serves exactly two files. CONFIRMS hypothesis 9: the file could be
deleted along with the `export * from './connect'` line, with the two importers
repointed at `@getaleph/global-common-utils` (they already have it as a workspace
dep — verified in frontend-common-components/package.json line 27). NOTE: `Sheet`
is NOT exported by workbook-utils/src/index.tsx by name, so it must come through
one of the `export *` barrels; a follow-up would need to check that. Because of
that uncertainty I demoted this to a runner-up rather than a top-8 finding.

STEP 8. `grep -rn "export type ServerTable"` -> only ONE definition, at
packages/global-common-utils/src/types.ts:153. So moving connectUtils.ts's
`table: ServerTable` to `./types` did not create a duplicate type. DEAD END /
refuted concern (I had suspected two competing ServerTable definitions).

STEP 9. `grep -n "no-duplicate-imports|no-duplicates" .oxlintrc.json` ->
`.oxlintrc.json:132  "no-duplicates": "warn"`. And
`grep -n '"lint"' packages/addin-core/package.json` ->
`oxlint --config ../../.oxlintrc.json ./src ./tests --max-warnings=520`.
So the three duplicate imports will each emit a warning that is silently absorbed
by a 520-warning budget — i.e. CI will not catch them; they must be caught in
review. Also confirmed `no-await-in-loop` IS enabled (line 29) so the
`oxlint-disable-next-line` at utils.ts:258 is load-bearing (not a dead comment) —
but it only exists because of the self-inflicted async.
`find . -maxdepth 3 -name "eslint.config.*" -not -path "*/node_modules/*"` -> no
results, i.e. this package lints with oxlint only; oxlint does honour
`// eslint-disable-next-line`, so the stale prefer-const disable at utils.ts:151
is functional but its stated reason is false.

STEP 10. Read /Users/colton/code/monorepo/apps/server/src/operations/alephFunction.ts
lines 85-134.
  - L102 `if (!parsed || !parsed.alephFormula) {` — the comment block L103-109
    directly beneath explains ONLY the `!parsed` case ("`parsed` is undefined
    when: ..."). The added `|| !parsed.alephFormula` is a third instance of the
    same TS-narrowing workaround (it is required only because
    `getSingleAlephFormula`'s return type leaks `string | undefined` from the
    undiscriminated union), and it silently widens the meaning of a documented
    branch. CONFIRMS hypothesis 5's third-instance question.

STEP 11. Read /Users/colton/code/monorepo/packages/addin-core/tests/operations/buildExplores/findConnectedTablesInRange.test.ts
lines 1-45.
  - L19-29 `const makeConnect = (overrides = {}): Connect => ({...}) as unknown as Connect;`
    Both an annotation AND a double cast — the cast alone or the annotation alone
    would do; together the annotation is decorative.
  - L31-37 `const makeFilteredConnect = (overrides = {}) => makeConnect({...}) as unknown as Connect;`
    `makeConnect` ALREADY returns `Connect`, so this second `as unknown as Connect`
    is a no-op cast of a `Connect` to a `Connect`. Pure noise. Runner-up.

STEP 12. Investigated the two NEW `@ts-expect-error`s.
  - `grep -n "getaleph" packages/frontend-common-components/package.json` -> it
    already depends on `@getaleph/addin-core` (line 25), so no new dependency
    edge; but the PROP TYPE changed from a plain data shape
    (`ServerTable['dimensions']`) to the addin-core `Dimension` CLASS.
  - `grep -n "drillDownDimensions" -A 12 .../DrilldownByOptionsModal.tsx` ->
    L37-41 the modal reads exactly two fields: `elem.prettyName` and `elem.name`.
  - The two consumers now need suppressions:
    apps/addin-components/.../ActionButtonsWrapper.tsx L125 and
    apps/web-ui/.../Workbook.tsx L1011.
  - `grep -n "handleGetDrillDownOptions" -A 15 apps/web-ui/.../Workbook.tsx` ->
    L803-814, a plain TS function; its dimensions come from
    `ExploresOperations.getDrillDownByOptions`. So at least the web-ui side is
    typed TS and could have been fixed properly.
  CONCLUSION: tightening a shared prop to a class, when the consumer only needs
  `{ name: string; prettyName: string }`, cost two brand-new `@ts-expect-error`
  suppressions (which suppress ALL errors on their line, not just this one).
  CLAUDE.md explicitly says to narrow with type guards rather than
  `@ts-expect-error`, and to fix the root type. This is added complexity.

STEP 13. Ranking. 13 confirmed candidates, 8 slots. Reported the 8 with the
highest maintenance cost / clearest simpler form; runners-up recorded below.

REFUTED / NOT REPORTED
  - Hypothesis 7 (`?: undefined` triple): needed for the destructuring call sites
    to typecheck. The defect is the missing discriminant, folded into candidate 3.
  - Hypothesis 11 (processRows/processColumns lost `async`): the change made the
    three sibling wrappers CONSISTENT. Not a defect.
  - Two competing `ServerTable` definitions: only one exists. Refuted.
  - `compareRowExtentsLessThan` brace expansion: verbose but consistent with the
    repo's `curly` style. Not a defect.
  - utils.ts:1 file-wide `// oxlint-disable unicorn/prefer-regexp-test`: still
    load-bearing (`.match` used at L54, L130, L137). Not stale.
  - `// eslint-disable-next-line no-restricted-syntax` / `no-continue` in
    buildAlephFormulasMap: pre-existing, unchanged by the diff.


================================================================================
5. CANDIDATES
================================================================================

--------------------------------------------------------------------------------
C1. packages/addin-core/src/operations/buildExplores/utils.ts:217
SUMMARY: Making `splitCellAndSheet` async (only so it can lazily fetch sheets)
forced four pieces of scaffolding into `getTablesAndCleanCellReferences` and made
every ref argument get resolved twice.
COST / SIMPLER FORM: The diff adds (a) a hand-rolled lazy promise cache
(`sheetsCache` + `getSheetsCached`, L215-222), (b) a `.forEach/.forEach` ->
`for..of/for..of` rewrite with an `oxlint-disable-next-line no-await-in-loop` and
an apologetic comment (L253-266), (c) a `.map(.map())` -> `Promise.all(map(async
=> Promise.all(map(async))))` double-nesting (L291-309), and (d) a second
`await splitCellAndSheet(arg, activeSheet)` at L297 that recomputes exactly what
L259 already computed for the same arg. All four disappear if the sheet list is
resolved once before the loop and `splitCellAndSheet` stays synchronous, e.g.
`const needsSheets = alephFormulas.some(a => a.some(x => x.isRef && x.value.includes('!')));`
`const sheets = needsSheets ? await getSheets() : [];` — laziness is preserved
(the whole point of the cache) and the two passes revert to plain forEach/map.
EVIDENCE:
  L215-222
    let sheetsCache: Sheet[] | undefined;
    // Make getting sheets lazy, so it doesn't fire if not necessary
    const getSheetsCached = async () => {
      if (!sheetsCache) { sheetsCache = await getSheets(); }
      return sheetsCache;
    };
  L256-259
      if (arg.isRef) {
        // No need to Promise.all since the only async call is cached
        // oxlint-disable-next-line no-await-in-loop
        const { cell, sheet } = await splitCellAndSheet(arg, activeSheet);
  L296-297
          if (arg.isRef) {
            const { cell, sheet } = await splitCellAndSheet(arg, activeSheet);
  data-access/index.ts:59  export const getSheets = (): Promise<Sheet[]> => {

--------------------------------------------------------------------------------
C2. packages/addin-core/src/operations/buildExplores/utils.ts:907
SUMMARY: The newUtils merge put two pairs of near-identical functions in the same
file — `getRowDimensions`/`getRowDimensionsNew` and
`getColumnDimensions`/`getColumnDimensionsNew` — ~50 lines of copy-paste whose
only real difference is which dimension-lookup method is used.
COST / SIMPLER FORM: L355-377 vs L907-933 differ only in
`table.getDimensionByValue(d)!` vs `table.getDimensionByAnyMatch(d)` plus how the
undefined case is handled (`!` at L357 vs an `if (filter)` at L912 plus a
surviving `@ts-expect-error` at L930). L382-392 vs L891-905 compute the same
"dimension has >1 value" predicate two ways
(`Object.keys(map[value]).length > 1` vs `values.length > 1`) and differ only in
whether undefined lookups are filtered out (non-New returns
`(Dimension | undefined)[]`, i.e. it can hand `undefined` entries to
`NoCodeExplore`). Both variants have live callers (index.js:277,601,605 vs
:847,861,903,919) so neither can be deleted; the simpler form is one function
each, taking the lookup as a parameter:
`getRowDimensions(table, keys, lookup: (k: string) => Dimension | undefined)`.
Doing so also lets both use `filters.some(d => d?.value === value)`, deleting the
`@ts-expect-error` at L930 AND the `!` at L357 (CLAUDE.md forbids both).
EVIDENCE:
  L357   const filters = Object.keys(dimensionFilterMap).map((dimension) => table.getDimensionByValue(dimension)!);
  L909   const filters = Object.keys(dimensionFilterMap).map((dimension) => table.getDimensionByAnyMatch(dimension));
  L928-932
    return table.dimensions.filter(
      ({ text, value }) =>
        // @ts-expect-error Table is yet not fully typed...
        !filters.find((dimension) => dimension.value === value) && !parentDimensionTexts.has(text),
    );
  L383   const columnDimensions: (Dimension | undefined)[] = [];
  L892   const columnDimensions: Dimension[] = [];

--------------------------------------------------------------------------------
C3. packages/addin-core/src/operations/buildExplores/utils.ts:170
SUMMARY: The same three-line `if (!alephFormulaArgs) throw new Error('Expected
aleph formula to have args')` is pasted into two functions, one copy is provably
unreachable, and a third variant of the same workaround was added in
apps/server/src/operations/alephFunction.ts:102 — all because
`ParsedAlephFormula` has no discriminant.
COST / SIMPLER FORM: `ParsedAlephFormula`'s two members both declare
`nFormulas: number` (L57, L63), so `nFormulas === 1` cannot narrow. At L170 the
function has already returned for `nFormulas === 0` (L153) and thrown for
`nFormulas > 1` (L156), so `alephFormulaArgs` is guaranteed present — that throw
can never fire. Same three lines again at L134-136. And alephFunction.ts:102 had
to become `if (!parsed || !parsed.alephFormula)` for the same reason, widening a
branch whose comment (L103-109) still documents only the `!parsed` case. One
root fix removes all three: have `splitAlephFormula` return
`{ nFormulas: number; parsed?: ParsedAlephFormulaWithArgs }` so a single
`if (!parsed)` narrows all four fields at once and `getSingleAlephFormula`'s
return type stops leaking `string | undefined`.
EVIDENCE:
  L133-136 (hasArrayParameters)
    const { alephFormulaArgs } = splitAlephFormula(alephFormula);
    if (!alephFormulaArgs) { throw new Error('Expected aleph formula to have args'); }
  L170-172 (getSingleAlephFormula, after the nFormulas 0/>1 guards)
    if (!alephFormulaArgs) { throw new Error('Expected aleph formula to have args'); }
  apps/server/src/operations/alephFunction.ts:102
    if (!parsed || !parsed.alephFormula) {

--------------------------------------------------------------------------------
C4. packages/frontend-common-components/src/components/ActionButtons/DrillDownButton.tsx:13
SUMMARY: Retyping the shared `getDrillDownByOptions` prop from a plain data shape
to the addin-core `Dimension` CLASS cost two brand-new `@ts-expect-error`
suppressions at the two call sites, for a modal that only reads two string
fields.
COST / SIMPLER FORM: DrilldownByOptionsModal.tsx L37-41 uses only
`elem.prettyName` and `elem.name`. Typing the prop as
`{ name: string; prettyName: string }[]` (or keeping the structural
`ServerTable['dimensions']`) satisfies both consumers with zero suppressions;
instead the diff adds `// @ts-expect-error TYPETODO: useDrilldown needs to be
typed` (apps/addin-components/.../ActionButtonsWrapper.tsx:125) and
`// @ts-expect-error TYPETODO: getDrillDownByOptions is not typed yet`
(apps/web-ui/.../Workbook.tsx:1011). `@ts-expect-error` suppresses every error on
its line, so any future mistake in those two JSX props is now invisible — and
Workbook.tsx's `handleGetDrillDownOptions` (L803-814) is ordinary TS that could
have been fixed properly. CLAUDE.md: "Narrow with type guards, not
`@ts-expect-error`, casts, or non-null assertions."
EVIDENCE:
  DrillDownButton.tsx:13-14   dimensions: Dimension[];   (was ServerTable['dimensions'])
  DrilldownByOptionsModal.tsx:37-41  drillDownDimensions.map((elem) => ({ label: elem.prettyName, value: elem.name, ... }))
  ActionButtonsWrapper.tsx:125  // @ts-expect-error TYPETODO: useDrilldown needs to be typed
  Workbook.tsx:1011             // @ts-expect-error TYPETODO: getDrillDownByOptions is not typed yet

--------------------------------------------------------------------------------
C5. apps/gs-addin/src/client/utils/formulaUtils.ts:214
SUMMARY: Mechanical brace insertion stranded two explanatory comments between the
`if` condition and its opening brace.
COST / SIMPLER FORM: The comment now reads as a trailing comment on the condition
and the `{` sits alone on the next line — a shape no formatter produces and no
one writes by hand, in the two most important early-returns of the function. It
happens twice (L214-217, L219-222). Clean form:
    // Non-ranges are always a single cell
    if (!m) {
      return { nRows: 1, nColumns: 1 };
    }
EVIDENCE:
  L213-222
    const m = arg.replaceAll('$', '').match(RANGE_REGEX);
    if (!m) // Non-ranges are always a single cell
    {
      return { nRows: 1, nColumns: 1 };
    }
    const [_group0, startColumn, startRow, lastColumn, lastRow] = m;
    if (!lastColumn) // Single cell reference
    {
      return { nRows: 1, nColumns: 1 };
    }

--------------------------------------------------------------------------------
C6. packages/addin-core/src/operations/buildExplores/utils.ts:5
SUMMARY: Three pairs of duplicate import statements from the same module, left
over from pasting newUtils.ts's imports in without merging them.
COST / SIMPLER FORM: L4+L5 (`@getaleph/global-common-utils`), L30-35+L36
(`../../transformers`), L38+L39 (`../connects`). Each is one oxlint
`import/no-duplicates` warning, absorbed silently by
`--max-warnings=520` in packages/addin-core/package.json:26, so CI will not flag
them. Simpler: merge each pair into one statement (net -3 lines).
EVIDENCE:
  L4  import { appendAndFilterToFilterGroup } from '@getaleph/global-common-utils';
  L5  import { CellValue, Connect, Sheet, SheetWithoutActive } from '@getaleph/global-common-utils';
  L30-35 import { AddinCoreExploreFilter, ... } from '../../transformers';
  L36 import { SpreadsheetInput } from '../../transformers';
  L38 import { parseConnectMeasure } from '../connects';
  L39 import { getConnectWithModifiedDimensionRanges } from '../connects';

--------------------------------------------------------------------------------
C7. packages/addin-core/src/operations/buildExplores/utils.ts:151
SUMMARY: The `let` destructure and its `eslint-disable prefer-const` comment are
stale — the diff removed the reassignment the comment describes.
COST / SIMPLER FORM: The old code did `alephFormulaArgs = alephFormulaArgs.map(
cleanPiece)`; the new code assigns to a fresh `const alephFormulaArgPieces`
(L174). Nothing in L150-181 reassigns any of the four destructured bindings, so
the comment's rationale ("alephFormulaArgs is modified") is now false and the
disable is only masking a warning the code no longer earns. Simpler: `const { ...
} = splitAlephFormula(formula);` and delete the comment line.
EVIDENCE:
  L151-152
    // eslint-disable-next-line prefer-const -- alephFormulaArgs is modified
    let { nFormulas, alephFormulaArgs, input, alephFormula } = splitAlephFormula(formula);
  L174
    const alephFormulaArgPieces = alephFormulaArgs.map((arg) => cleanPiece(arg));

--------------------------------------------------------------------------------
C8. packages/addin-core/src/operations/buildExplores/utils.ts:372
SUMMARY: `const parentDimensionTexts = new Set(parentDimensionTextsSet);` copies
a Set that is never touched again — twice, once in each of the duplicated row
functions.
COST / SIMPLER FORM: An extra allocation, an extra name, and a reader wondering
what invariant the copy protects (answer: none — `parentDimensionTextsSet` is
dead after the copy in both functions). Simpler: delete both lines and name the
accumulator `parentDimensionTexts` from the start.
EVIDENCE:
  L356/L372 (getRowDimensions)
    const parentDimensionTextsSet = new Set();
    ...
    const parentDimensionTexts = new Set(parentDimensionTextsSet);
  L908/L927 (getRowDimensionsNew)
    const parentDimensionTextsSet = new Set<string>();
    ...
    const parentDimensionTexts = new Set(parentDimensionTextsSet);

--------------------------------------------------------------------------------
RUNNERS-UP (confirmed, not in the returned top 8)
--------------------------------------------------------------------------------
R1. packages/addin-core/tests/operations/buildExplores/newUtils.test.ts:29 —
    File name and top-level `describe('newUtils')` both name a module that no
    longer exists (its import was repointed to `./utils` at L6), and
    `utils.test.js` in the same directory already tests that module. Simpler:
    fold the two `describe`s into utils.test.js, or at minimum rename the file
    and delete the now-meaningless outer `describe` wrapper (one less nesting
    level).

R2. apps/gs-addin/src/client/utils/formulaUtils.ts:25-26 —
    `const s = formula.toString();` on a parameter already typed `string`, and
    `Boolean(ALEPH_FORMULA_REGEX.test(s))` wrapping something that is already a
    boolean. Simpler: `return ALEPH_FORMULA_REGEX.test(formula);`. (Caveat: the
    regex carries /g, so `.test()` is stateful across calls — a separate
    correctness problem; the truly correct simplification drops /g or keeps
    `.match()`.)

R3. packages/workbook-utils/src/connect.ts:1 —
    Now a 7-line pure re-export whose only purpose is to keep
    `export * from './connect'` in index.tsx alive for two importers
    (frontend-common-components InThisFile/types.ts:2 and
    CopyConnectedTableModal.tsx:13, both of which already depend on
    @getaleph/global-common-utils). Simpler: delete the file + the barrel line
    and repoint those two imports. (Needs a check that `Sheet` — also imported
    from workbook-utils in those files — still resolves.)

R4. packages/addin-core/tests/operations/buildExplores/findConnectedTablesInRange.test.ts:37 —
    `makeFilteredConnect` does `makeConnect({...}) as unknown as Connect`, but
    `makeConnect` is already annotated `: Connect` (L19). A no-op double cast.
    Also L19+L29 carry BOTH the `: Connect` annotation and
    `as unknown as Connect`; one of the two is redundant.

R5. packages/addin-core/src/operations/buildExplores/utils.ts:52-54, 84, 130 —
    Three `.toString()` calls on values now annotated `string`. Either the
    annotation is a lie (the doc comment at L79-83 says Excel supplies numbers,
    in which case the type should be `string | number`) or all three calls are
    dead. Both readings can't be right; pick one.

R6. packages/global-common-utils/src/types.ts:374 —
    `export type SheetWithoutActive = Omit<Sheet, 'isActive'> & { isActive?: boolean };`
    The name says "without" but the type permits `isActive`. Either
    `Omit<Sheet, 'isActive'>` (simpler, and sufficient for every non-literal
    call site) or a name that matches ("SheetWithOptionalActive").
