================================================================================
1. PROMPT RECEIVED (verbatim)
================================================================================

You are finder Angle B ("removed-behavior auditor") — one of 10 parallel finder agents in a maximum-effort code review.

REVIEW SCOPE: the diff of git branch `colton/typ-797-convert-packagesaddin-coresrcoperationsbuildexploresutilsjs` vs `trunk`, in repo /Users/colton/code/monorepo.
- Full unified diff is saved at: /Users/colton/code/monorepo/code-review-notes/00-review.diff  (READ THIS FIRST, in full)
- Regenerate with: `git diff trunk...HEAD` run from /Users/colton/code/monorepo
- What the PR does: converts packages/addin-core/src/operations/buildExplores/utils.js to TypeScript, merges the deleted newUtils.ts into utils.ts, moves the `Connect` type from packages/workbook-utils/src/connect.ts to packages/global-common-utils/src/connectUtils.ts, adds `SheetWithoutActive`, and touches apps/gs-addin formulaUtils.ts, apps/server alephFunction.ts, addin-components, frontend-common-components, and two integration fixtures.

YOUR ANGLE: For EVERY line the diff DELETES or REPLACES, name the invariant or behavior it enforced, then search the NEW code for where that invariant is re-established. If you cannot find it, that is a candidate: a removed guard, a dropped error path, a narrowed or widened validation, a deleted test that covered a real case, a lost JSDoc contract that callers relied on.

Use `git show trunk:<path>` to read the original versions. Key originals:
- `git show trunk:packages/addin-core/src/operations/buildExplores/utils.js`
- `git show trunk:packages/addin-core/src/operations/buildExplores/newUtils.ts`
- `git show trunk:packages/workbook-utils/src/connect.ts`
- `git show trunk:apps/gs-addin/src/client/utils/formulaUtils.ts`
- `git show trunk:packages/addin-core/src/transformers/explores.ts`
- `git show trunk:packages/global-common-utils/src/types.ts`

Specific things to audit:
1. `newUtils.ts` was deleted and its four exports (`getFilterGroupNew`, `getColumnDimensionsNew`, `getRowDimensionsNew`, `processConnectMeasures`) were pasted into utils.ts. Diff the moved bodies character-by-character against the originals — was anything dropped, reordered, or subtly altered? (e.g. `new Set([])` -> `new Set<string>()`, removed `@ts-expect-error` lines, changed imports).
2. `processConnectMeasures` was imported by utils.ts from './newUtils' — verify all other importers of './newUtils' in the repo were updated (grep for `newUtils`).
3. `ConnectSettings` / `ConnectRowSettings` in the NEW connectUtils.ts gained `specialConnectFilter`. Was anything from the old workbook-utils definitions dropped? Check `sheet: { id: string; name: string }` -> `sheet: SheetWithoutActive` and whether `SheetWithoutActive` requires fields the old type did not (read the full `Sheet` type).
4. `packages/workbook-utils/src/connect.ts` used `ServerTable` from `./useWorkbookTables`; the new global-common-utils `Connect` uses global-common-utils' own `ServerTable`. Compare the two `ServerTable` definitions — is the re-exported `Connect` now structurally different for workbook-utils consumers?
5. `sheetRangesValues` type in transformers/explores.ts was widened to allow `undefined` inner values — find consumers that index into it and would now be `possibly undefined` or, worse, silently accept undefined at runtime.
6. In `getTablesAndCleanCellReferences`, the old code did `if (arg.isRef && serviceResult[sheet.id] && serviceResult[sheet.id][cell])`; the new code does `if (serviceResult[sheet.id]?.[cell])`. Any behavioural difference?
7. `getSingleAlephFormula` used to reassign `alephFormulaArgs` in place and return it; now it builds `alephFormulaArgPieces`. Verify the returned shape and the `// eslint-disable-next-line prefer-const` comment are still accurate.
8. Old JSDoc `@returns` blocks were deleted in favour of TS types — check every deleted `@returns` against the new TS signature for a mismatch (e.g. `cellReferences: string[]` became `CellValue[]`, `activeSheet` typed as `Sheet` vs `SheetWithoutActive`).
9. Tests: `packages/addin-core/tests/operations/buildExplores/newUtils.test.ts` and `findConnectedTablesInRange.test.ts` changed — did any assertion or case get weakened/removed? Did the test file name become stale?

MANDATORY NOTE-TAKING (explicit user request): You MUST write a detailed plain-text notes file at
/Users/colton/code/monorepo/code-review-notes/02-angle-B-removed-behavior.txt
using the Write tool, BEFORE returning your final answer. It MUST contain, in this order:
1. "PROMPT RECEIVED" — the verbatim full text of this prompt.
2. "WHAT I AM LOOKING FOR"
3. "GOALS"
4. "METHOD / HOW I ACCOMPLISHED IT" — chronological log: every command run, every file read (absolute path + line ranges), what you saw, what you concluded. Include dead ends and refuted hypotheses.
5. "CANDIDATES" — file, line, summary, failure scenario, quoted code evidence.

OUTPUT: return a JSON array of up to 8 candidate findings, each {"file","line","summary","failure_scenario"}. RECALL mode — err on the side of surfacing. If you truly find nothing, return [].


================================================================================
2. WHAT I AM LOOKING FOR
================================================================================

Behaviour that existed on trunk and is NOT re-established on the branch. Concretely:
  (a) removed runtime guards / early returns / defensive `&&` chains
  (b) dropped or replaced error paths (thrown type changed, message lost, catch removed)
  (c) validation narrowed (input rejected that used to be accepted) or widened
      (input accepted that used to be rejected) -- both are defects
  (d) deleted or weakened test assertions, stale test names
  (e) lost JSDoc contracts that callers relied on, and NEW TS annotations that
      contradict the JSDoc / the function body / the actual runtime values --
      a TS type that lies is a removed contract even though no line of runtime
      code changed
  (f) newly-added suppressions (`@ts-expect-error`, `!`, `as X`, eslint-disable)
      that hide a real defect rather than fix it
  (g) regex / string-method swaps that look equivalent but are not
      (`.match(/g)` vs `.test()`, `replace(/\$/g)` vs `replaceAll`)

================================================================================
3. GOALS
================================================================================

G1. Read the entire 1536-line diff before forming any hypothesis.
G2. For every deleted line, name the invariant, then prove or disprove that the
    new code re-establishes it.
G3. Byte-diff the four function bodies moved out of newUtils.ts into utils.ts.
G4. Confirm no dangling importers of './newUtils'.
G5. Compare old workbook-utils Connect/ConnectSettings/ConnectRowSettings against
    the new global-common-utils versions field by field.
G6. Read the full `Sheet` type and decide whether `SheetWithoutActive` tightens
    or loosens anything.
G7. Trace every function whose signature was newly annotated back to its real
    runtime call sites (mostly in the still-untyped index.js) and check whether
    the annotation is true.
G8. Empirically verify the regex behaviour change in gs-addin formulaUtils.ts.
G9. Compile the affected packages (`types:check`) to see what the compiler
    agrees / disagrees with, and (reversibly) remove the two new
    `@ts-expect-error` comments to see exactly what error they suppress.
G10. Write these notes, then emit <=8 JSON candidates.

================================================================================
4. METHOD / HOW I ACCOMPLISHED IT (chronological)
================================================================================

STEP 1 -- Read the diff.
  Read /Users/colton/code/monorepo/code-review-notes/00-review.diff lines 1-1250
  (first page, truncated at the 25k-token cap), then lines 1251-1536.
  Simultaneously ran:
    git log --oneline trunk..HEAD
      -> 8 commits: d48c770a02 start .. c9ec31c19c guh
    git branch -a --list '*typ-79*'
      -> confirms this branch is stacked on colton/typ-796-...index.js
  Observed the 20 changed files. Noted immediately-suspicious lines:
    - diff line 39: gs-addin isAlephFormula `!!s.match(RE)` -> `Boolean(RE.test(s))`
    - diff line 120: server `if (!parsed)` -> `if (!parsed || !parsed.alephFormula)`
    - diff line 674-680: `getSheets().find(...)` -> `await getSheetsCached()` + new throw
    - diff line 1221: sheetRangesValues widened with `| undefined`
    - diff line 1253-1263 / 1272: tests gained `as unknown as Connect`
    - diff line 1374-1384: NEW specialConnectFilter shape in connectUtils.ts

STEP 2 -- Dump the trunk originals.
    git show trunk:packages/addin-core/src/operations/buildExplores/utils.js
      -> scratchpad/utils.trunk.js (774 lines)
    git show trunk:packages/addin-core/src/operations/buildExplores/newUtils.ts
      -> scratchpad/newUtils.trunk.ts (155 lines)
    git show trunk:apps/gs-addin/src/client/utils/formulaUtils.ts
      -> scratchpad/formulaUtils.trunk.ts
  grep -rn "newUtils" over all ts/tsx/js/jsx excluding node_modules:
    ONLY hit = packages/addin-core/tests/operations/buildExplores/newUtils.test.ts:29
               `describe('newUtils', () => {`
  CONCLUSION (audit item 2): all importers of './newUtils' WERE updated. The only
  residue is the stale test FILE NAME and the stale `describe('newUtils')` label.
  -> item 2 REFUTED as a bug, downgraded to hygiene.

STEP 3 -- Byte-diff the moved bodies (audit item 1).
    awk 'NR>=855' packages/addin-core/src/operations/buildExplores/utils.ts > moved-new.ts
    awk 'NR>=14'  scratchpad/newUtils.trunk.ts                             > moved-old.ts
    diff -u moved-old.ts moved-new.ts
  Result: the ONLY differences in all four moved functions
  (getFilterGroupNew, getColumnDimensionsNew, getRowDimensionsNew,
   processConnectMeasures) are inside getRowDimensionsNew:
      - const parentDimensionTextsSet = new Set([]);
      + const parentDimensionTextsSet = new Set<string>();
      and four deleted `// @ts-expect-error Table is yet not fully typed...` lines
      (now unnecessary because Table gained `dimensionGroups?: DimensionGroup[]`).
  `new Set([])` and `new Set<string>()` are both empty sets -> no behaviour change.
  The trailing `@ts-expect-error` on
      `!filters.find((dimension) => dimension.value === value)`
  was KEPT (utils.ts:949) -- correct, `filters` can still contain undefined.
  CONCLUSION (audit item 1): the move is faithful. NOT a finding.

STEP 4 -- Read trunk utils.js lines 30-270 (scratchpad/utils.trunk.js) in full.
  Key trunk facts recorded:
    - isAlephFormula used `Boolean(s.match(ALEPH_FORMULA_REGEX))` (kept in the new
      addin-core utils.ts:54 -- good; the file even carries
      `// oxlint-disable unicorn/prefer-regexp-test` on line 1).
    - splitCellAndSheet was SYNCHRONOUS and did
        internalSheet = getSheets().find((s) => s.name === sheetName);
    - the second pass did
        if (arg.isRef && serviceResult[sheet.id] && serviceResult[sheet.id][cell])
    - getSingleAlephFormula did `alephFormulaArgs = alephFormulaArgs.map(cleanPiece)`
      hence the `// eslint-disable-next-line prefer-const -- alephFormulaArgs is modified`.

  Ran in parallel:
    grep -n "export const getSheets" packages/addin-core/src/data-access/index.ts
      -> line 59: `export const getSheets = (): Promise<Sheet[]> => {`
  !! getSheets() is ASYNC on trunk too. So trunk's `getSheets().find(...)` was
  `Promise.find` -> TypeError at runtime. i.e. on trunk EVERY cross-sheet
  reference (`Sheet4!A1`) inside an Aleph formula threw a TypeError out of
  splitCellAndSheet.
  This explains the two integration-fixture additions
  (`"Sheet4": { "A1": [["Sage Intacct (Native) | Profit and Loss"]] }`): the new
  code actually resolves the reference and therefore records the range.
  CONCLUSION: the async/`getSheetsCached` rewrite is a genuine BUG FIX, not a
  regression. The new `throw new AlephError('Could not find sheet ...')` replaces
  a TypeError with a typed error on the same path.
  -> HYPOTHESIS "new AlephError is an unguarded new error path" -- checked next.

STEP 5 -- Trace who catches the new AlephError.
    grep -rn "getTablesAndCleanCellReferences|parseAndCleanAlephFormula"
      -> callers: index.js:185, index.js:964, index.js:1800
    Read /Users/colton/code/monorepo/packages/addin-core/src/operations/buildExplores/index.js
      lines 160-240, 930-1010, 1770-1845.
    index.js:961-978: parseAndCleanAlephFormula IS wrapped in
        try { ... } catch (error) { console.log('Something failed ...'); }
        ... cellsToRefresh.push(cellA1);
      i.e. exactly the "fall back to refreshing the formula and reading the cache"
      that the new code comment claims. The comment is ACCURATE.
    index.js:185 and index.js:1800 call getTablesAndCleanCellReferences directly
      with NO try/catch, but they pass ALREADY-CLEANED args (every arg has
      isRef:false after parseAndCleanAlephFormula), so splitCellAndSheet is never
      reached there.
  CONCLUSION: audit item 6 + the new throw are FINE. REFUTED.
  Also checked the recording mutation: trunk passed a possibly-undefined
  `sheetRangesValues` as the reduce seed (would have thrown); new code uses
  `?? {}` and `(sheets ?? [])`. Strictly more robust. REFUTED as a regression.

STEP 6 -- Empirically test the gs-addin regex swap (audit: not in my list, found
          by reading the diff).
    node -e "... old = !!s.match(RE); new = Boolean(RE.test(s)) ..."
    OUTPUT:
      old repeated: true true true true
      new repeated: true false true false
      lastIndex after new: 0
  CONFIRMED REGRESSION. `RegExp.prototype.test` on a `/g` regex advances
  `lastIndex`; `String.prototype.match` with `/g` does not. The regex is a
  module-level singleton.
  Read /Users/colton/code/monorepo/apps/gs-addin/src/client/utils/formulaUtils.ts
    lines 1-70   -> line 22 regex has the `/g` flag; line 24-27 the new isAlephFormula
    lines 82-111 -> buildAlephFormulasMap loops `for (const [cellA1, cellFormula] of
                    formulasMap.entries())` and calls isAlephFormula per cell (line 93)
    lines 890-930 -> findArrayAlephFormulaInternal calls isAlephFormula per cell
                     inside nested row/column loops (line 908)
    lines 356-372, 415-440, 775-800 -> AlephFormulaFinder.findInRanges ->
                     findArrayAlephFormulaInternal, and the exported
                     findAlephFormulasInRanges wraps AlephFormulaFinder.
  Reachability check:
    grep -rn "formulaUtils" apps/gs-addin/src (excluding the file itself) -> NOTHING
    grep -rn "findScalarAlephFormulas|AlephFormulaFinder|findAlephFormulasInRanges|
              parseAlephFormulaArraySize" repo-wide
      -> only apps/gs-addin/test/client/utils/formulaUtils.test.ts imports
         AlephFormulaFinder. The addin-core hits are separate same-named local fns.
  So the module is currently staged/not-yet-wired code, but the two loops are the
  module's own hot paths and the defect is unconditional. Reporting it as the #1
  candidate with the reachability caveat.
  Also verified `arg.replace(/\$/g,'')` -> `arg.replaceAll('$','')` (diff line 50):
  equivalent. REFUTED.
  Also `compareRowExtentsLessThan` and `coerceArg` brace-only reformatting:
  behaviour identical. REFUTED.

STEP 7 -- apps/server/src/operations/alephFunction.ts `!parsed || !parsed.alephFormula`.
    Read /Users/colton/code/monorepo/apps/server/src/operations/alephFunction.ts
      lines 60-140.
  getSingleAlephFormula returns undefined only when nFormulas===0, throws when
  nFormulas>1, and otherwise sets alephFormula = arr[0][0] (a non-empty match).
  So `!parsed.alephFormula` is unreachable at runtime; it exists only because the
  new `ParsedAlephFormula` union makes `alephFormula` `string | undefined`.
  Behaviour unchanged. The comment right below ("`parsed` is undefined when: ...")
  is now marginally stale. Low value -> not reported as a top candidate.
  CONCLUSION: REFUTED as a behaviour change; noted as type-precision loss in
  getSingleAlephFormula's return type (it should be a discriminated result so
  downstream doesn't need the dead check).

STEP 8 -- Audit item 3 / 4: the Connect type move.
    grep -n "export type Sheet = " -A 20 -B 8 packages/global-common-utils/src/types.ts
      -> Sheet = { name: string; id: string; isActive: boolean }
         SheetWithoutActive = Omit<Sheet,'isActive'> & { isActive?: boolean }
      i.e. { name, id, isActive? }. Old Connect had sheet: { id: string; name: string }.
      -> purely ADDITIVE/optional. Nothing required that wasn't required before.
      AUDIT ITEM 3 (sheet part): REFUTED.
    grep -rn "ServerTable" packages/workbook-utils/src/useWorkbookTables.ts
      -> line 2 imports ServerTable from @getaleph/global-common-utils, line 10
         `export type { ServerTable, Dimension, DimensionGroup, ... }` -- it is a
         RE-EXPORT, not a second definition.
      AUDIT ITEM 4: REFUTED -- the two ServerTable's are the same type.
    Field-by-field on ConnectSettings / ConnectRowSettings / ConnectDimension /
    Connect (diff lines 1366-1426 vs 1473-1528): nothing dropped; only
    `specialConnectFilter?: { isOn?: boolean }` added to two of them.
    BUT: grep'd for a pre-existing definition of that concept:
      packages/addin-core/src/entities/ConnectedTable.ts:20
        export type SpecialConnectFilter = { id?: string; range: string; isOn: boolean };
      used by ConnectRowSettings (line 30) and ConnectColumnSettings (line 40)
      in the SAME package that consumes the new global Connect.
    -> the new global copy DROPS `id` and `range` and makes `isOn` optional.
    That is a lossy 3rd copy -> CANDIDATE. And it is exactly why
    findConnectedTablesInRange.test.ts had to add `as unknown as Connect` to
    makeFilteredConnect (its fixture sets `range: 'E2:E10'`, absent from the new type).

STEP 9 -- Audit item 5: sheetRangesValues widening.
    grep -rn "sheetRangesValues" repo-wide (excluding node_modules/json)
      -> TS consumers: packages/addin-core/integration-tests/utils/spreadsheet.ts:72-91
      -> JS consumers: src/operations/explores.js:527/972-991,
                       src/operations/buildExplores/index.js:1212/1399/1431/1471/1505
      -> apps/addin-components/src/pages/Connect/index.tsx:91 declares its own
         `Record<string, Record<string, unknown>>` (unaffected).
    Read packages/addin-core/integration-tests/utils/spreadsheet.ts lines 55-115.
      line 84: `Object.entries(rangeValuesMap ?? {}).map(([range, values]) => ({range, values}))`
      line 89-90: `// @ts-expect-error SpreadsheetInput values typed as unknown[][] ...`
                  `promises.push(spreadsheetService.setRangesValues(...))`
      -> `values` is now `unknown[][] | undefined` and flows into a call whose type
         error is already suppressed, so an undefined grid would reach
         setRangesValues silently.
    WHY was it widened? grep -n "getRangesValues" -A 12 packages/global-common-utils/src/types.ts
      -> line 630-633: `getRangesValues: (...) => Promise<Record<string, Record<string, CellValue[][]>>>`
      The service NEVER returns undefined inner values. So the widening is NOT
      required by the data source; it is required by utils.ts:281-286
        `(acc, [id, values]) => ({ ...acc, [name]: { ...acc[name], ...values } })`
      where `acc[name]` is `... | undefined`, so TS infers `| undefined` into the
      inner value type. Root fix is `...(acc[idToSheetMap[id].name] ?? {})`.
    -> CANDIDATE (widened contract to satisfy the compiler; CLAUDE.md "fix the
       root type" violation).

STEP 10 -- Audit item 7: getSingleAlephFormula shape + prefer-const comment.
    Read packages/addin-core/src/operations/buildExplores/utils.ts lines 1-360.
      line 151-152:
        // eslint-disable-next-line prefer-const -- alephFormulaArgs is modified
        let { nFormulas, alephFormulaArgs, input, alephFormula } = splitAlephFormula(formula);
      line 174: `const alephFormulaArgPieces = alephFormulaArgs.map(...)`
      line 176-180: returns { alephFormula, alephFormulaArgs: alephFormulaArgPieces, input }
    RETURNED SHAPE: identical keys/values to trunk -> behaviour preserved.
    But NOTHING is reassigned any more, so `let` should be `const` and the
    eslint-disable justification ("alephFormulaArgs is modified") is now FALSE.
    grep for reportUnusedDisableDirectives/noInlineConfig -> not configured, so it
    won't fail lint; it is a misleading stale suppression. -> minor candidate.

STEP 11 -- Audit item 8: deleted @returns vs new TS signatures.
    splitAlephFormula: old @returns said `alephFormula: int` (wrong) -> new
      ParsedAlephFormula says `alephFormula: string` (right). IMPROVEMENT.
    getTablesAndCleanCellReferences:
      old @param claimed `Array<{alephFormula, alephFormulaArgs, input}>`; the real
      callers pass `alephFormulas.map((e) => e.alephFormulaArgs)` (index.js:185,
      1800) -> the old JSDoc was WRONG, the new `AlephFormulaArgPiece[][]` is right.
      IMPROVEMENT.
      old @returns `cellReferences: string[]` -> new `cellReferences: CellValue[]`
      (utils.ts:191-194). Runtime always could be a number (values read from cells),
      so the new type is more honest.
      HOWEVER utils.ts:468-471 declares
        type FilledTableAndCellReference = { table: Table; cellReferences: string[] }
      and it is the parameter type of dimensionsMatch (589) and
      getFilterGroupAndVaryingDimensionsForScalarFormulas (478) -- the direct
      consumers of getTablesAndCleanCellReferences' output (index.js:190-225,
      1804-1820). So the SAME file says the producer yields CellValue[] and the
      consumer takes string[]. Only index.js being untyped hides it. Noted as a
      secondary candidate (will bite the sibling TYP-796 index.js conversion;
      index.js:983 already does `value?.toLowerCase()` on these values).
    activeSheet: `Sheet` for getTablesAndCleanCellReferences/parseAndCleanAlephFormula/
      findExploreInRange, `SheetWithoutActive` for connectAreaContainsRange/
      findConnectedTablesInRange. Consistent with the old JSDoc
      `{ id, name, isActive }`. REFUTED.

STEP 12 -- Audit item 9: tests.
    git diff trunk...HEAD -- <the two test files>
    findConnectedTablesInRange.test.ts:
      - ACTIVE_SHEET gained `id: 'sheet1'` (STRENGTHENED, not weakened)
      - makeConnect / makeFilteredConnect gained `as unknown as Connect`
        (WEAKENED type checking of the fixtures)
      - no `it(...)` removed, no expect removed. 12 call sites all intact.
    newUtils.test.ts: only the import path changed. File name + describe label now
    stale. No assertion lost.
    -> audit item 9: no assertion weakened; hygiene + double-cast only.

STEP 13 -- Verify the branch compiles, and unmask the two new @ts-expect-errors.
    pnpm turbo run types:check --filter=@getaleph/addin-core
      --filter=@getaleph/global-common-utils --filter=@getaleph/workbook-utils
      --filter=@getaleph/frontend-common-components --env-mode=loose
      -> exit 0. Branch compiles.
    Then (reversibly) deleted the two new `@ts-expect-error` lines and ran
      pnpm turbo run types:check --filter=@getaleph/addin-components --filter=@getaleph/web-ui
    OUTPUT (verbatim, trimmed):
      ActionButtonsWrapper.tsx(125,9): error TS2322: Type '() => Promise<{
        dimensions: Dimension$1[] | undefined; suggestedDimensions: never[];
        discoveryContext: { sourceType: string; range: Range$1; activeSheet: Sheet;
        rangeValues: unknown[][]; connectedTable: Connect | undefined; ... } ...'
        is not assignable to type '() => Promise<{ dimensions: Dimension$1[];
        suggestedDimensions: string[]; discoveryContext?: unknown; }>'.
    Then `git checkout --` both files. REVERTED (verified).
  TWO discoveries from that error text:
    (i) the suppressed error is a NULLABILITY error: `dimensions: Dimension[] | undefined`
        vs the required `Dimension[]`. DrilldownByOptionsModal.tsx:37 does
        `drillDownDimensions.map(...)` -> TypeError on undefined. The
        `@ts-expect-error` hides a real crash, it does not fix a naming mismatch.
    (ii) `connectedTable: Connect | undefined` -- proof that
        findConnectedTablesInRange's `return narrowedMatches.filter(Boolean)`
        (utils.ts:711) returns `(Connect | undefined)[]`, because TS's
        `filter(Boolean)` overload does not narrow. That `undefined` then leaks
        into discoveryContext across package boundaries.
    grep -> packages/global-common-utils/src/nullabilityUtils.ts:12 already exports
      `isNotNullish = <T>(value: T | null | undefined): value is T` -- the
      reusable guard that should have been used.

STEP 14 -- Is `Connect` even the right type for findConnectedTablesInRange?
    Read packages/addin-core/src/entities/ConnectedTable.ts lines 1-85 and 85-145.
      ConnectedTable (addin-core ENTITY) has: sheet: Sheet, table?: Table,
      rows/columns/filters: Record<string, ConnectParam>, rowSettings:
      ConnectRowSettings (with SpecialConnectFilter incl. `range`), measures?, ...
    Read packages/global-common-utils/src/types.ts lines 153-198 (ServerTable):
      `measures: string[]`, `dimensions: Dimension[]` (plain server objects), NO methods.
    Traced the runtime values:
      index.js:1337 JSDoc `getConnectedTables: () => ConnectedTable[]`
      index.js:1420 `const connectedTables = getConnectedTables();`
      connectsNew.ts:564 getConnectedTables -> constructConnectedTablesOld /
        constructRobustConnectedTables; connectsNew.ts:573
        `addOrUpdateConnectEntitiesOld = (connects: ConnectedTable[])` confirms
        the domain type.
      index.js:1421 findConnectedTablesInRange(connectedTables, ...)
      index.js:1439 getExploreForTableRange(tableInRange, ...)
      index.js:874-877 getExploreForTableRange -> getProcessedConnectedTableForDrillDown(connectTable, range)
      utils.ts:743 getProcessedConnectedTableForDrillDown(connectedTable: ConnectedTable, range)
    => THE SAME OBJECT is annotated `Connect` at utils.ts:673 and `ConnectedTable`
       at utils.ts:743, in one call chain. They are structurally incompatible.
    Further proof the `Connect` annotation is the wrong one:
      index.js:1738 `dimensions: getVisibleDimensions(table.dimensions)` where
      `table` = `tablesInRange[0].table`; utils.ts:398-400
      `getVisibleDimensions(dimensions: Dimension[])` calls `dim.clone()`.
      `ServerTable['dimensions']` elements are plain objects with no `.clone()`.
      Also processConnectMeasures (utils.ts:935+) calls
      `connectedTable.table.getMeasureByText(...)` -- not on ServerTable.
    -> STRONG CANDIDATE.

STEP 15 -- Remaining newly-annotated signatures vs their real callers.
    Read utils.ts lines 352-652 and 652-851.
    (a) utils.ts:408 getFilterGroup(dimensionFilterMap: Record<string, Record<string,
        FilterGroup['body']>>) -- CORRECT vs index.js:557-591 which builds
        `dimensionFilterMap[dim] = { [value]: [filter] }`.
    (b) utils.ts:382 getColumnDimensions(table, dimensionFilterMap: Record<string, unknown[]>)
        -- WRONG. Body does `Object.keys(dimensionFilterMap[value]).length > 1`
        (a record, not an array), and index.js:605 passes the EXACT SAME object it
        passed to getFilterGroup at index.js:593. -> CANDIDATE.
    (c) utils.ts:446 `[filterGroup] = filterValues as [FilterGroup];`
        Read packages/addin-core/src/entities/FilterGroup.ts:1-80 and Filter.ts:1-60.
          FilterGroupBody = (FilterGroup | Filter<FilterValue>)[]
          class Filter has `body?: never`
          FilterGroup.parse correctly declares `FilterGroup | Filter<FilterValue>`
        In the single-dimension / single-value case (dimensionValues.length === 1 ->
        `new FilterGroup({})` -> operator undefined -> isVariableDimension false ->
        the `else` branch) filterValues[0] is a `Filter` instance at runtime.
        The cast makes getFilterGroup's inferred return type `FilterGroup`, erasing
        the real `FilterGroup | Filter` contract. -> CANDIDATE.
    (d) utils.ts:453-457 pushAllValuesToFilterGroup: param renamed
        dimensionName -> dimension: Dimension. Callers (utils.ts:544/555/561) pass
        `dimensionNameToDimension[dimensionName]`, a real Dimension. Just a rename.
        REFUTED.
    (e) utils.ts:649-650 `(lastRowSubrange.cellStart.row ?? NaN) - 1`
        trunk `undefined - 1` === NaN, new `NaN - 1` === NaN. REFUTED.
    (f) utils.ts:424 `variableDimensionsCount += isVariableDimension ? 1 : 0`
        vs trunk `+= isVariableDimension` (bool coerces to 1/0). REFUTED.
    (g) utils.ts:777/801 processRows/processColumns lost their `async` keyword.
        Both still return Promise.all(...). Difference: a synchronous throw inside
        (e.g. connectedTable.rows undefined -> Object.values throws) now propagates
        synchronously out of processRows() so processColumns() is never invoked and
        processFilters()'s already-created promise is left dangling (possible
        unhandled rejection). The enclosing function is still `async`, so the caller
        still sees a rejection. Very low severity -> not reported.
        (Note processFilters was already non-async on trunk.)
    (h) New non-null assertions added: utils.ts:357 `getDimensionByValue(dimension)!`,
        :497 `getDimensionByValue(dimension, {ignoreCase:true})!`, :715 `e.sheet!.id`,
        :728-729 `previousRowsCount!`/`previousColumnsCount!`, :771/795/819
        `dimension!.value`. None change runtime behaviour (trunk would have thrown
        the same TypeError), but each hides a real crash path and violates
        CLAUDE.md "Narrow with type guards, not ... non-null assertions".
        Reported as a rolled-up secondary note, not a top-8 candidate.
    (i) utils.ts:129-137 hasArrayParameters gained
        `if (!alephFormulaArgs) throw new Error('Expected aleph formula to have args')`.
        Reachable: a cell containing TWO ALEPH calls passes isAlephFormula (a /g
        .match returns both) then splitAlephFormula returns {nFormulas: 2} with no
        args. Trunk threw `TypeError: Cannot read properties of undefined (reading
        'some')`. Both throw and both escape buildAlephFormulasMap, so no behaviour
        change -- but the replacement is a bare `Error` with a developer-only
        message where the file's own convention is AlephError/DrillDownError with a
        user-facing message. Low severity.
        The twin guard at utils.ts:170-172 (inside getSingleAlephFormula) is dead
        code -- the nFormulas 0/>1 checks above it make it unreachable.
    (j) utils.ts:4-5, 30-36, 38-39: three duplicated import sources
        (@getaleph/global-common-utils twice, ../../transformers twice, ../connects
        twice). Cosmetic.

STEP 16 -- Misc diff hunks checked and cleared.
    apps/addin-components/src/hooks/useDrillDown.jsx: `catch (e)` -> `catch (error)`;
      identical. REFUTED.
    packages/frontend-common-components DrillDownButton.tsx / DrilldownByOptionsModal.tsx:
      ServerTable['dimensions'] -> Dimension[] (addin-core). Checked
      packages/frontend-common-components/package.json -- @getaleph/addin-core is
      already a dependency and addin-core does NOT depend back on
      frontend-common-components, so no missing dep / no cycle. REFUTED.
      Read DrilldownByOptionsModal.tsx:19-57: only `.prettyName` and `.name` are
      read, present on both shapes -> the retype is semantically OK; the fallout is
      only the nullability suppression (STEP 13).
    packages/addin-core/src/entities/Table.ts: `dimensionGroups?: DimensionGroup[]`
      added -- this is what let the four @ts-expect-errors go away in
      getRowDimensionsNew. Verified DimensionGroup.dimensions is string[] and the
      new optional fields (isHyperlink, customCalculatedItems, createdAt, updatedAt)
      are additive; CustomCalculatedItem is defined at types.ts:8. REFUTED.
    packages/addin-core/src/data-access/index.ts: Sheet -> SheetWithoutActive on 5
      internal pair types + getRangeValues/getSelectionValues params. Grepped
      `\.isActive` across addin-core/workbook-utils/gs-addin/excel-addin: only used
      on sheet LISTS in trackActiveSheet, never inside getRangesValues. Widening is
      behaviourally safe. REFUTED.
    Integration fixtures: the two added Sheet4/A1 entries are the consequence of the
      getSheets fix (STEP 4), not an independent change. REFUTED.

================================================================================
5. CANDIDATES
================================================================================

--------------------------------------------------------------------------------
C1  apps/gs-addin/src/client/utils/formulaUtils.ts:26   [HIGH]
--------------------------------------------------------------------------------
SUMMARY: `isAlephFormula` was switched from `s.match(RE)` to `RE.test(s)` on a
module-level `/g` regex, making it stateful: consecutive calls with the same
string alternate true/false.

EVIDENCE:
  line 22:  const ALEPH_FORMULA_REGEX = /ALEPH(.*?)GET(INTEGRATION)?DATA\((.*?)\)/g;
  trunk:    return !!s.match(ALEPH_FORMULA_REGEX);
  branch:   return Boolean(ALEPH_FORMULA_REGEX.test(s));
  node repro:
    old repeated: true true true true
    new repeated: true false true false
  The sibling implementation in packages/addin-core/src/operations/buildExplores/
  utils.ts:54 deliberately KEPT `Boolean(s.match(ALEPH_FORMULA_REGEX))` and the
  file opens with `// oxlint-disable unicorn/prefer-regexp-test` (line 1) -- i.e.
  the hazard was already known in the other copy of this function.
  Callers that loop:
    line 93  (buildAlephFormulasMap, per cell in formulasMap.entries())
    line 908 (findArrayAlephFormulaInternal, per cell in nested row/col loops,
              reached from AlephFormulaFinder.findInRanges -> exported
              findAlephFormulasInRanges at line 780)

FAILURE SCENARIO: a selection/scan block containing 2+ Aleph formulas. The 1st
call leaves lastIndex at the end of formula 1; the 2nd call starts searching from
that offset, finds nothing, returns false and resets lastIndex. Every other Aleph
formula is classified "non-Aleph" and skipped ("Ignoring non-Aleph formula ...",
line 94), so the map/refresh set silently loses half the cells -- and the results
change depending on how many times the function has been called before.

CAVEAT (for calibration): grep shows apps/gs-addin/src has no non-test importer of
formulaUtils.ts today, so this is staged code. The defect is nonetheless
unconditional and will land the moment the module is wired up.

--------------------------------------------------------------------------------
C2  packages/addin-core/src/operations/buildExplores/utils.ts:673   [HIGH]
--------------------------------------------------------------------------------
SUMMARY: `findConnectedTablesInRange` / `connectAreaContainsRange` /
`hasActiveSpecialConnectFilter` were annotated with the raw server payload type
`Connect`, but the runtime values are addin-core `ConnectedTable` ENTITIES -- and
the very next function in the same call chain annotates the same objects as
`ConnectedTable`.

EVIDENCE:
  utils.ts:672-676  export async function findConnectedTablesInRange(
                      connectedTables: Connect[], activeSheet: SheetWithoutActive, range: Range)
  utils.ts:743      export async function getProcessedConnectedTableForDrillDown(
                      connectedTable: ConnectedTable, range: Range)
  call chain: index.js:1420 getConnectedTables()   [JSDoc index.js:1337:
                 `getConnectedTables: () => ConnectedTable[]`]
           -> index.js:1421 findConnectedTablesInRange(connectedTables, ...)
           -> index.js:1439 getExploreForTableRange(tableInRange, ...)
           -> index.js:877  getProcessedConnectedTableForDrillDown(connectTable, range)
  Structural incompatibility:
    global-common-utils Connect.table: ServerTable   (types.ts:170 `measures: string[]`,
      types.ts:171 `dimensions: Dimension[]` plain objects, no methods)
    addin-core ConnectedTable.table?: Table          (entity, has getMeasureByText)
  Used-as-entity proof:
    index.js:1738 `dimensions: getVisibleDimensions(table.dimensions)` and
    utils.ts:398-400 `getVisibleDimensions(dimensions: Dimension[]) { ... dim.clone() }`
    utils.ts:939+ processConnectMeasures: `connectedTable.table.getMeasureByText(measure)`
  Fallout already visible in the diff: findConnectedTablesInRange.test.ts had to
  add `as unknown as Connect` to both fixture factories.

FAILURE SCENARIO: anyone typing index.js (the sibling TYP-796 branch does exactly
this) trusts `Connect` and writes `tablesInRange[0].table.measures[0]` expecting a
string, or drops the `.clone()`-capable Dimension assumption; the compiler agrees
and the code crashes at runtime with "table.dimensions[i].clone is not a function"
/ "getMeasureByText is not a function". Right now the wrong type is silently
absorbed only because index.js is untyped.

--------------------------------------------------------------------------------
C3  packages/addin-core/src/operations/buildExplores/utils.ts:711   [MEDIUM-HIGH]
--------------------------------------------------------------------------------
SUMMARY: `return narrowedMatches.filter(Boolean)` does not narrow in TypeScript,
so the newly-typed `findConnectedTablesInRange` publicly returns
`(Connect | undefined)[]`, and that `undefined` leaks across package boundaries.

EVIDENCE:
  utils.ts:691-709 `const narrowedMatches = await Promise.all(rawMatches.map(... return undefined ...))`
                   -> `(Connect | undefined)[]`
  utils.ts:711     `return narrowedMatches.filter(Boolean);`
  Compiler proof (from removing the new @ts-expect-error in
  apps/addin-components/src/components/ActionButtons/ActionButtonsWrapper.tsx and
  running types:check):
    "... discoveryContext: { ... connectedTable: Connect | undefined; ... }"
  A ready-made guard already exists and is unused here:
    packages/global-common-utils/src/nullabilityUtils.ts:12
      export const isNotNullish = <T>(value: T | null | undefined): value is T

FAILURE SCENARIO: index.js:1421-1423 does
  `if (tablesInRange.length === 1) { [tableInRange] = tablesInRange; }`
and index.js:1745 stores `connectedTable: tablesInRange[0]`. Per the declared
type that element may be `undefined`, so any downstream TS code that consumes
`discoveryContext.connectedTable` (web-ui / addin-components drilldown) must either
carry a bogus undefined check or, as here, suppress the error entirely --
contributing to C4.

--------------------------------------------------------------------------------
C4  apps/addin-components/src/components/ActionButtons/ActionButtonsWrapper.tsx:125
    (same defect at apps/web-ui/src/pages/Workbooks/Workbook/Workbook.tsx:1011)  [MEDIUM-HIGH]
--------------------------------------------------------------------------------
SUMMARY: the two new `@ts-expect-error TYPETODO` comments do not suppress a
"needs to be typed" nit -- they suppress a genuine nullability error introduced by
newly typing `getVisibleDimensions`: `dimensions` may be `undefined` while
`DrillDownButtonProps.getDrillDownByOptions` promises `Dimension[]`.

EVIDENCE (verbatim compiler output after temporarily deleting the suppression):
  ActionButtonsWrapper.tsx(125,9): error TS2322: Type '() => Promise<{
    dimensions: Dimension$1[] | undefined; suggestedDimensions: never[];
    discoveryContext: { ... connectedTable: Connect | undefined; ... } ...' is not
    assignable to type '() => Promise<{ dimensions: Dimension$1[];
    suggestedDimensions: string[]; discoveryContext?: unknown; }>'.
  Consumer that will crash:
    packages/frontend-common-components/src/components/ActionButtons/DrilldownByOptionsModal.tsx:37
      drillDownDimensions.map((elem) => ({ label: elem.prettyName, value: elem.name, ... }))
  (No `?? []` anywhere on the path: DrillDownButton.tsx:66-70 spreads the result
  straight into modal state.)

FAILURE SCENARIO: a getDrillDownByOptions branch that yields no dimensions (the
`getDrillDownByOptions` implementation in
packages/addin-core/src/operations/buildExplores/index.js:1600-1860 has several
returns; the compiler resolves the union to `Dimension[] | undefined`) reaches
DrillDownByOptionsModal and throws "Cannot read properties of undefined (reading
'map')", breaking the Drill-Down-By modal instead of showing an empty list.

--------------------------------------------------------------------------------
C5  packages/addin-core/src/operations/buildExplores/utils.ts:446   [MEDIUM]
--------------------------------------------------------------------------------
SUMMARY: `[filterGroup] = filterValues as [FilterGroup];` asserts away a real
polymorphic return -- `getFilterGroup` genuinely returns a bare `Filter` in the
single-dimension/single-value case -- so the function's inferred return type
`FilterGroup` is a lie.

EVIDENCE:
  trunk:  `[filterGroup] = filterValues;`            (untyped, honest)
  branch: `[filterGroup] = filterValues as [FilterGroup];`
  utils.ts:408 param type `Record<string, Record<string, FilterGroup['body']>>`
  packages/addin-core/src/entities/FilterGroup.ts:8
    type FilterGroupBody = (FilterGroup | Filter<FilterValue>)[];
  packages/addin-core/src/entities/Filter.ts:59   `body?: never;`
  packages/addin-core/src/entities/FilterGroup.ts:41
    static parse(...): FilterGroup | Filter<FilterValue>   <-- the honest signature
  Path: dimensionValues.length === 1 -> `new FilterGroup({})` (no operator) ->
  rootOperator undefined -> Object.keys(filterValuesFilterMap).length === 1 so
  isVariableDimension false -> falls to the `else` -> filterValues[0] is the
  `new Filter({...})` built at index.js:564-570 / :575.

FAILURE SCENARIO: a TS caller of `getFilterGroup` (index.js:593 once converted)
trusts the `FilterGroup` return and does `result.body.push(extraFilter)` or
iterates `result.body`; for the extremely common single-fixed-dimension scalar
drilldown the value is a `Filter` whose `body` is `undefined`, giving
"Cannot read properties of undefined (reading 'push')" with no compiler warning.

--------------------------------------------------------------------------------
C6  packages/addin-core/src/operations/buildExplores/utils.ts:382   [MEDIUM]
--------------------------------------------------------------------------------
SUMMARY: `getColumnDimensions(table, dimensionFilterMap: Record<string, unknown[]>)`
contradicts its own body and its only caller -- the values are
`Record<string, Filter[]>`, not arrays.

EVIDENCE:
  utils.ts:382-386
    export function getColumnDimensions(table: Table, dimensionFilterMap: Record<string, unknown[]>) {
      ...
      const isVariableDimension = Object.keys(dimensionFilterMap[value]).length > 1;
  (`Object.keys(...)` on a value only makes sense for a record)
  index.js:557-591 builds it as `dimensionFilterMap[dimensionOption.value] = { [value]: [filter] }`
  index.js:593 `const filterGroup = getFilterGroup(dimensionFilterMap);`      <- Record<string, Record<...>>
  index.js:605 `const columnDimensions = getColumnDimensions(table, dimensionFilterMap);` <- SAME object
  So one object is simultaneously declared `Record<string, Record<string, FilterGroup['body']>>`
  and `Record<string, unknown[]>`.

FAILURE SCENARIO: when index.js is converted to TS the shared `dimensionFilterMap`
cannot satisfy both signatures, forcing a cast; or a future caller obeys the
signature and passes an actual array, where `Object.keys(arr).length > 1` silently
means "length > 1" for arrays but "more than one filter value" for records -- the
two notions diverge as soon as a single filter value carries multiple filters.

--------------------------------------------------------------------------------
C7  packages/addin-core/src/transformers/explores.ts:161   [MEDIUM]
--------------------------------------------------------------------------------
SUMMARY: `SpreadsheetInput.sheetRangesValues` inner values were widened to
`unknown[][] | undefined` purely to satisfy a spread of a possibly-undefined
accumulator in utils.ts -- the data source never produces undefined, so the
contract now lies to every consumer.

EVIDENCE:
  trunk:  sheetRangesValues?: Record<string, Record<string, unknown[][]> | undefined>;
  branch: sheetRangesValues?: Record<string, Record<string, unknown[][] | undefined> | undefined>;
  the only reason (utils.ts:278-286):
    recording.data.input.spreadsheet.sheetRangesValues = Object.entries(serviceResult).reduce<...>(
      (acc, [id, values]) => ({ ...acc, [idToSheetMap[id].name]: { ...acc[idToSheetMap[id].name], ...values } }),
      recording.data.input.spreadsheet.sheetRangesValues ?? {},
    );
  (`acc[name]` is `... | undefined`, so TS pushes `| undefined` into the inner
  value type; the root fix is `...(acc[idToSheetMap[id].name] ?? {})`)
  the source never returns undefined:
    packages/global-common-utils/src/types.ts:630-633
      getRangesValues: (...) => Promise<Record<string, Record<string, CellValue[][]>>>;

FAILURE SCENARIO: packages/addin-core/integration-tests/utils/spreadsheet.ts:84
now yields `{ range, values: unknown[][] | undefined }` and pushes it into
`spreadsheetService.setRangesValues(...)` on line 90, whose type error is already
`@ts-expect-error`-suppressed -- so an undefined grid reaches the spreadsheet
service unchecked instead of being rejected by the compiler. Every other consumer
must now add a `?? []`/`?? {}` for a state the producer cannot create.

--------------------------------------------------------------------------------
C8  packages/global-common-utils/src/connectUtils.ts:9-13 and :21-25   [LOW-MEDIUM]
--------------------------------------------------------------------------------
SUMMARY: the new `specialConnectFilter?: { isOn?: boolean }` is a third, lossy
copy of an existing type -- it drops `range` and `id` and makes `isOn` optional --
which is exactly why the tests needed `as unknown as Connect`.

EVIDENCE:
  connectUtils.ts:9-13 / 21-25 (new)
    specialConnectFilter?: { isOn?: boolean; };
  packages/addin-core/src/entities/ConnectedTable.ts:20 (pre-existing, in the very
  package that consumes the new Connect)
    export type SpecialConnectFilter = { id?: string; range: string; isOn: boolean };
    ... used at ConnectedTable.ts:30 (ConnectRowSettings) and :40 (ConnectColumnSettings)
  the smoking gun in the test diff:
    makeFilteredConnect({ rowSettings: { specialConnectFilter: { range: 'E2:E10', isOn: true } } })
      ... }) as unknown as Connect;      <-- `range` is not in the new type

FAILURE SCENARIO: a TS consumer of `Connect` cannot read
`connect.rowSettings.specialConnectFilter.range`, even though
getConnectWithModifiedDimensionRanges (operations/connects.js) depends on that
field to compute the narrowed dimension ranges; and because `isOn` is optional
here but required in addin-core, the two definitions will drift. CLAUDE.md:
"Before writing a helper, hook, type ... search for an existing one".

--------------------------------------------------------------------------------
SECONDARY / LOW (not in the JSON top-8)
--------------------------------------------------------------------------------
S1. utils.ts:191-194 vs :468-471 -- `TableAndCellReference.cellReferences: CellValue[]`
    (producer) vs `FilledTableAndCellReference.cellReferences: string[]` (consumer,
    used by dimensionsMatch:589 and getFilterGroupAndVaryingDimensionsForScalarFormulas:478).
    Same file, contradictory. index.js:983 already does `value?.toLowerCase()` on
    these values, which throws for numeric cell values. Will surface when index.js
    is typed.
S2. utils.ts:151-152 -- stale `let` + stale justification
    `// eslint-disable-next-line prefer-const -- alephFormulaArgs is modified`;
    nothing is reassigned any more (the refactor introduced alephFormulaArgPieces
    at line 174). All four destructured bindings are now const-eligible.
S3. utils.ts:170-172 -- `throw new Error('Expected aleph formula to have args')`
    inside getSingleAlephFormula is unreachable (the nFormulas 0/>1 checks above
    it already cover it). The twin at utils.ts:134-136 (hasArrayParameters) IS
    reachable for a cell containing two ALEPH calls and replaces a TypeError with a
    bare developer-only `Error`, where every other throw in the file is an
    AlephError/DrillDownError with a user-facing message.
S4. Non-null assertions added where a guard belongs (CLAUDE.md violation):
    utils.ts:357 `getDimensionByValue(dimension)!` (masks the crash at :360
    `filters.forEach(({ text }) =>` when a dimension is missing), :497, :715
    `e.sheet!.id`, :728-729 `previousRowsCount!`/`previousColumnsCount!`,
    :771/:795/:819 `dimension!.value`.
S5. Test hygiene: packages/addin-core/tests/operations/buildExplores/newUtils.test.ts
    -- file name and `describe('newUtils')` (line 29) refer to a deleted module;
    should be folded into utils.test.js / renamed. And
    findConnectedTablesInRange.test.ts now double-casts its fixtures
    (`as unknown as Connect`), so they no longer validate against the function's
    declared input contract.
S6. utils.ts duplicated import sources: `@getaleph/global-common-utils` at lines 4
    and 5, `../../transformers` at lines 30-35 and 36, `../connects` at lines 38
    and 39.
S7. utils.ts:777 and :801 -- processRows/processColumns lost `async`, so a
    synchronous throw now short-circuits the Promise.all argument list at :825-830
    and leaves processFilters()'s promise dangling. Cosmetic in practice.
S8. apps/server/src/operations/alephFunction.ts:102 -- `!parsed.alephFormula` is
    dead at runtime; it exists only because getSingleAlephFormula's new return type
    is a non-discriminated `{ alephFormula: string | undefined; ... } | undefined`.
    The comment beneath it ("`parsed` is undefined when: ...") no longer describes
    the condition. Fix at the root: make splitAlephFormula/getSingleAlephFormula
    return a discriminated union so callers don't need the dead check.

--------------------------------------------------------------------------------
EXPLICITLY REFUTED (do not re-report)
--------------------------------------------------------------------------------
R1. The four moved newUtils bodies are byte-identical apart from `new Set([])` ->
    `new Set<string>()` and four now-unnecessary `@ts-expect-error` lines.
R2. All './newUtils' importers were updated (only the test file NAME is stale).
R3. Nothing was dropped from the old workbook-utils Connect/ConnectSettings/
    ConnectRowSettings/ConnectDimension; `sheet: {id,name}` -> SheetWithoutActive
    (= {name,id,isActive?}) is purely additive.
R4. workbook-utils' `ServerTable` is a re-export of global-common-utils'
    `ServerTable` (useWorkbookTables.ts:2,10) -- identical, no structural drift.
R5. `serviceResult[sheet.id]?.[cell]` is equivalent to the old
    `arg.isRef && serviceResult[sheet.id] && serviceResult[sheet.id][cell]`
    (the code is already inside `if (arg.isRef)`).
R6. getSingleAlephFormula's returned OBJECT SHAPE is unchanged.
R7. The getSheets()/splitCellAndSheet async rewrite is a real BUG FIX: on trunk
    `getSheets()` returned a Promise, so `.find(...)` threw a TypeError for every
    cross-sheet reference. The new AlephError lands in the existing swallowing
    try/catch at index.js:961-978, which is exactly the "fall back to refreshing
    the formula and reading the cache" the new comment claims. The two integration
    fixture additions are the consequence of this fix.
R8. `replace(/\$/g,'')` -> `replaceAll('$','')`, `variableDimensionsCount +=
    bool ? 1 : 0`, `(x ?? NaN) - 1`, brace-only reformatting in
    compareRowExtentsLessThan / coerceArg / runResolvedFunction, and
    `catch (e)` -> `catch (error)`: all behaviour-preserving.
R9. Sheet -> SheetWithoutActive in data-access and SheetRangesPairsList is
    behaviourally safe: `.isActive` is only read on sheet LISTS in
    trackActiveSheet, never inside getRangesValues.
R10. frontend-common-components already depends on @getaleph/addin-core and
    addin-core does not depend back, so importing `Dimension` from addin-core adds
    no missing dep and no cycle. The modal only reads `.prettyName`/`.name`.
R11. No test case or assertion was deleted or weakened in either test file; the
    ACTIVE_SHEET fixture was actually strengthened with an `id`.
R12. types:check passes (exit 0) for addin-core, global-common-utils,
    workbook-utils and frontend-common-components on this branch.
