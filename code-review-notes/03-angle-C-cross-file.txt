================================================================================
1. PROMPT RECEIVED (verbatim)
================================================================================
You are finder Angle C ("cross-file tracer") — one of 10 parallel finder agents in a maximum-effort code review.

REVIEW SCOPE: the diff of git branch `colton/typ-797-convert-packagesaddin-coresrcoperationsbuildexploresutilsjs` vs `trunk`, in repo /Users/colton/code/monorepo.
- Full unified diff is saved at: /Users/colton/code/monorepo/code-review-notes/00-review.diff  (READ THIS FIRST, in full)
- Regenerate with: `git diff trunk...HEAD` run from /Users/colton/code/monorepo
- What the PR does: converts packages/addin-core/src/operations/buildExplores/utils.js to TypeScript, merges the deleted newUtils.ts into utils.ts, moves the `Connect` type from packages/workbook-utils/src/connect.ts to packages/global-common-utils/src/connectUtils.ts, adds `SheetWithoutActive`, and touches apps/gs-addin formulaUtils.ts, apps/server alephFunction.ts, addin-components, frontend-common-components, and two integration fixtures.

YOUR ANGLE: For each function/type the diff changes, find its CALLERS (Grep for the symbol across /Users/colton/code/monorepo/apps and /Users/colton/code/monorepo/packages, excluding node_modules) and check whether the change breaks any call site: a new precondition, a changed return shape, a new thrown exception, a new async/await requirement, a timing or ordering dependency. Also check CALLEES: does a parallel change in the same PR make a call unsafe?

Symbols/types to trace exhaustively:
- `getTablesAndCleanCellReferences`, `parseAndCleanAlephFormula`, `getSingleAlephFormula`, `splitAlephFormula`, `hasArrayParameters`, `buildAlephFormulasMap`, `getFilterGroup`, `getFilterGroupAndVaryingDimensionsForScalarFormulas`, `getRowDimensions`, `getColumnDimensions`, `getVisibleDimensions`, `findConnectedTablesInRange`, `findExploreInRange`, `getProcessedConnectedTableForDrillDown`, `tablesMatch`, `dimensionsMatch`, `pushAllValuesToFilterGroup`, `getFilterGroupNew`, `getColumnDimensionsNew`, `getRowDimensionsNew`, `processConnectMeasures`
- `Connect`, `ConnectDimension`, `ConnectSettings`, `ConnectRowSettings`, `ConnectDirection` (moved package!), `ServerTable` (there are TWO — one in global-common-utils, one in workbook-utils/useWorkbookTables), `SheetWithoutActive`, `Sheet`, `DimensionGroup`
- `getRangeValues`, `getSelectionValues`, `getRangesValues`, `getOptimizedSheetRangesPairsList` (signatures widened from `Sheet` to `SheetWithoutActive`)
- `getDrillDownByOptions` prop on `DrillDownButton` / `ActionButtons` (type changed from `ServerTable['dimensions']` to `Dimension[]` from `@getaleph/addin-core`)

Critical checks:
1. `splitCellAndSheet` inside `getTablesAndCleanCellReferences` now THROWS `AlephError` when a sheet named in a formula cannot be found. Trace every caller chain up from `getTablesAndCleanCellReferences` / `parseAndCleanAlephFormula` and determine whether that throw is caught, and whether the code comment "Let the caller fall back to refreshing the formula and reading the cache" is actually true. Name the file/line of the catch (or the absence of one).
2. `packages/frontend-common-components` now imports `Dimension` from `@getaleph/addin-core`. Check /Users/colton/code/monorepo/packages/frontend-common-components/package.json actually declares that dependency, and check for a dependency CYCLE (does addin-core depend, directly or transitively, on frontend-common-components?). Also check whether the app(s) consuming frontend-common-components (web-ui, addin-components, gs-addin) can resolve @getaleph/addin-core.
3. `packages/workbook-utils/src/connect.ts` now re-exports the Connect types from `@getaleph/global-common-utils`. Confirm workbook-utils declares that dependency and that no import cycle is introduced. Then find all consumers of `Connect` from `@getaleph/workbook-utils` and check whether the new structural shape (notably `sheet: SheetWithoutActive` and `table: ServerTable` from a DIFFERENT module) type-checks at those sites.
4. `Table` entity gained `dimensionGroups?: DimensionGroup[]` typed from global-common-utils. Check what actually gets assigned there at runtime (Entity#set / Object.assign from server payload) and whether the server's shape matches `DimensionGroup` (required fields like `__v`, `table`, `tooManyValues`, `hiddenDimensions`).
5. The two `@ts-expect-error` comments added in apps/addin-components/.../ActionButtonsWrapper.tsx and apps/web-ui/.../Workbook.tsx: verify each actually suppresses an error (an unused `@ts-expect-error` is itself a compile error) and that the underlying mismatch is real.
6. `newUtils.test.ts` now imports from `../../../src/operations/buildExplores/utils`. Grep for any other file still importing `./newUtils` or `newUtils.ts`.
7. Run type checks if cheap and safe, to gather hard evidence. Suggested (use pnpm only, never npx): `cd /Users/colton/code/monorepo && pnpm turbo run types:check --filter=@getaleph/addin-core --filter=@getaleph/frontend-common-components --filter=@getaleph/workbook-utils --filter=@getaleph/global-common-utils` . If it is slow or fails for unrelated environment reasons, note that and move on — do NOT block on it. Time-box any build/typecheck to ~5 minutes.

MANDATORY NOTE-TAKING (explicit user request): You MUST write a detailed plain-text notes file at
/Users/colton/code/monorepo/code-review-notes/03-angle-C-cross-file.txt
using the Write tool, BEFORE returning your final answer. It MUST contain, in this order:
1. "PROMPT RECEIVED" — the verbatim full text of this prompt.
2. "WHAT I AM LOOKING FOR"
3. "GOALS"
4. "METHOD / HOW I ACCOMPLISHED IT" — chronological log: every command run (with output summary), every file read (absolute path + line ranges), what you concluded. Include dead ends and refuted hypotheses.
5. "CANDIDATES" — file, line, summary, failure scenario, quoted code evidence.

OUTPUT: return a JSON array of up to 8 candidate findings, each {"file","line","summary","failure_scenario"}. RECALL mode — err on the side of surfacing. If you truly find nothing, return [].


================================================================================
2. WHAT I AM LOOKING FOR
================================================================================
Cross-file breakage caused by this PR. Specifically:
- A changed signature / return shape / thrown exception / async-ness that a CALLER in
  another file cannot satisfy or does not handle.
- A CALLEE changed in the same PR that makes an existing call unsafe.
- Type annotations added during the JS->TS conversion that are FACTUALLY WRONG about
  the runtime values flowing through them (a "type lie"): these do not fail `tsc`
  today because the callers live in .js files, but they mislead every future reader
  and will break the next conversion step.
- Moved types (`Connect` family from workbook-utils -> global-common-utils) introducing
  missing dependencies, dependency cycles, or structural mismatches at consumer sites.
- `@ts-expect-error` directives that either (a) are unused (itself a compile error) or
  (b) hide a genuine runtime hazard.
- Regex / statefulness / ordering regressions introduced by "cosmetic" rewrites
  (e.g. `.match()` -> `.test()`), which are the classic silent killers in a
  reformatting-heavy PR.

================================================================================
3. GOALS
================================================================================
G1. Read the full diff (1536 lines) before touching anything else.
G2. Enumerate every exported symbol whose signature/behavior the diff touches, and
    grep the WHOLE monorepo (tracked files only, no dist/node_modules) for callers.
G3. Answer the 7 explicit critical checks with file:line evidence, including
    explicitly REFUTING the ones that turn out to be fine.
G4. Gather hard evidence: run `types:check` on every affected package; run `lint` on
    addin-core; reproduce suspected runtime bugs with `node -e`.
G5. Do NOT mutate the working tree (9 other agents share this checkout).
G6. Write these notes, then return <=8 findings as JSON.

================================================================================
4. METHOD / HOW I ACCOMPLISHED IT  (chronological)
================================================================================

--- STEP 1: orient ---
CMD: `ls -la code-review-notes/ && wc -l code-review-notes/00-review.diff`
  -> 00-review.diff, 1535 lines, 59145 bytes.
CMD: `git log --oneline trunk..HEAD` + `git diff trunk...HEAD --stat`
  -> 8 commits (d48c770a02 "start" .. c9ec31c19c "guh").
  -> 20 files changed, 489 insertions, 366 deletions. Confirms the file list in the
     prompt. Note `utils.js -> utils.ts` rename with 66% similarity and
     `newUtils.ts` deleted (155 lines) and re-appended into utils.ts.

--- STEP 2: read the full diff ---
READ /Users/colton/code/monorepo/code-review-notes/00-review.diff lines 1-1250, then 1250-1536.
Observations recorded while reading:
  - diff:39  apps/gs-addin formulaUtils.ts `isAlephFormula`:
              `!!s.match(ALEPH_FORMULA_REGEX)` -> `Boolean(ALEPH_FORMULA_REGEX.test(s))`.
              FLAGGED IMMEDIATELY: ALEPH_FORMULA_REGEX has the /g flag. `.test()` on a
              /g regex mutates lastIndex; `.match()` does not. Prime suspect.
  - diff:120 apps/server alephFunction.ts added `|| !parsed.alephFormula`.
  - diff:187-232 data-access: `Sheet` -> `SheetWithoutActive` widenings.
  - diff:248-257 Table entity gains `dimensionGroups?: DimensionGroup[]`.
  - diff:640-768 getTablesAndCleanCellReferences: splitCellAndSheet becomes async,
              `getSheets()` is now AWAITED (was `getSheets().find(...)` — synchronous
              use of a Promise!), and a NEW `throw new AlephError(...)` for
              "sheet not found".
  - diff:920  `tablesMatch(tables: { table: { id: string } }[])`
  - diff:938  `connectAreaContainsRange({...}: Connect, activeSheet: SheetWithoutActive, ...)`
  - diff:966  `findConnectedTablesInRange(connectedTables: Connect[], ...)`  <- suspicious
  - diff:1013 `getProcessedConnectedTableForDrillDown(connectedTable: ConnectedTable, ...)`
              <- SAME runtime objects as above but a DIFFERENT declared type.
  - diff:1253-1272 the test file now needs `as unknown as Connect` TWICE. Smell.
  - diff:1294/1347 frontend-common-components now imports `Dimension` from
              '@getaleph/addin-core' instead of `ServerTable` from workbook-utils /
              global-common-utils.
  - diff:1366-1426 new packages/global-common-utils/src/connectUtils.ts.
  - diff:1443-1467 DimensionGroup gains optional fields; Sheet gains SheetWithoutActive;
              SheetRangesPairsList.sheet widened.
  - diff:1473-1536 workbook-utils/src/connect.ts reduced to a pure re-export.

--- STEP 3: read the new file in full ---
READ /Users/colton/code/monorepo/packages/addin-core/src/operations/buildExplores/utils.ts (1-993, whole file).
  Noted:
  - lines 3-5: THREE separate import statements, two of them from the SAME module
    ('@getaleph/global-common-utils'). Also lines 30-36 both import from
    '../../transformers', and lines 38-39 both import from '../connects'.
    => 3 duplicate-import pairs.
  - lines 56-69: `ParsedAlephFormula` union is NOT discriminated (`nFormulas: number`
    in both variants), so destructuring in getSingleAlephFormula yields
    `string | undefined` for alephFormula/input. That is the root cause of the
    `|| !parsed.alephFormula` band-aid in apps/server.
  - lines 129-138 hasArrayParameters: new `throw new Error('Expected aleph formula to
    have args')`.
  - lines 170-172 getSingleAlephFormula: same new throw (unreachable: nFormulas===0
    returns early, nFormulas>1 throws DrillDownError, nFormulas===1 always has args).
  - lines 191-194 `TableAndCellReference.cellReferences: CellValue[]`
  - lines 468-471 `FilledTableAndCellReference.cellReferences: string[]`
    => two contradictory declared shapes for the same data flow.
  - lines 205-209 / 312-316: `recording` is a REQUIRED parameter.
  - lines 215-250: sheetsCache + async splitCellAndSheet + new AlephError throw.
  - lines 355/382/408: getRowDimensions(Record<string, unknown>),
    getColumnDimensions(Record<string, unknown[]>),
    getFilterGroup(Record<string, Record<string, FilterGroup['body']>>)
    -> three different annotations, all fed the SAME object by index.js.
  - line 672 findConnectedTablesInRange(connectedTables: Connect[])
  - line 743 getProcessedConnectedTableForDrillDown(connectedTable: ConnectedTable)
  - line 930 a surviving `@ts-expect-error Table is yet not fully typed...` inside
    getRowDimensionsNew (real cause is getDimensionByAnyMatch returning `| undefined`).

--- STEP 4: callers of getTablesAndCleanCellReferences / parseAndCleanAlephFormula ---
CMD: grep across apps+packages.
  -> only packages/addin-core/src/operations/buildExplores/index.js:185, 964, 1800
     (plus dist/ build output, ignored).

CMD: `grep -n "export const getSheets" -A 20 packages/addin-core/src/data-access/index.ts`
  -> line 59: `export const getSheets = (): Promise<Sheet[]> => {...}`
CMD: `git show trunk:packages/addin-core/src/data-access/index.ts | grep -n getSheets`
  -> line 58, same `Promise<Sheet[]>` in trunk.
CMD: `git show trunk:packages/addin-core/src/operations/buildExplores/utils.js | sed -n '160,240p'`
  -> confirms trunk had `internalSheet = getSheets().find((s) => s.name === sheetName);`
CONCLUSION (important): on trunk, ANY cross-sheet reference (`Sheet4!A1`) threw
  `TypeError: getSheets(...).find is not a function`. So cross-sheet refs NEVER
  resolved locally on trunk; they always fell through to the cache/refresh path.
  This PR makes them work. That explains the two integration-fixture edits that add
  `"Sheet4": { "A1": [["Sage Intacct (Native) | Profit and Loss"]] }`. => genuine
  BEHAVIOR change hidden inside a "convert to TS" PR, with no new unit test.

--- STEP 5: is the new AlephError throw caught? (critical check #1) ---
READ /Users/colton/code/monorepo/packages/addin-core/src/operations/buildExplores/index.js 150-230, 930-1060, 1440-1600, 1760-1860, 865-930, 555-615, 1405-1435, 1720-1760.
  - index.js:962-978 IS a try/catch around `parseAndCleanAlephFormula`:
        } catch (error) {
          // TODO: ERROR TO FIX - SWALLOWED LOGGED
          console.log('Something failed with the aleph formula parsing and cleaning: ', error);
        }
        ...
        cellsToRefresh.push(cellA1);
    -> then index.js:990 `triggerRefreshFormulas` + read from cache.
    => The comment "Let the caller fall back to refreshing the formula and reading the
       cache" is TRUE for the parseAndCleanAlephFormula chain. CATCH IS AT
       packages/addin-core/src/operations/buildExplores/index.js:975.
  - The other two call sites (index.js:185 inside getExploreForScalarAlephFormula, and
    index.js:1798 inside getDrillDownByOptions) are NOT wrapped in a try/catch.
    BUT: I then verified their inputs.
READ /Users/colton/code/monorepo/packages/addin-core/src/operations/SpreadsheetUtils.js 120-200.
  -> getFormulaArgumentsFromCache builds every arg as `{ value: param, isRef: false }`.
  -> parseAndCleanAlephFormula also returns every arg with `isRef: false`.
  => at index.js:185 and :1798 no arg has isRef===true, so splitCellAndSheet is never
     invoked and the new throw is UNREACHABLE from those two sites.
  => HYPOTHESIS REFUTED: the new throw does not escape uncaught. Downgraded to a
     logging-noise finding (see CANDIDATE 7) because `new AlephError(msg)` uses the
     default `options = { shouldLog: true }` (verified in
     /Users/colton/code/monorepo/packages/frontend-common-utils/src/error/error.ts:139-158,
     `if (options.shouldLog) { console.error(this); }`), so an EXPECTED fallback path
     now emits console.error.
  Also noted: splitCellAndSheet does not strip quotes, so `'My Sheet'!A1` (the standard
  A1 form for a sheet name containing a space) can never match `s.name === sheetName`
  and therefore always takes the new throw.

--- STEP 6: critical check #2 (frontend-common-components -> addin-core) — REFUTED ---
CMD: `cat packages/frontend-common-components/package.json`
  -> dependencies include `"@getaleph/addin-core": "workspace:*"`. Declared. OK.
CMD: node -e on packages/addin-core/package.json
  -> addin-core deps: frontend-common-utils, global-common-utils, server-sdk, axios,
     colorjs.io, colorparsley, dayjs, fast-formula-parser, lodash, nanoid.
     NO frontend-common-components, NO workbook-utils. => NO CYCLE.
CMD: `grep -rln '"@getaleph/frontend-common-components"' --include=package.json`
  -> web-ui, ppt-addin, excel-addin, addin-components, slides-ui, image-generation-service.
  addin-core is already imported at runtime by frontend-common-components
  (packages/frontend-common-components/src/components/Reports/CustomDataTableChart.tsx:56
  uses `ExploresOperations.getSingleAlephFormula`), so this is not a new runtime edge.
  => CHECK #2 FULLY REFUTED. No missing dep, no cycle.

--- STEP 7: critical check #3 (Connect move) — LARGELY REFUTED ---
CMD: `sed -n '1,14p' packages/workbook-utils/src/useWorkbookTables.ts`
  -> line 1-7 `import { ServerTable, Dimension, DimensionGroup, OrderCriteria,
     CustomListCriteria } from '@getaleph/global-common-utils';`
     line 10 `export type { ServerTable, ... };`
  => There is only ONE `ServerTable`; workbook-utils merely RE-EXPORTS the
     global-common-utils one. The prompt's premise that there are two distinct
     `ServerTable` types is WRONG. `Connect.table: ServerTable` is unchanged.
CMD: `cat packages/workbook-utils/package.json`
  -> declares `"@getaleph/global-common-utils": "workspace:*"`. OK. global-common-utils
     depends on nothing internal except server-sdk => no cycle.
CMD: `grep -n "export type Sheet\b" -B3 -A12 packages/global-common-utils/src/types.ts`
  -> Sheet = { name, id, isActive }. SheetWithoutActive = Omit<Sheet,'isActive'> &
     { isActive?: boolean }. So `Connect.sheet` went from `{id, name}` to
     `{id, name, isActive?}` — same required fields => structurally compatible.
  Also `ConnectSettings`/`ConnectRowSettings` only GAINED an optional
  `specialConnectFilter?: { isOn?: boolean }` => widening only.
CMD: git grep for Connect consumers
  -> packages/frontend-common-components/src/components/InThisFile/CopyConnectedTableModal.tsx:13
     packages/frontend-common-components/src/components/InThisFile/types.ts:2
     both `import { Connect, Sheet } from '@getaleph/workbook-utils'` — still resolve.
  => CHECK #3 REFUTED for the sites that consume Connect *from workbook-utils*.
     (The real Connect problem is a different one — see CANDIDATE 2.)
CMD: grep for `isActive` in the SheetRangesPairsList implementors
     (apps/workbook-automation-service/server/{Sharepoint,Google}SpreadsheetService.ts,
      apps/gs-addin/src/client/services/ApiSpreadsheetService.ts,
      packages/workbook-utils/src/spreadsheetService.ts)
  -> nobody reads `.isActive` off a SheetRangesPairsList element. Widening is safe.
  => the Sheet -> SheetWithoutActive widenings are all in the safe direction. REFUTED.

--- STEP 8: critical check #4 (Table.dimensionGroups) — MOSTLY REFUTED ---
READ /Users/colton/code/monorepo/packages/addin-core/src/entities/Table.ts 1-120.
  -> `dimensionGroups?: DimensionGroup[]` added to both TableOptions and the class;
     TableOptions has `[key: string]: unknown` and the ctor does
     `super({ value, text, ...options })`, i.e. Entity#set/Object.assign. So the value
     really is whatever the server sent.
READ /Users/colton/code/monorepo/packages/global-common-utils/src/types.ts 90-108 (DimensionGroup)
READ /Users/colton/code/monorepo/apps/server/src/mappers/tables.js 40-120
  -> the mapper destructures `dimensionGroups` off the Mongoose doc and passes it
     through verbatim, so `__v`, `table`, `tooManyValues`, `hiddenDimensions` come from
     the schema. `ServerTable.dimensionGroups?: DimensionGroup[]` ALREADY used this
     exact type before the PR (global-common-utils/src/types.ts:172), so the new
     annotation on the addin Table is consistent with the pre-existing one. Not a new
     defect. => REFUTED (no finding).
  Side observation: apps/addin-components/src/api/tables.ts:41 does
  `table.dimensionGroups.find(...)` under a `@ts-expect-error`; with the property now
  declared as optional, that directive stays used (possibly-undefined). Fine.

--- STEP 9: critical check #6 (stale newUtils imports) — REFUTED ---
CMD: `grep -rn "newUtils" --include=*.{ts,tsx,js,jsx} apps packages | grep -v node_modules | grep -v /dist/`
  -> single hit: packages/addin-core/tests/operations/buildExplores/newUtils.test.ts:29
     `describe('newUtils', () => {` — a stale name only. No stale import anywhere.
  Note: the file is still called newUtils.test.ts while
  packages/addin-core/tests/operations/buildExplores/utils.test.js already exists for
  the same module => two differently-named test files for one module.

--- STEP 10: critical check #7 (type checks) — HARD EVIDENCE ---
CMD (background, 38s): `pnpm turbo run types:check --filter=@getaleph/addin-core
     --filter=@getaleph/frontend-common-components --filter=@getaleph/workbook-utils
     --filter=@getaleph/global-common-utils --filter=@getaleph/workbook-automation-service`
  -> "Tasks: 13 successful, 13 total". EXIT 0.
CMD (background, 15s): `pnpm turbo run types:check --filter=@getaleph/web-ui
     --filter=@getaleph/addin-components --filter=@getaleph/gs-addin`
  -> "Tasks: 12 successful, 12 total". EXIT 0.
  => Nothing in the PR fails tsc. In particular BOTH new `@ts-expect-error`s must be
     USED (an unused one is TS2578 and would have failed). CHECK #5's first half:
     satisfied, both directives suppress a real error.

--- STEP 11: what exactly do the two @ts-expect-error directives suppress? ---
ATTEMPT (DENIED, and rightly so): I tried
  `perl -0pi -e 's{...@ts-expect-error...}{}' apps/web-ui/.../Workbook.tsx ...`
  to strip both directives and re-run tsc. The permission classifier blocked it
  because 9 other agents share this working tree. I did NOT work around it; the tree
  stayed clean (`git status --porcelain` -> only the untracked code-review-notes/).
  I substituted static evidence instead:
CMD: `grep -n "getDrillDownByOptions" -A 30 packages/addin-core/dist/index.d.ts`
  -> lines 4348-4378 of the freshly-built d.ts:
       getDrillDownByOptions: (args_0?: any) => Promise<{
           dimensions: Dimension$1[];
           suggestedDimensions: never[];
           discoveryContext: { sourceType: string; range: Range$1;
             activeSheet: Sheet;
             rangeValues: unknown[][];
             connectedTable: _getaleph_global_common_utils.Connect | undefined;
             explore?: undefined; arrayAlephFormulaResult?: undefined; };
       } | {
           discoveryContext: {... explore: Explore; connectedTable?: undefined; ...};
           length: number; toString(): string; pop(): any;
           push(...items: Dimension[]): number; ...   <-- an ARRAY-shaped branch
       } | ...
  Two conclusions:
  (a) `Dimension$1` IS the addin-core class (dist/index.d.ts:580
      `declare class Dimension$1 extends SelectableEntity`), so the class-vs-plain-type
      distinction is NOT what the directives suppress.
  (b) The union has branch(es) with NO `dimensions` property, so `dimensions` is
      `Dimension[] | undefined` at the consumer. That `| undefined` is what the
      `@ts-expect-error` swallows.
  (c) NEW SIDE EFFECT OF THIS PR: `discoveryContext.connectedTable` is now typed
      `_getaleph_global_common_utils.Connect | undefined` in addin-core's PUBLIC d.ts —
      a direct consequence of annotating findConnectedTablesInRange with `Connect[]`.
      At runtime it is an addin-core ConnectedTable instance, not a server Connect.
READ /Users/colton/code/monorepo/packages/frontend-common-components/src/components/ActionButtons/DrillDownButton.tsx 50-129
  -> line 60-67: `const { dimensions, ... } = await getDrillDownByOptions();`
     then `setDrillDownByModalState({ ..., dimensions, ... })`
     then line 97 `drillDownDimensions={drillDownByModalState.dimensions}`
READ /Users/colton/code/monorepo/packages/frontend-common-components/src/components/ActionButtons/DrilldownByOptionsModal.tsx 1-80
  -> line 36 `drillDownDimensions.map((elem) => ({ label: elem.prettyName, value: elem.name, ...}))`
     inside a useMemo => an undefined `dimensions` crashes during RENDER (outside the
     try/catch at DrillDownButton.tsx:68).
  -> the modal only reads `.prettyName` and `.name`, both present on the addin-core
     Dimension class (packages/addin-core/src/entities/Dimension.ts:49-50) AND on the
     plain global-common-utils Dimension type. So the change is runtime-neutral but
     type-negative.
READ /Users/colton/code/monorepo/apps/addin-components/src/hooks/useDrillDown.jsx 25-64
  -> getDrillDownByOptions returns `{ dimensions, suggestedDimensions: ... ?? [], discoveryContext }`,
     inheriting the `dimensions?: undefined` union member.
READ /Users/colton/code/monorepo/apps/web-ui/src/pages/Workbooks/Workbook/Workbook.tsx 780-830
  -> handleGetDrillDownOptions does the same thing.
  => CHECK #5 second half: the underlying mismatch is real, but its true cause is
     addin-core's untyped `getDrillDownByOptions` returning an inconsistent union —
     NOT `ServerTable['dimensions']` vs `Dimension[]`. Both consumers now suppress it,
     so the newly-tightened prop contract is satisfied by ZERO call sites.
     => CANDIDATE 3.

--- STEP 12: findConnectedTablesInRange typed as server `Connect` ---
READ /Users/colton/code/monorepo/packages/addin-core/src/operations/buildExplores/index.js 1405-1435 and 1720-1760.
  -> :1420 `const connectedTables = getConnectedTables();`
     :1421 `await findConnectedTablesInRange(connectedTables, activeSheet, range)`
     :1729/:1731 the same in getDrillDownByOptions.
CMD: `git grep -n "getConnectedTables"`
  -> apps/addin-components/src/hooks/useDrillDown.jsx:41 supplies
     `freshConnectedTables?.filter(...)` which comes from
     `ConnectsOperations.getConnectedTables(...)` (useConnectedTables.jsx:214) i.e.
     addin-core ConnectedTable ENTITIES.
READ /Users/colton/code/monorepo/packages/addin-core/src/entities/ConnectedTable.ts 1-80
  -> ConnectedTableOptions has `sheet: Sheet`, `table?: Table` (addin-core class),
     `rows: Record<string, ConnectParam>` where `ConnectParam.dimension?: Dimension`,
     and NO `_id`, NO `dataTable` required, etc.
  -> server `Connect` (global-common-utils/src/connectUtils.ts) REQUIRES
     `_id: string`, `dataTable: string`, `sheetId: string`, `table: ServerTable`,
     `rows: Record<string, { dimension: ConnectDimension; id; range }>`.
  => ConnectedTable is NOT assignable to Connect. The same array element is then
     handed to `getProcessedConnectedTableForDrillDown(connectedTable: ConnectedTable)`
     (utils.ts:743) in the SAME file. Two mutually exclusive types for one value.
     Corroborating evidence in the PR itself: the test had to write
     `as unknown as Connect` twice
     (packages/addin-core/tests/operations/buildExplores/findConnectedTablesInRange.test.ts:1263, 1272
     in diff coordinates), which CLAUDE.md explicitly discourages.
     => CANDIDATE 2.

--- STEP 13: the `recording` parameter ---
READ index.js 1790-1810 -> `getTablesAndCleanCellReferences(parsedFormulas.map(...), activeSheet)`
  — only TWO arguments; and index.js:1797 `parseScalarAlephFormulas(activeSheet,
  alephFormulasMap, spreadsheetUtils, forceCacheRefresh)` omits `recording`, so
  `parseAndCleanAlephFormula(activeSheet, formula, undefined)`.
  utils.ts:208 and :315 declare `recording: ExploreRecording` (required); the bodies
  guard with `if (recording)` (utils.ts:272). => the annotation is wrong; should be
  optional. Not caught today only because index.js is JS. => CANDIDATE 4.

--- STEP 14: cellReferences CellValue[] vs string[] ---
CMD: `grep -n "export type CellValue" -A5 packages/global-common-utils/src/types.ts`
  -> line 602 `export type CellValue = string | number;`
  getTablesAndCleanCellReferences returns `cellReferences: CellValue[]` (utils.ts:193).
  dimensionsMatch / getFilterGroupAndVaryingDimensionsForScalarFormulas consume
  `FilledTableAndCellReference { cellReferences: string[] }` (utils.ts:468-471).
  index.js:210-216 is the code in between and does `value?.toLowerCase()` — optional
  chaining does NOT protect against a NUMBER (numbers have no toLowerCase), so a
  numeric cell value there is a TypeError. Pre-existing runtime hazard, but the PR now
  documents it in one type and denies it in the adjacent one. => CANDIDATE 5.

--- STEP 15: getFilterGroup / getRowDimensions / getColumnDimensions annotations ---
READ index.js 555-615.
  -> `dimensionFilterMap[dimensionOption.value] = { value: [filter] }` and
     `filterMap[value] = [filter]` => the real shape is
     `Record<string, Record<string, Filter[]>>`.
  -> index.js:593 getFilterGroup(dimensionFilterMap)  [utils.ts:408 wants
     Record<string, Record<string, FilterGroup['body']>>  — CORRECT]
  -> index.js:601 getRowDimensions(table, dimensionFilterMap) [utils.ts:355 wants
     Record<string, unknown> — correct because it is the widest]
  -> index.js:605 getColumnDimensions(table, dimensionFilterMap) [utils.ts:382 wants
     Record<string, unknown[]> — WRONG: the values are objects, not arrays;
     Record<string, Record<string, Filter[]>> is not assignable to it]
  The body `Object.keys(dimensionFilterMap[value]).length > 1` only makes sense for the
  object-map shape, so the annotation contradicts the body too. => CANDIDATE 6.
DEAD END CHECKED: I suspected `getColumnDimensionsNew`/`getRowDimensionsNew`'s
  `DimensionFilterMap = Record<string, string[]>` was wrong because
  getExploreForTableRange builds `dimToValuesMap` with `new Set([value])`. READ
  index.js 874-930 and found index.js:892-895 converts every value with
  `Array.from(values)` before use. => REFUTED, annotation is correct.

--- STEP 16: THE BIG ONE — gs-addin isAlephFormula ---
READ /Users/colton/code/monorepo/apps/gs-addin/src/client/utils/formulaUtils.ts 1-120.
  line 22 `const ALEPH_FORMULA_REGEX = /ALEPH(.*?)GET(INTEGRATION)?DATA\((.*?)\)/g;`
  line 24-27 `export function isAlephFormula(formula: string) {
                const s = formula.toString();
                return Boolean(ALEPH_FORMULA_REGEX.test(s));
              }`
  line 52 `const arr = Array.from(formulaString.matchAll(ALEPH_FORMULA_REGEX));`  <-- SAME object
  line 88-106 buildAlephFormulasMap loops every cell formula calling isAlephFormula.
CMD: `git grep -n --word-regexp isAlephFormula -- 'apps/gs-addin/**'`
  -> used at formulaUtils.ts:93 (buildAlephFormulasMap) and :908 (findArrayAlephFormula).
EXPERIMENT 1 (node -e), repeated calls with the SAME formula:
    NEW: call0 true (lastIndex=35), call1 false (lastIndex=0), call2 true, call3 false, call4 true
    OLD: true, true, true, true, true
  => confirmed: alternating false negatives.
EXPERIMENT 2 (node -e), interaction with matchAll:
    fresh split nFormulas = 1
    isAlephFormula = true   lastIndex now 35
    split AFTER isAlephFormula nFormulas = 0   <-- matchAll RESPECTS lastIndex
  => confirmed: `String.prototype.matchAll` clones the regex WITH its lastIndex, so
     after isAlephFormula() returns true, splitAlephFormula() on the same string in
     the same tick sees ZERO matches.
READ formulaUtils.ts 196-235 (parseAlephFormulaArraySize: `if (nFormulas !== 1) return null;`)
READ formulaUtils.ts 938-987 (the cache-miss branch of findArrayAlephFormula:
     line 908 `if (!isAlephFormula(formula)) continue;` ... line 951
     `const nRowsColumns = parseAlephFormulaArraySize(formula); if (nRowsColumns !== null) {...}`)
  => the array-formula source is silently skipped on every cache miss.
  Note addin-core's own copy (utils.ts:52-55) kept `Boolean(s.match(...))`, so ONLY the
  gs-addin copy regressed. => CANDIDATE 1 (highest severity, confirmed).

--- STEP 17: lint evidence for the duplicate imports ---
CMD: `cd packages/addin-core && pnpm run lint | grep -E "buildExplores/utils.ts|no-duplicates"`
  -> src/operations/buildExplores/utils.ts:4:46  warning import(no-duplicates)
     src/operations/buildExplores/utils.ts:35:8  warning import(no-duplicates)
     src/operations/buildExplores/utils.ts:38:37 warning import(no-duplicates) '../connects'
     (plus no-accumulating-spread at 274/282, prefer-array-some at 375/931, no-console
      at 759/847). Lint still EXITS 0 (--max-warnings budget). => CANDIDATE 8.

--- STEP 18: apps/server alephFunction.ts ---
READ /Users/colton/code/monorepo/apps/server/src/operations/alephFunction.ts 40-130.
  -> `let parsed: ReturnType<typeof getSingleAlephFormula>;` and the new
     `if (!parsed || !parsed.alephFormula)`. `alephFormula` is `arr[0][0]`, always a
     non-empty string when nFormulas===1, so the new sub-condition is dead code at
     runtime — it exists purely because the ParsedAlephFormula union is not
     discriminated (utils.ts:56-69). The comment two lines below
     ("`parsed` is undefined when: ...") no longer describes the condition it guards.
     Also the comment at line 93 "getSingleAlephFormula only throw in two cases" is now
     stale: utils.ts:171 adds a third `throw new Error('Expected aleph formula to have
     args')`. Both are cosmetic/low; folded into CANDIDATE 5's neighbourhood and not
     surfaced separately (8-finding cap).

--- STEP 19: misc REFUTED items (recorded so nobody re-checks) ---
  - `getOptimizedSheetRangesPairsList` / `getRangeValues` / `getSelectionValues` /
    `getRangesValues`: all widened Sheet -> SheetWithoutActive, i.e. contravariant and
    therefore safe for all existing callers. Confirmed by the passing typechecks.
  - `hasArrayParameters`'s new `throw new Error(...)`: reachable only for a cell with
    >=2 ALEPH calls AND a range arg; trunk threw `TypeError: ... reading 'some'` for the
    exact same input. Behaviourally equivalent, no regression.
  - `connectAreaContainsRange`'s `(x ?? NaN) - 1`: `undefined - 1` is already NaN, so
    identical.
  - `serviceResult[sheet.id]?.[cell]` vs `serviceResult[sheet.id] && serviceResult[...][cell]`:
    equivalent.
  - `getSheetsCached` stores the RESULT not the PROMISE, so it would not dedupe
    concurrent callers — but the first (sequential) loop always populates it before the
    concurrent second phase runs, so no thundering herd in practice. Not surfaced.
  - `recording.data.input.spreadsheet.sheets ?? []` then `idToSheetMap[id].name`:
    if sheets is missing this now throws on `.name` instead of on `.reduce`. Same class
    of crash; the `?? []` just moves it. Not surfaced.
  - `apps/addin-components/src/hooks/useDrillDown.jsx` `catch (e)` -> `catch (error)`:
    no shadowing in scope. Pure lint fix (unicorn/catch-error-name). Fine.
  - `arg.replace(/\$/g,'')` -> `arg.replaceAll('$','')`: equivalent (no regex state
    involved because a string pattern is used).
  - `variableDimensionsCount += isVariableDimension ? 1 : 0` replaces
    `+= isVariableDimension` (boolean coercion): identical arithmetic.

--- STEP 20: cleanliness ---
CMD: `git status --porcelain` -> `?? code-review-notes/` only. Working tree untouched.

================================================================================
5. CANDIDATES
================================================================================

CANDIDATE 1 — CONFIRMED, HIGH
file: apps/gs-addin/src/client/utils/formulaUtils.ts
line: 26
summary: `isAlephFormula` was switched from `String.prototype.match` to
  `RegExp.prototype.test` on the module-level `/g` regex. `.test()` mutates the shared
  regex's `lastIndex`; `.match()` does not. Every second call returns a false negative,
  and `splitAlephFormula`'s `matchAll` on the same regex object inherits the poisoned
  `lastIndex`.
evidence:
  apps/gs-addin/src/client/utils/formulaUtils.ts:22
    const ALEPH_FORMULA_REGEX = /ALEPH(.*?)GET(INTEGRATION)?DATA\((.*?)\)/g;
  apps/gs-addin/src/client/utils/formulaUtils.ts:24-27
    export function isAlephFormula(formula: string) {
      const s = formula.toString();
      return Boolean(ALEPH_FORMULA_REGEX.test(s));   // was: !!s.match(ALEPH_FORMULA_REGEX)
    }
  apps/gs-addin/src/client/utils/formulaUtils.ts:52
    const arr = Array.from(formulaString.matchAll(ALEPH_FORMULA_REGEX));
  apps/gs-addin/src/client/utils/formulaUtils.ts:92-96
    for (const [cellA1, cellFormula] of formulasMap.entries()) {
      if (!isAlephFormula(cellFormula)) {
        console.log(`Ignoring non-Aleph formula ${cellA1} ${cellFormula}`);
        continue;
  apps/gs-addin/src/client/utils/formulaUtils.ts:908 + :951
    if (!isAlephFormula(formula)) { continue; }
    ... const nRowsColumns = parseAlephFormulaArraySize(formula);   // -> null
  Note: addin-core's sibling copy was NOT changed and still uses
  `Boolean(s.match(ALEPH_FORMULA_REGEX))`
  (packages/addin-core/src/operations/buildExplores/utils.ts:52-55).
node repro A (same formula, repeated):  true,false,true,false,true
node repro B: isAlephFormula(f) -> true (lastIndex=35); then
  Array.from(f.matchAll(R)).length -> 0  (fresh: 1)
failure scenario:
  (a) Google Sheets user selects a range containing 2+ ALEPH.GETINTEGRATIONDATA cells
      and clicks Drill down. buildAlephFormulasMap keeps formula #1, drops #2 as
      "non-Aleph", keeps #3, drops #4... The caller compares map size to the number of
      cells and bails, so drill-down reports no data.
  (b) Drill down on an ALEPH array formula whose size is not cached: line 908 says
      "yes, Aleph formula", then parseAlephFormulaArraySize sees nFormulas===0 and
      returns null, so findArrayAlephFormula never returns the source cell and the
      drill-down silently finds nothing.
fix: `return ALEPH_FORMULA_REGEX.test(s)` is only safe on a non-global regex — either
  drop the `/g`, use a separate non-global constant for the predicate, or keep
  `s.match(...)`. Also reset `lastIndex = 0` is a band-aid, not a fix.

CANDIDATE 2 — CONFIRMED (type-level), MEDIUM-HIGH
file: packages/addin-core/src/operations/buildExplores/utils.ts
line: 672
summary: `findConnectedTablesInRange(connectedTables: Connect[], ...)` was annotated
  with the SERVER `Connect` type from @getaleph/global-common-utils, but every
  production caller passes addin-core `ConnectedTable` ENTITY instances, and the very
  same objects are annotated as `ConnectedTable` by
  `getProcessedConnectedTableForDrillDown` 70 lines later in the same file.
evidence:
  utils.ts:672-676  export async function findConnectedTablesInRange(
                      connectedTables: Connect[], activeSheet: SheetWithoutActive, range: Range,)
  utils.ts:743      export async function getProcessedConnectedTableForDrillDown(
                      connectedTable: ConnectedTable, range: Range)
  index.js:1420-1421  const connectedTables = getConnectedTables();
                      const tablesInRange = await findConnectedTablesInRange(connectedTables, ...);
  index.js:877        await getProcessedConnectedTableForDrillDown(connectTable, range);  // same object
  useDrillDown.jsx:41-42 getConnectedTables: () => freshConnectedTables?.filter(...)  <- from
                      ConnectsOperations.getConnectedTables (addin-core entities)
  Connect requires `_id: string; dataTable: string; sheetId: string; table: ServerTable;
    rows: Record<string, { dimension: ConnectDimension; id: string; range: string }>`
    (packages/global-common-utils/src/connectUtils.ts:44-58)
  ConnectedTable has `table?: Table` (addin-core class), `rows: Record<string, ConnectParam>`
    with `dimension?: Dimension`, and no `_id`
    (packages/addin-core/src/entities/ConnectedTable.ts:57-79)
  The PR's own test needed `as unknown as Connect` twice
    (packages/addin-core/tests/operations/buildExplores/findConnectedTablesInRange.test.ts)
  Public API leak: packages/addin-core/dist/index.d.ts:4356
    connectedTable: _getaleph_global_common_utils.Connect | undefined;
failure scenario: a TS consumer trusts `getDrillDownByOptions().discoveryContext
  .connectedTable` to be a server `Connect` and reads `connectedTable._id` or
  `connectedTable.table.dimensionGroups` -> undefined at runtime (the real object is a
  ConnectedTable whose `.table` is an addin-core Table). Equally, converting
  buildExplores/index.js to TS will now hard-error on the
  findConnectedTablesInRange(connectedTables, ...) call.

CANDIDATE 3 — CONFIRMED, MEDIUM
file: packages/frontend-common-components/src/components/ActionButtons/DrillDownButton.tsx
line: 13
summary: Retyping `getDrillDownByOptions`'s `dimensions` from `ServerTable['dimensions']`
  to addin-core `Dimension[]` broke BOTH consumers, and the fix applied was a
  `@ts-expect-error` at each call site rather than a correct prop type — so the shared
  contract is now satisfied by zero callers, and the directives blanket-suppress a real
  `dimensions: undefined` possibility that crashes the modal during render.
evidence:
  DrillDownButton.tsx:12-16   getDrillDownByOptions: () => Promise<{ dimensions: Dimension[]; ... }>
  apps/addin-components/src/components/ActionButtons/ActionButtonsWrapper.tsx:125-126
      // @ts-expect-error TYPETODO: useDrilldown needs to be typed
      getDrillDownByOptions={getDrillDownByOptions}
  apps/web-ui/src/pages/Workbooks/Workbook/Workbook.tsx:1011-1012
      // @ts-expect-error TYPETODO: getDrillDownByOptions is not typed yet
      getDrillDownByOptions={handleGetDrillDownOptions}
  packages/addin-core/dist/index.d.ts:4348-4370 — the return union has a branch with NO
      `dimensions` property, so `dimensions` is `Dimension[] | undefined` at both sites.
  DrillDownButton.tsx:60-67 puts it straight into state; :97 passes it to
      drillDownDimensions; DrilldownByOptionsModal.tsx:36 does
      `drillDownDimensions.map((elem) => ({ label: elem.prettyName, ... }))` inside a
      useMemo — i.e. during render, OUTSIDE the try/catch at DrillDownButton.tsx:68.
failure scenario: addin-core's getDrillDownByOptions returns the union branch without
  `dimensions` (e.g. the explore branch); DrillDownButton stores `undefined` and opens
  the modal; `drillDownDimensions.map` throws "Cannot read properties of undefined
  (reading 'map')" during render -> blank panel / error boundary, with the type system
  silenced at both entry points.

CANDIDATE 4 — CONFIRMED, MEDIUM
file: packages/addin-core/src/operations/buildExplores/utils.ts
line: 208
summary: `recording` is declared as a REQUIRED parameter on both
  `getTablesAndCleanCellReferences` and `parseAndCleanAlephFormula`, but real callers
  omit it and the bodies guard `if (recording)`.
evidence:
  utils.ts:205-209   export async function getTablesAndCleanCellReferences(
                       alephFormulas: AlephFormulaArgPiece[][], activeSheet: Sheet,
                       recording: ExploreRecording,): Promise<TableAndCellReference[]>
  utils.ts:272       if (recording) {
  utils.ts:312-316   export async function parseAndCleanAlephFormula(
                       activeSheet: Sheet, formula: string, recording: ExploreRecording,)
  index.js:1798-1801 const tableNamesAndCellReferences = await getTablesAndCleanCellReferences(
                       parsedFormulas.map((e) => e.alephFormulaArgs), activeSheet,);   // 2 args
  index.js:1796-1797 const parsedFormulas = await parseScalarAlephFormulas(
                       activeSheet, alephFormulasMap, spreadsheetUtils, forceCacheRefresh,);
                     // recording omitted -> parseAndCleanAlephFormula(..., undefined)
failure scenario: converting buildExplores/index.js to TS immediately fails with
  "Expected 3 arguments, but got 2". Meanwhile the declaration tells every reader that
  `recording` is guaranteed, which is false — anyone who removes the `if (recording)`
  guard on the strength of the type will crash the getDrillDownByOptions path with
  "Cannot read properties of undefined (reading 'data')".

CANDIDATE 5 — CONFIRMED (type-level), MEDIUM
file: packages/addin-core/src/operations/buildExplores/utils.ts
line: 193
summary: The producer type says cell references can be numbers
  (`cellReferences: CellValue[]`, CellValue = string | number) while the two consumers
  in the same file declare `cellReferences: string[]`. The code sitting between them
  (index.js) does `value?.toLowerCase()`, which throws on a number.
evidence:
  utils.ts:191-194  type TableAndCellReference = { table: string; cellReferences: CellValue[] };
  utils.ts:468-471  type FilledTableAndCellReference = { table: Table; cellReferences: string[] };
  utils.ts:589      export function dimensionsMatch(tables: FilledTableAndCellReference[])
  utils.ts:478-480  export function getFilterGroupAndVaryingDimensionsForScalarFormulas(
                      formulasWithPureDimensions: FilledTableAndCellReference[],)
  packages/global-common-utils/src/types.ts:602  export type CellValue = string | number;
  index.js:210-216  const filteredCellReferences = table.cellReferences.filter((value, i, arr) => {
                      if ((i % 2 === 0 && value?.toLowerCase() === 'measure') ||
                          (i % 2 !== 0 && arr[i - 1]?.toLowerCase() === 'measure')) {
  utils.ts:79-83 even documents the cause: "Excel spreadsheet service returns numbers as
  numbers instead of strings".
failure scenario: an ALEPH formula whose dimension-value argument references a cell
  holding a number (`=ALEPH.GETINTEGRATIONDATA(A1, B1, C1)` with C1 = 2024) in Excel.
  getTablesAndCleanCellReferences yields `2024` (a number); index.js:213 evaluates
  `arr[i-1]?.toLowerCase()` on it -> "TypeError: arr[i-1].toLowerCase is not a
  function", aborting the drill-down. The `string[]` annotation asserts this cannot
  happen, so the hazard is now invisible to the next maintainer.

CANDIDATE 6 — CONFIRMED (type-level), MEDIUM-LOW
file: packages/addin-core/src/operations/buildExplores/utils.ts
line: 382
summary: `getColumnDimensions(table, dimensionFilterMap: Record<string, unknown[]>)` is
  the wrong shape: the sole caller passes the same
  `Record<string, Record<string, Filter[]>>` object it passes to `getFilterGroup`
  (typed `Record<string, Record<string, FilterGroup['body']>>`) and `getRowDimensions`
  (typed `Record<string, unknown>`). Three incompatible annotations for one value.
evidence:
  utils.ts:382  export function getColumnDimensions(table: Table, dimensionFilterMap: Record<string, unknown[]>)
  utils.ts:386  const isVariableDimension = Object.keys(dimensionFilterMap[value]).length > 1;
                // Object.keys on a map, not an array — the body contradicts the type
  index.js:570  dimensionFilterMap[dimensionOption.value] = { value: [filter] };
  index.js:587  dimensionFilterMap[dimensionOption.value] = filterMap;   // Record<string, Filter[]>
  index.js:593  const filterGroup = getFilterGroup(dimensionFilterMap);
  index.js:601  rowDimensions = getRowDimensions(table, dimensionFilterMap);
  index.js:605  const columnDimensions = getColumnDimensions(table, dimensionFilterMap);
failure scenario: converting buildExplores/index.js to TS fails at index.js:605 with
  "Type 'Record<string, Filter[]>' is not assignable to type 'unknown[]'". Someone
  reading the signature would also reasonably rewrite the body to `values.length > 1`
  (as getColumnDimensionsNew does), which returns false for every dimension and makes
  the drill-down produce no column dimensions.

CANDIDATE 7 — CONFIRMED, LOW-MEDIUM
file: packages/addin-core/src/operations/buildExplores/utils.ts
line: 242
summary: The new "sheet not found" error is thrown on a path the code itself documents
  as the normal fallback, but `new AlephError(msg)` uses the default
  `{ shouldLog: true }` and therefore console.errors every time. Combined with the fact
  that `splitCellAndSheet` never strips the quotes Excel/Sheets put around sheet names
  containing spaces, this fires for every `'My Sheet'!A1` reference.
evidence:
  utils.ts:239-243
    const referencedSheet = (await getSheetsCached()).find((s) => s.name === sheetName);
    if (!referencedSheet) {
      // Let the caller fall back to refreshing the formula and reading the cache.
      throw new AlephError(`Could not find sheet "${sheetName}" referenced in an Aleph formula`);
    }
  packages/frontend-common-utils/src/error/error.ts:139-158
    constructor(message: string, options: AlephErrorOptions = { shouldLog: true, isNetworkError: false }, ...)
    ... if (options.shouldLog) { console.error(this); }
  The catch that makes the comment true is at
  packages/addin-core/src/operations/buildExplores/index.js:975
    } catch (error) { console.log('Something failed with the aleph formula parsing and cleaning: ', error); }
  Related BEHAVIOR CHANGE in the same hunk: trunk had
  `internalSheet = getSheets().find(...)` while `getSheets(): Promise<Sheet[]>`
  (packages/addin-core/src/data-access/index.ts:59), i.e. on trunk EVERY cross-sheet
  reference threw `TypeError: getSheets(...).find is not a function` and fell back to
  the server refresh. This PR makes unquoted cross-sheet refs resolve locally for the
  first time — the only proof is the two integration-fixture additions of
  `"Sheet4": { "A1": [["Sage Intacct (Native) | Profit and Loss"]] }`; there is no unit
  test for it.
failure scenario: a customer drills down on
  `=ALEPH.GETINTEGRATIONDATA('Q3 Model'!A1, "Account", "Revenue")`. The sheet name in
  the formula is `'Q3 Model'` (quoted) but `sheet.name` is `Q3 Model`, so the lookup
  fails and an AlephError is constructed -> console.error noise on a perfectly normal
  code path, plus a `shouldLog` error report for an expected fallback. Should be
  `{ shouldLog: false }` (as splitAlephFormula:114-118 does for its own expected
  parse failure) or, better, not an error at all.

CANDIDATE 8 — CONFIRMED, LOW
file: packages/addin-core/src/operations/buildExplores/utils.ts
line: 5
summary: The merge of newUtils.ts into utils.ts left three sets of duplicate import
  statements (verified by lint), and the corresponding test file was never renamed, so
  the module now has two test files with mismatched names.
evidence:
  `pnpm run lint` in packages/addin-core reports:
    src/operations/buildExplores/utils.ts:4:46  warning import(no-duplicates)
    src/operations/buildExplores/utils.ts:35:8  warning import(no-duplicates)
    src/operations/buildExplores/utils.ts:38:37 warning import(no-duplicates) Module '../connects' is imported more than once
  utils.ts:4-5   import { appendAndFilterToFilterGroup } from '@getaleph/global-common-utils';
                 import { CellValue, Connect, Sheet, SheetWithoutActive } from '@getaleph/global-common-utils';
  utils.ts:30-36 import { AddinCoreExploreFilter, ... } from '../../transformers';
                 import { SpreadsheetInput } from '../../transformers';
  utils.ts:38-39 import { parseConnectMeasure } from '../connects';
                 import { getConnectWithModifiedDimensionRanges } from '../connects';
  packages/addin-core/tests/operations/buildExplores/newUtils.test.ts:6 now imports
    from '../../../src/operations/buildExplores/utils' but line 29 still says
    `describe('newUtils', ...)` and the file sits next to an existing
    packages/addin-core/tests/operations/buildExplores/utils.test.js.
failure scenario: no runtime failure. It burns the package's `--max-warnings` budget
  (lint currently passes only because of the allowance) and the next person looking for
  tests of `getFilterGroupNew` will grep for utils.test and miss newUtils.test.ts.

================================================================================
APPENDIX — commands run, in order
================================================================================
1.  ls -la code-review-notes/ ; wc -l code-review-notes/00-review.diff
2.  git log --oneline trunk..HEAD ; git diff trunk...HEAD --stat
3.  Read 00-review.diff (1-1250, 1250-1536)
4.  Read packages/addin-core/src/operations/buildExplores/utils.ts (whole)
5.  grep -rn 'getTablesAndCleanCellReferences|parseAndCleanAlephFormula' apps packages
6.  grep -n 'export const getSheets' -A20 packages/addin-core/src/data-access/index.ts
7.  git show trunk:packages/addin-core/src/operations/buildExplores/utils.js | sed -n 160,240p
8.  git show trunk:packages/addin-core/src/data-access/index.ts | grep -n getSheets
9.  sed -n 150,230p / 930,1060p / 1440,1600p / 1760,1860p / 865,930p / 555,615p /
    1405,1435p / 1720,1760p  packages/addin-core/src/operations/buildExplores/index.js
10. grep -rn 'class AlephError' -A40 packages/frontend-common-utils/src/
11. cat packages/frontend-common-components/package.json ; cat packages/workbook-utils/package.json
12. grep -rn newUtils --include={ts,tsx,js,jsx} apps packages   (only the describe() hit)
13. node -e on addin-core/package.json and global-common-utils/package.json  (cycle check)
14. grep 'export type ServerTable' -A45 global-common-utils/src/types.ts ; sed -n 1,14p
    workbook-utils/src/useWorkbookTables.ts
15. grep 'export type Sheet\b' -B3 -A12 global-common-utils/src/types.ts ;
    git grep SheetRangesPairsList
16. pnpm turbo run types:check --filter=addin-core,frontend-common-components,
    workbook-utils,global-common-utils,workbook-automation-service   -> 13/13 PASS
17. grep isActive in the four spreadsheet-service implementations   -> nobody reads it
18. for s in <20 symbols>; do git grep -n --word-regexp "$s" -- apps/** packages/**; done
19. Read apps/gs-addin/src/client/utils/formulaUtils.ts 1-120, 196-235, 880-935, 938-987
20. git grep --word-regexp isAlephFormula -- apps/gs-addin/**
21. node -e  (regex lastIndex experiment #1: alternating true/false)
22. pnpm turbo run types:check --filter=web-ui,addin-components,gs-addin  -> 12/12 PASS
23. sed -n 40,130p apps/server/src/operations/alephFunction.ts
24. sed -n 40,90p / 265,300p packages/frontend-common-components/src/components/Reports/CustomDataTableChart.tsx
25. sed -n 1,120p packages/addin-core/src/entities/Table.ts ; git grep dimensionGroups
26. sed -n 85,115p global-common-utils/src/types.ts ; sed -n 40,120p apps/server/src/mappers/tables.js
27. sed -n 25,60p apps/addin-components/src/api/tables.ts
28. git grep 'getDrillDownByOptions|DrillDownButton|<ActionButtons'
29. Read packages/frontend-common-components/src/components/ActionButtons/DrilldownByOptionsModal.tsx 1-80
30. sed -n 780,830p apps/web-ui/src/pages/Workbooks/Workbook/Workbook.tsx
31. grep 'prettyName|clone|class Dimension' packages/addin-core/src/entities/Dimension.ts
32. cat packages/workbook-utils/src/addin-core.d.ts   (a `declare module` shim, all `any`)
33. grep -rln '"@getaleph/frontend-common-components"' --include=package.json
34. git grep Connect / ConnectDimension / ConnectSettings / ConnectRowSettings / ConnectDirection
35. sed -n 1,80p packages/addin-core/src/entities/ConnectedTable.ts
36. git grep sheetRangesValues ; grep 'export type CellValue' -A5 global-common-utils/src/types.ts
37. sed -n 120,200p packages/addin-core/src/operations/SpreadsheetUtils.js
38. node -e  (regex lastIndex experiment #2: matchAll respects lastIndex -> nFormulas 0)
39. cd packages/addin-core && pnpm run lint | grep 'buildExplores/utils.ts|no-duplicates'
40. git grep parseAlephFormulaArraySize -- apps/gs-addin/**
41. Read packages/frontend-common-components/src/components/ActionButtons/DrillDownButton.tsx 50-129
42. Read apps/addin-components/src/hooks/useDrillDown.jsx 25-64 ;
    sed -n 110,135p apps/addin-components/src/components/ActionButtons/ActionButtonsWrapper.tsx
43. grep getDrillDownByOptions -A30 packages/addin-core/dist/index.d.ts (lines 4348-4378)
44. git status --porcelain   -> clean except untracked code-review-notes/

BLOCKED / NOT DONE
- Stripping the two @ts-expect-error directives and re-running tsc to capture the exact
  TS error text: the permission classifier denied the in-tree edit (correctly — 9 other
  agents share this checkout). I did not work around it. Substituted static evidence
  from packages/addin-core/dist/index.d.ts:4348-4378, which shows the union member that
  lacks `dimensions` and thus the `| undefined` being suppressed.
