================================================================================
1. PROMPT RECEIVED (verbatim)
================================================================================
You are finder angle "REUSE" — one of 10 parallel finder agents in a maximum-effort code review. Your job is CLEANUP, specifically: flag new code in the diff that RE-IMPLEMENTS something the codebase already has, and name the existing helper/type to call instead.

REVIEW SCOPE: the diff of git branch `colton/typ-797-convert-packagesaddin-coresrcoperationsbuildexploresutilsjs` vs `trunk`, in repo /Users/colton/code/monorepo.
- Full unified diff is saved at: /Users/colton/code/monorepo/code-review-notes/00-review.diff  (READ THIS FIRST, in full)
- Regenerate with: `git diff trunk...HEAD` run from /Users/colton/code/monorepo
- What the PR does: converts packages/addin-core/src/operations/buildExplores/utils.js to TypeScript, merges the deleted newUtils.ts into utils.ts, moves the `Connect` type from packages/workbook-utils to packages/global-common-utils/src/connectUtils.ts, adds `SheetWithoutActive`, and touches apps/gs-addin formulaUtils.ts, apps/server alephFunction.ts, addin-components, frontend-common-components.

The repo CLAUDE.md rule you are enforcing: "Before writing a helper, hook, type, or component, search for an existing one — it usually exists... Genuinely new, broadly useful helpers go in a shared location, not the file you happen to be editing." and "If the same logic appears twice in your diff, extract a helper."

Concrete hypotheses to investigate (confirm or refute each, with file:line evidence):
1. `packages/global-common-utils/src/connectUtils.ts` newly declares `ConnectDimension`, `ConnectSettings`, `ConnectRowSettings`, `specialConnectFilter`. Grep the whole repo (excluding node_modules) for pre-existing definitions of these same shapes — check at least `packages/ui-components/src/**/Connect*`, `packages/addin-core/src/entities/ConnectedTable.ts`, `packages/addin-core/src/entities/Dimension.ts`, `packages/global-common-utils/src/types.ts` (`ServerDimension`?). Report every duplicate definition of the same concept that now exists in N places.
2. `SheetWithoutActive = Omit<Sheet, 'isActive'> & { isActive?: boolean }` in global-common-utils/src/types.ts — is there already a partial-sheet type in the repo (e.g. `SheetRef`, `{ id, name }` inline shapes, `PartialSheet`)? Grep for `isActive` and for inline `{ id: string; name: string }` sheet params.
3. The diff writes `return narrowedMatches.filter(Boolean)` — does the repo already have a nullability helper (grep `isNotNullish`, `isDefined`, `notEmpty`, `filterNullish` in packages/global-common-utils and packages/*/src/utils)? Name it and its path.
4. `getRowDimensions` vs `getRowDimensionsNew`, and `getColumnDimensions` vs `getColumnDimensionsNew`, and `getFilterGroup` vs `getFilterGroupNew` now all live in ONE file (packages/addin-core/src/operations/buildExplores/utils.ts) after the merge. Read all six and report the duplicated logic and whether the `New` variants supersede the old ones (grep callers of each to see if the old ones are still used). Also check whether the JSDoc "Copied directly from addin-core" / "Copied directly from addin-components" comments point at yet another copy still living in apps/addin-components — grep apps/addin-components for `getRowDimensions`, `getColumnDimensions`, `getFilterGroup`.
5. `utils.ts` has `type ParsedAlephFormulaWithoutArgs` / `WithArgs`, and the same file plus apps/gs-addin/src/client/utils/formulaUtils.ts and apps/server/src/operations/alephFunction.ts all parse Aleph formulas with near-identical regexes (`ALEPH_FORMULA_REGEX`, `ARRAY_ARGUMENT_REGEX`, `splitAlephFormula`, `isAlephFormula`). Quantify the duplication: how many copies of `ALEPH_FORMULA_REGEX` / `isAlephFormula` / `splitAlephFormula` exist in the repo, and does one already live in a shared package that the others could import?
6. The three duplicate import statements in utils.ts (two from `@getaleph/global-common-utils`, two from `../../transformers`, two from `../connects`) — confirm with `pnpm run lint` in packages/addin-core if cheap (time-box 4 min; use pnpm only, never npx; note that `@getaleph/global-common-utils` may need building first per CLAUDE.md).
7. `ExploreRecording` type declared locally in utils.ts — is there already a recording type (grep `drillDownRecording`, `recording`, `ExploreRecording`, `Recording` in packages/addin-core and apps/addin-components) that should have been imported instead?

MANDATORY NOTE-TAKING (explicit user request): You MUST write a detailed plain-text notes file at
/Users/colton/code/monorepo/code-review-notes/06-reuse.txt
using the Write tool, BEFORE returning your final answer. It MUST contain, in this order:
1. "PROMPT RECEIVED" — the verbatim full text of this prompt.
2. "WHAT I AM LOOKING FOR"
3. "GOALS"
4. "METHOD / HOW I ACCOMPLISHED IT" — chronological log: every command run, every file read (absolute path + line ranges), what you saw, what you concluded. Include dead ends and refuted hypotheses.
5. "CANDIDATES" — file, line, summary, failure scenario, quoted code evidence.

OUTPUT: return a JSON array of up to 8 candidate findings, each {"file","line","summary","failure_scenario"}. For this angle, `failure_scenario` must state the CONCRETE COST (what is duplicated, which existing helper should be used and where it lives, what will drift) rather than a crash. RECALL mode — err on the side of surfacing. If you truly find nothing, return [].


================================================================================
2. WHAT I AM LOOKING FOR
================================================================================
New code in this branch's diff that RE-IMPLEMENTS something the monorepo already
has. Specifically:

- New TYPE declarations that duplicate an existing exported type (same concept,
  same or weaker shape) — especially where the PR "moves a type to a shared
  package" but leaves sibling copies behind, or introduces a name that now
  collides with an existing export of the same name in another package.
- New FUNCTIONS / regexes / guards that duplicate an existing helper.
- Logic that appears TWICE inside the diff itself (CLAUDE.md: "If the same logic
  appears twice in your diff, extract a helper").
- Hand-written inline shapes that should be `Pick<>`/`Omit<>`/an import of an
  existing named type.
- Anything where the diff is visibly HAND-SYNCING two forked copies of the same
  code (strongest possible evidence of the drift cost).
- Duplicate import statements from the same module (a mechanical "reuse" smell
  the repo's own linter flags).

For each, I need the *existing* helper/type's absolute path + line, so the
finding is actionable ("call X in Y instead").


================================================================================
3. GOALS
================================================================================
G1. Read the whole diff (1536 lines) before judging anything.
G2. Confirm or refute each of the 7 supplied hypotheses with file:line evidence.
G3. Quantify duplication (how many copies of each concept now exist, and where).
G4. For every confirmed duplicate, name the canonical/pre-existing definition.
G5. Run the repo linter on packages/addin-core to mechanically confirm the
    duplicate-import hypothesis (time-boxed, pnpm only).
G6. Empirically verify the `filter(Boolean)` claim with the repo's own tsc rather
    than relying on memory of TS version behaviour.
G7. Produce <=8 ranked candidates, each with a CONCRETE COST failure_scenario.


================================================================================
4. METHOD / HOW I ACCOMPLISHED IT  (chronological)
================================================================================

--- STEP 1: read the diff -------------------------------------------------------
READ /Users/colton/code/monorepo/code-review-notes/00-review.diff lines 1-1250
     (paginated; tool capped at 25k tokens)
READ /Users/colton/code/monorepo/code-review-notes/00-review.diff lines 1251-1536
RAN  `git diff trunk...HEAD --stat`

Saw: 20 files changed, +489/-366.
Key structural facts extracted from the diff:
  * packages/addin-core/src/operations/buildExplores/newUtils.ts DELETED (155
    lines) and its 4 exports (getFilterGroupNew, getColumnDimensionsNew,
    getRowDimensionsNew, processConnectMeasures) appended verbatim to the end of
    the renamed utils.js -> utils.ts.
  * packages/workbook-utils/src/connect.ts gutted from 55 lines of type decls to
    a 7-line `export type { ... } from '@getaleph/global-common-utils'`.
  * packages/global-common-utils/src/connectUtils.ts NEW (61 lines) receiving
    ConnectDirection / ConnectSettings / ConnectRowSettings / ConnectDimension /
    Connect. NOTE: ConnectSettings and ConnectRowSettings each GAINED a new
    inline `specialConnectFilter?: { isOn?: boolean }` member that did NOT exist
    in the workbook-utils original — i.e. this is genuinely NEW type authoring,
    not a pure move.
  * global-common-utils/src/types.ts: `Sheet` unchanged, `SheetWithoutActive`
    added, `DimensionGroup` gained isHyperlink/customCalculatedItems/
    createdAt/updatedAt.
  * utils.ts now has THREE pairs of duplicate import statements (lines 4/5,
    30-35/36, 38/39).
  * The diff EDITS apps/gs-addin/src/client/utils/formulaUtils.ts `isAlephFormula`
    to `Boolean(ALEPH_FORMULA_REGEX.test(s))` while the addin-core twin keeps
    `Boolean(s.match(ALEPH_FORMULA_REGEX))`, and hand-edits gs-addin's
    `argRowsColumns` from `.replace(/\$/g,'')` to `.replaceAll('$','')`.
  * findConnectedTablesInRange.test.ts had to add `as unknown as Connect` casts
    in TWO places to keep compiling against the new `Connect`.

--- STEP 2: hypothesis 1 — Connect* type duplication ----------------------------
RAN  grep -rn "ConnectDimension"    (--include ts/tsx/js/jsx, minus node_modules/dist)
RAN  grep -rn "ConnectSettings\b" ; grep -rn "ConnectRowSettings"
Found the following pre-existing definitions of the same concepts:
  * packages/addin-core/src/entities/ConnectedTable.ts:14  `type ConnectSettings`
  * packages/addin-core/src/entities/ConnectedTable.ts:20  `export type SpecialConnectFilter`
  * packages/addin-core/src/entities/ConnectedTable.ts:22  `export type ConnectRowSettings`
  * packages/addin-core/src/entities/ConnectedTable.ts:33  `export type ConnectColumnSettings`
  * packages/ui-components/src/components/Connect/types.ts:1  `ConnectDimension` (different shape, SAME NAME)
  * packages/ui-components/src/components/Connect/types.ts:12 `ConnectDirection`
  * packages/ui-components/src/components/Connect/types.ts:14 `ConnectSettings`
  * packages/ui-components/src/components/Connect/types.ts:77 `ConnectDirectionSettings` (has the full specialConnectFilter)
  * packages/frontend-common-components/src/connects/connectTypes.ts:9  `ConnectSettings`
  * packages/frontend-common-components/src/connects/connectTypes.ts:27 `AddinCoreDimensionConnectSpecific` (near-identical to new ConnectDimension)
  * packages/frontend-common-components/src/connects/connectTypes.ts:64 `AddinCoreConnectTable` (near-identical to new Connect)
  * packages/frontend-common-components/src/components/AutomationsBuilder/AutomationActionModal.tsx:109 `ConnectDirection`
  * packages/workbook-utils/src/utils.ts:59 `WorkbookConnectSettings`

READ /Users/colton/code/monorepo/packages/addin-core/src/entities/ConnectedTable.ts (full, 313 lines)
READ /Users/colton/code/monorepo/packages/global-common-utils/src/connectUtils.ts (full, 61 lines)
READ /Users/colton/code/monorepo/packages/ui-components/src/components/Connect/types.ts (full, 90 lines)
READ /Users/colton/code/monorepo/packages/frontend-common-components/src/connects/connectTypes.ts (full, 188 lines)

CONCLUSION A (CONFIRMED, strong): the new `specialConnectFilter?: { isOn?: boolean }`
appears TWICE inside the new 61-line file (connectUtils.ts:9-11 and :17-19) and is
a weaker re-invention of the already-named
`packages/addin-core/src/entities/ConnectedTable.ts:20  export type SpecialConnectFilter = { id?: string; range: string; isOn: boolean }`
which is used at ConnectedTable.ts:30,40 and robustConnects.ts:573,584,806, and
whose full shape also exists inline at ui-components/Connect/types.ts:85-89.

CONCLUSION B (CONFIRMED): `ConnectRowSettings` / `ConnectSettings` are now exported
under the SAME NAMES from two packages with DIFFERENT shapes
(global-common-utils/src/connectUtils.ts:5,14 — `pull: { type: string }` —
vs addin-core/src/entities/ConnectedTable.ts:14,22 — `pull: { type: 'default'|'star'|'smartInsert' }`).
`packages/addin-core/src/operations/robustConnects.ts` imports from BOTH modules
(from '@getaleph/global-common-utils' at lines 2-11, and ConnectRowSettings/
ConnectColumnSettings/SpecialConnectFilter from '../entities/ConnectedTable' at
lines 17-22). Confirmed by:
RAN  sed -n '1,45p' packages/addin-core/src/operations/robustConnects.ts

CONCLUSION C (CONFIRMED, best single find): `packages/workbook-utils/src/utils.ts:30-53`
holds a BYTE-IDENTICAL local `type ConnectDimension`, plus
:57 `WorkbookConnectDirection`, :59 `WorkbookConnectSettings`, :65 `WorkbookConnect`
(with :74 `sheet: { id: string; name: string }`) — i.e. an exact second copy of
everything this PR just relocated, sitting in the SAME PACKAGE whose connect.ts
the PR reduced to a re-export.
READ /Users/colton/code/monorepo/packages/workbook-utils/src/utils.ts lines 1-45 and 40-99
RAN  cat packages/workbook-utils/src/connect.ts   (now a 7-line re-export)

RAN  grep -rn "export type ServerTable" -> only global-common-utils/src/types.ts:153.
     workbook-utils/src/useWorkbookTables.ts:10 merely RE-EXPORTS it. So the
     `Connect['table']: ServerTable` retarget is NOT a duplication problem.
READ /Users/colton/code/monorepo/packages/global-common-utils/src/types.ts lines 60-219
     -> ServerTable:153 already has `dimensionGroups?: DimensionGroup[]` (line 172),
     so the new `Table.dimensionGroups?: DimensionGroup[]` in the diff correctly
     REUSES the shared `DimensionGroup`. REFUTED as a duplication finding — good.
     There is no `ServerDimension`; the dimension type is `Dimension` (types.ts ~line 40-88).

RAN  grep -rn "export type ConnectDirection" -> 4 copies:
     ui-components/Connect/types.ts:12,
     frontend-common-components/.../AutomationActionModal.tsx:109,
     global-common-utils/src/connectUtils.ts:3,
     (+ workbook-utils/src/utils.ts:57 as WorkbookConnectDirection).

--- STEP 3: hypothesis 2 — SheetWithoutActive -----------------------------------
RAN  grep -rn "Pick<Sheet|Partial<Sheet>|Omit<Sheet"
     -> only ConnectedTable.ts:247 `Partial<Sheet>`, googleSpreadsheetUtils.ts:397
        `Pick<Sheet,'id'>`, and the new types.ts:374.
RAN  grep -rn "SheetRef|SheetInfo|SheetLike|PartialSheet|SheetIdAndName"  -> ZERO hits.
RAN  grep -rn "^type Sheet = {|^export type Sheet = {"
     -> 10 local redeclarations, incl.
        packages/frontend-common-components/src/connects/connectTypes.ts:3,
        apps/gs-addin/src/client/utils/formulaUtils.ts:9  (a file THIS DIFF touches,
        which already imports from '@getaleph/global-common-utils' on lines 3-7),
        apps/gs-addin/src/server/SpreadsheetServiceNew.ts:107,
        apps/workbook-automation-service/front/src/Workbook.tsx:44, ...
RAN  grep -rn "sheet: { id: string; name: string }"
     -> workbook-utils/src/utils.ts:74, workbook-automation-service/front/src/api.ts:57,
        gs-addin/src/client/services/ApiSpreadsheetService.ts:1024 and :1091.

CONCLUSION (hypothesis 2 REFUTED as authored-duplicate): there was NO pre-existing
named partial-sheet type, and `SheetWithoutActive` was correctly placed in the
shared package. It is a genuinely new, correctly-located helper type. I am NOT
reporting it as a duplicate. (Secondary observation kept for the record: the 4
inline `{ id: string; name: string }` sheet shapes above are now the ones that
should adopt it — but that is pre-existing code, not new code in the diff, so I
downgraded it and did not spend a finding slot on it.)

--- STEP 4: hypothesis 3 — nullability helper -----------------------------------
RAN  grep -rn "isNotNullish|isDefined\b|notEmpty|filterNullish|isNonNullable|isTruthy|isNotNil"
     -> HIT: packages/global-common-utils/src/nullabilityUtils.ts:12
              `export const isNotNullish = <T>(value: T|null|undefined): value is T`
        with a JSDoc whose example is literally
              `const ids: string[] = maybeIds.filter(isNotNullish);`
RAN  grep -n "nullability" packages/global-common-utils/src/index.ts
     -> line 28: `export * from './nullabilityUtils';`  (so it IS public API)
RAN  cat packages/global-common-utils/src/nullabilityUtils.ts   (15 lines, read in full)
RAN  grep -rn "\.filter(Boolean)" packages/*/src apps/*/src   (20+ hits, incl. the new one)
RAN  grep -rn "filter(isNotNullish)"  -> apps/server/.../pullQueries.ts:148,
     apps/server/.../filterParser.ts:147

Then I did NOT take TS narrowing on faith. I wrote a scratch probe and ran the
repo's own tsc (5.4.5):
WROTE /private/tmp/claude-501/.../scratchpad/tstest/t.ts
RAN   packages/addin-core/node_modules/.bin/tsc --noEmit --strict ... t.ts
RESULT: `error TS2322: Type '(C | undefined)[]' is not assignable to type 'C[]'`
=> CONFIRMED: `filter(Boolean)` does NOT narrow on this toolchain.
READ /Users/colton/code/monorepo/packages/addin-core/src/operations/buildExplores/utils.ts lines 660-719
     -> :711 `return narrowedMatches.filter(Boolean);` where narrowedMatches is
        `(Connect | undefined)[]`, and the other return at :684 is `Connect[]`, so
        the function's inferred return type leaks `undefined`.
     -> same pattern at :383 `const columnDimensions: (Dimension | undefined)[] = []`
        (an annotation the DIFF newly added) vs :892 `const columnDimensions: Dimension[] = []`
        in the New twin, which does the undefined-drop by hand with an `if`.

--- STEP 5: hypothesis 4 — old vs New dimension helpers -------------------------
RAN  grep -rn "getRowDimensions|getColumnDimensions|getFilterGroup|getVisibleDimensions"
READ /Users/colton/code/monorepo/packages/addin-core/src/operations/buildExplores/utils.ts lines 348-417
READ /Users/colton/code/monorepo/packages/addin-core/src/operations/buildExplores/utils.ts lines 855-934

Callers (all in packages/addin-core/src/operations/buildExplores/index.js):
  OLD still live: getFilterGroup:593, getRowDimensions:277 and :601, getColumnDimensions:605
  NEW live:       getFilterGroupNew:839 and :911, getRowDimensionsNew:847 and :903,
                  getColumnDimensionsNew:861 and :919
=> CONFIRMED: the `New` variants do NOT supersede the old ones; both code paths
   are reachable and now sit in one file.

Body comparison:
  getRowDimensions      utils.ts:355-377   |  getRowDimensionsNew  utils.ts:907-933
  -> 25 lines of identical dimensionGroup-parent-walking. Deltas: `getDimensionByValue`
     vs `getDimensionByAnyMatch`; `filters.forEach(({text}) => ...)` + a `!`
     non-null assert at :357 vs `filters.forEach((filter) => { if (filter) ... })`.
     BOTH end with the identical no-op `const parentDimensionTexts = new Set(parentDimensionTextsSet);`
     (:372 and :927) and the identical final `.filter(({text,value}) => !filters.find(...))`.
     The New copy still carries `// @ts-expect-error Table is yet not fully typed...`
     at :930 even though the diff added `Table.dimensionGroups` and removed the other
     three @ts-expect-errors — the old copy solves the same expression with `!` at :357.
     Two different workarounds for the same line = textbook drift.
  getColumnDimensions   utils.ts:382-392   |  getColumnDimensionsNew utils.ts:891-905
  -> same loop; `Object.keys(dimensionFilterMap[value]).length > 1` vs `values.length > 1`
     (equivalent for arrays), and New adds the undefined-drop. New is literally
     Old + `.filter(isNotNullish)`.
  getFilterGroup (:408) vs getFilterGroupNew (:858) -> genuinely different
     algorithms (FilterGroup entity tree vs appendAndFilterToFilterGroup). NOT a
     duplicate. Refuted for this pair.

RAN  grep -rn "getRowDimensions|getColumnDimensions|getFilterGroup|getProcessedConnectedTableForDrillDown"
     restricted to apps/addin-components/src, apps/web-ui/src, packages/frontend-common-components/src
     -> ZERO hits.
=> The `/** Copied directly from addin-components */` JSDoc at utils.ts:353, :380,
   :403 and above getProcessedConnectedTableForDrillDown (~:740) is STALE; there is
   no such copy any more. (Noted; folded into the duplication finding rather than
   spending a slot.)

--- STEP 6: hypothesis 5 — Aleph formula parsing duplication --------------------
RAN  grep -rn "ALEPH_FORMULA_REGEX" ; "function isAlephFormula|const isAlephFormula" ;
     "splitAlephFormula" ; "ARRAY_ARGUMENT_REGEX|ARRAY_ARGUMENT_STRICT_REGEX"
RAN  grep -rn "RANGE_REGEX|parseAlephFormulaArraySize|hasArrayParameters|RowExtent"
READ /Users/colton/code/monorepo/apps/gs-addin/src/client/utils/formulaUtils.ts lines 1-120
READ /Users/colton/code/monorepo/packages/addin-core/src/operations/buildExplores/index.js lines 1040-1089
READ /Users/colton/code/monorepo/packages/addin-core/src/operations/buildExplores/utils.ts lines 1-60
RAN  grep -n "addin-core|global-common-utils|frontend-common" apps/gs-addin/package.json

Copy census:
  ALEPH_FORMULA_REGEX = /ALEPH(.*?)GET(INTEGRATION)?DATA\((.*?)\)/g   -- IDENTICAL in
      packages/addin-core/src/operations/buildExplores/utils.ts:45
      apps/gs-addin/src/client/utils/formulaUtils.ts:22
    (+ 2 unrelated narrower variants: apps/server/src/constants/index.ts:282,
       apps/gs-addin/src/server/indexNew.ts:136)
  isAlephFormula        -- 2 defs: utils.ts:52, formulaUtils.ts:24
  splitAlephFormula     -- 2 defs: utils.ts:78, formulaUtils.ts:43 (bodies identical
                           down to the `quoteRegex = /"([^"]*)"|,/g` comment)
  buildAlephFormulasMap -- 2 defs: utils.ts:~836, formulaUtils.ts:88
  RANGE_REGEX = /^([A-Z]+)([0-9]+)(?::([A-Z]+)([0-9]+))?$/  -- IDENTICAL in
      packages/addin-core/src/operations/buildExplores/index.js:55
      apps/gs-addin/src/client/utils/formulaUtils.ts:165
  parseAlephFormulaArraySize -- 2 defs: index.js:1047, formulaUtils.ts:200
  apps/gs-addin/src/client/utils/formulaUtils.ts:18 contains the smoking gun:
      `// import { buildAlephFormulasMap, isAlephFormula, splitAlephFormula } from './buildExplores/utils';`
    i.e. a commented-out import of the addin-core original.

Two things this diff does that prove the cost:
  (a) DIVERGES the twins: gs-addin isAlephFormula becomes
      `Boolean(ALEPH_FORMULA_REGEX.test(s))` while addin-core keeps
      `Boolean(s.match(ALEPH_FORMULA_REGEX))`. Since the regex carries /g,
      `.test()` mutates lastIndex and `.match()` does not, so the two copies now
      answer differently for the same input on successive calls.
  (b) HAND-SYNCS the twins: formulaUtils.ts argRowsColumns is edited from
      `.replace(/\$/g,'')` to `.replaceAll('$','')`, which is exactly what
      index.js:1060 already said.
  apps/gs-addin/package.json has NO dependency on @getaleph/addin-core, but DOES
  depend on @getaleph/global-common-utils (line 40) — which is the very package
  this PR is already adding a new module to. So a shared home exists today.
  (apps/server/src/operations/alephFunction.ts turned out to be a genuinely
  different, stricter parser (coerceArg / AlephFunctionParseException) — not a
  third copy. That sub-hypothesis is REFUTED.)

--- STEP 7: hypothesis 6 — duplicate imports (mechanical confirmation) ----------
RAN  ls packages/addin-core ; grep -n '"lint"' packages/addin-core/package.json
     -> `oxlint --config ../../.oxlintrc.json ./src ./tests --max-warnings=520`
        (oxlint, not eslint — no eslint.config in the package)
RAN  grep -n "no-duplicates" .oxlintrc.json  -> line 132: `"no-duplicates": "warn"`
RAN  timeout 250 pnpm -F @getaleph/addin-core run lint   (~40s, no build needed)
RAN  same, filtered to `buildExplores/utils|no-duplicates`
OUTPUT (verbatim):
  src/operations/buildExplores/utils.ts:4:46: warning import(no-duplicates): Modules should not be imported multiple times in the same file
  src/operations/buildExplores/utils.ts:35:8:  warning import(no-duplicates): Modules should not be imported multiple times in the same file
  src/operations/buildExplores/utils.ts:38:37: warning import(no-duplicates): Module '../connects' is imported more than once in this file
  src/operations/buildExplores/utils.ts:274:26: warning oxc(no-accumulating-spread)
  src/operations/buildExplores/utils.ts:282:9:  warning oxc(no-accumulating-spread)
  src/operations/buildExplores/utils.ts:375:16: warning unicorn(prefer-array-some)
  src/operations/buildExplores/utils.ts:931:16: warning unicorn(prefer-array-some)
=> CONFIRMED hypothesis 6: 3 new no-duplicates warnings.
=> BONUS: the SAME rule fires at 375 and 931 — the linter itself is reporting the
   getRowDimensions/getRowDimensionsNew duplication twice.

--- STEP 8: hypothesis 7 — ExploreRecording -------------------------------------
RAN  grep -rn "ExploreRecording|drillDownRecording|Recording" in packages/addin-core/src,
     apps/addin-components/src, packages/global-common-utils/src (first attempt spilled
     into apps/*/build bundles — dead end; re-ran restricted to src/ dirs)
RAN  find for *recording* files
READ /Users/colton/code/monorepo/apps/addin-components/src/hooks/useDrillDown.jsx lines 50-119
     -> :61-77 builds `drillDownRecording = { app, operation: 'buildExplores',
        data: { input: { drillDownOptions, forceCacheRefresh, spreadsheet: { sheets } } } }`
        and :102-103 later sets `.data.input.table` and `.data.output`.
RAN  grep -n "recording|Recording" apps/addin-components/src/components/ActionButtons/ActionButtonsWrapper.tsx
     -> :3 `import { FixtureRecording } from '@getaleph/server-sdk';`
     -> :24 `onDrillDownRecordingChange?: (drillDownRecording: FixtureRecording) => void;`
RAN  grep -n FixtureRecording packages/server-sdk/dist/cjs/generated/v1/index.d.ts
READ that file lines 1718-1737
     -> `FixtureRecording = { name; app; operation: 'runExplore'|'runPulls'|'runPushes'|'buildExplores'; data: {[k:string]: any}; skipReason? }`
        i.e. `data` is untyped, so it is NOT a drop-in.
READ /Users/colton/code/monorepo/packages/addin-core/src/transformers/explores.ts lines 150-219
     -> :155 `SpreadsheetInput` (already imported by utils.ts:36)
     -> :174-202 `ExplorePullFixture` = `{ name; operation; app; data: { input: { ...;
        table; spreadsheet: SpreadsheetInput }; query; sheetOutput } }`
     -> :210 `InputExplorePillFixture` = DeepPartial variant, i.e. "recording under
        construction" — exactly the concept utils.ts:183 re-declares.
CONCLUSION (hypothesis 7 CONFIRMED, medium strength): `ExploreRecording` is a third,
weaker hand-rolled slice of an existing family of recording/fixture types that
already lives in the module utils.ts already imports from. It belongs beside
ExplorePullFixture in transformers/explores.ts (as a `buildExplores` sibling), not
inline in a leaf file. It is not a byte-for-byte duplicate, hence ranked lower.

--- STEP 9: extra scan for "same logic twice in the diff" -----------------------
READ /Users/colton/code/monorepo/packages/addin-core/src/operations/buildExplores/utils.ts lines 125-199
RAN  grep -n "Expected aleph formula to have args" .../utils.ts  -> lines 135 and 171
     Both are `if (!alephFormulaArgs) { throw new Error('Expected aleph formula to
     have args'); }` — the identical guard written twice, purely to appease the new
     non-discriminated `ParsedAlephFormula` union (utils.ts:56-70). Both are
     unreachable at runtime (hasArrayParameters already matched the regex;
     getSingleAlephFormula already returned/threw for nFormulas 0 and >1).
RAN  grep for keyBy/indexBy/arrayToMap/toRecord/groupBy
     -> only packages/global-common-utils/src/groupUtils.ts:19 `groupBy` (Map-returning,
        not a fit for the `idToSheetMap` build at utils.ts:270-276). lodash IS a
        dependency of addin-core (package.json:58). So the spread-in-reduce at :274
        is an efficiency/simplification issue more than a reuse one — left to the
        efficiency angle, not claimed here.
RAN  grep for existing "clone non-hidden dimensions" helper -> none. getVisibleDimensions
     (utils.ts:398) is genuinely new/unique. REFUTED as duplicate.


================================================================================
5. CANDIDATES
================================================================================

--- C1 -------------------------------------------------------------------------
file: packages/global-common-utils/src/connectUtils.ts
line: 22
summary: The `ConnectDimension` / `ConnectDirection` / `ConnectSettings` /
  `Connect` block was moved OUT of packages/workbook-utils/src/connect.ts into
  global-common-utils, but a byte-identical second copy of the same block was
  left behind in the SAME package at packages/workbook-utils/src/utils.ts:30-78.
failure scenario / concrete cost: `packages/workbook-utils/src/utils.ts:30`
  declares its own `type ConnectDimension` with the exact same 24 fields, plus
  `WorkbookConnectDirection` (:57), `WorkbookConnectSettings` (:59) and
  `WorkbookConnect` (:65, with `sheet: { id: string; name: string }` at :74 —
  which is what `SheetWithoutActive` was just invented for). workbook-utils'
  connect.ts is now a pure re-export, so the package simultaneously exports the
  shared `ConnectDimension` and keeps a private clone; `WorkbookConnect` /
  `ServerWorkbook.connectsJson` (:92) will silently drift from `Connect` on the
  next server payload change. utils.ts:30-78 should be deleted and replaced with
  imports of `ConnectDimension`/`ConnectDirection`/`ConnectSettings`/`Connect`
  from '@getaleph/global-common-utils'.
evidence:
  packages/workbook-utils/src/utils.ts:30
    type ConnectDimension = {
      value: string;  text: string;  id: string;  name: string;  prettyName: string;
      table: string;  integrationTable?: string;  ...  type: string;  index: number;
    };
  packages/workbook-utils/src/utils.ts:57
    export type WorkbookConnectDirection = 'bidirectional' | 'pull' | 'push';
  packages/workbook-utils/src/utils.ts:74
    sheet: { id: string; name: string };
  packages/workbook-utils/src/connect.ts (whole file, post-diff)
    export type { ConnectDirection, ConnectSettings, ConnectRowSettings,
      ConnectDimension, Connect } from '@getaleph/global-common-utils';

--- C2 -------------------------------------------------------------------------
file: packages/global-common-utils/src/connectUtils.ts
line: 9
summary: New anonymous `specialConnectFilter?: { isOn?: boolean }` written twice
  in one 61-line file, re-inventing (more weakly) the existing named
  `SpecialConnectFilter` in packages/addin-core/src/entities/ConnectedTable.ts:20.
failure scenario / concrete cost: `SpecialConnectFilter = { id?: string; range:
  string; isOn: boolean }` already exists at ConnectedTable.ts:20 and is used at
  ConnectedTable.ts:30 and :40 and in robustConnects.ts:573, :584, :806; the same
  shape is inlined at ui-components/Connect/types.ts:85-89. The new copy drops
  `range` and `id` and makes `isOn` optional, so `Connect` cannot express a real
  special-connect filter — which is exactly why this PR had to add
  `as unknown as Connect` casts to the test at
  packages/addin-core/tests/operations/buildExplores/findConnectedTablesInRange.test.ts:29
  and :37 in order to pass `specialConnectFilter: { range: 'E2:E10', isOn: true }`.
  Any future code that reads `connect.rowSettings.specialConnectFilter.range`
  through `Connect` will not compile and will get another cast. Should import
  `SpecialConnectFilter` (moving it to connectUtils.ts next to the other Connect
  types) and use it for both `ConnectSettings` and `ConnectRowSettings`.
evidence:
  packages/global-common-utils/src/connectUtils.ts:5-20
    export type ConnectSettings = { direction; pullEmptyAsZero; pushEmptyAsZero;
      specialConnectFilter?: { isOn?: boolean }; };
    export type ConnectRowSettings = { pull: { type: string }; push: { type: string };
      specialConnectFilter?: { isOn?: boolean }; };
  packages/addin-core/src/entities/ConnectedTable.ts:20
    export type SpecialConnectFilter = { id?: string; range: string; isOn: boolean };
  packages/addin-core/tests/.../findConnectedTablesInRange.test.ts:29,37
    }) as unknown as Connect;

--- C3 -------------------------------------------------------------------------
file: packages/global-common-utils/src/connectUtils.ts
line: 14
summary: `ConnectSettings` and `ConnectRowSettings` are now exported under those
  exact names from BOTH @getaleph/global-common-utils and
  packages/addin-core/src/entities/ConnectedTable.ts, with different (weaker)
  shapes, into files that import from both modules.
failure scenario / concrete cost: `packages/global-common-utils/src/index.ts:43`
  adds `export * from './connectUtils'`, so `ConnectRowSettings` /
  `ConnectSettings` become public names of global-common-utils. addin-core's
  robustConnects.ts already imports 9 names from '@getaleph/global-common-utils'
  (lines 2-11) and `ConnectColumnSettings, ConnectParam, ConnectRowSettings,
  SpecialConnectFilter` from '../entities/ConnectedTable' (lines 17-22). An
  auto-import or a merged import line silently swaps the strict
  `pull: { type: 'default' | 'star' | 'smartInsert' }` for the loose
  `pull: { type: string }`, killing the exhaustiveness that
  robustConnects.ts:455 `constructDimensionRangeSettings<ConnectRowSettings>`
  relies on. Also note global-common-utils has NO `ConnectColumnSettings`: the
  new `Connect` reuses `ConnectRowSettings` for `columnSettings` (:60) whereas
  ConnectedTable.ts:33 keeps them distinct. Either reuse the addin-core types
  (move them here) or rename the new ones (e.g. `ServerConnectSettings`).
evidence:
  packages/global-common-utils/src/index.ts:43   export * from './connectUtils';
  packages/global-common-utils/src/connectUtils.ts:14-20  ConnectRowSettings { pull: { type: string } ... }
  packages/addin-core/src/entities/ConnectedTable.ts:22-31 ConnectRowSettings { pull: { type: 'default'|'star'|'smartInsert'; deleteMissingRows?: boolean } ... }
  packages/addin-core/src/operations/robustConnects.ts:2-11 and :17-22 (imports both modules)

--- C4 -------------------------------------------------------------------------
file: packages/addin-core/src/operations/buildExplores/utils.ts
line: 711
summary: `narrowedMatches.filter(Boolean)` re-implements (badly) the existing
  `isNotNullish` type guard that lives in the very package this file already
  imports from twice.
failure scenario / concrete cost: `isNotNullish` is at
  packages/global-common-utils/src/nullabilityUtils.ts:12, exported publicly via
  packages/global-common-utils/src/index.ts:28, and its JSDoc example is literally
  `const ids: string[] = maybeIds.filter(isNotNullish);`. I verified with the
  repo's own tsc 5.4.5 that `filter(Boolean)` does NOT narrow (`error TS2322:
  Type '(C | undefined)[]' is not assignable to type 'C[]'`), so
  `findConnectedTablesInRange` now returns `Connect[] | (Connect | undefined)[]`
  and every future TS consumer of it inherits a phantom `undefined` (index.js:1421
  and :1731 are still JS today, so the leak is silent). The same file has the same
  problem newly annotated in at :383 `const columnDimensions: (Dimension |
  undefined)[] = []` inside `getColumnDimensions`, while its twin
  `getColumnDimensionsNew` (:892) drops undefineds with a hand-written `if`.
  One `.filter(isNotNullish)` fixes all three sites.
evidence:
  packages/addin-core/src/operations/buildExplores/utils.ts:709-711
    );
    return narrowedMatches.filter(Boolean);
  packages/global-common-utils/src/nullabilityUtils.ts:12
    export const isNotNullish = <T>(value: T | null | undefined): value is T => {

--- C5 -------------------------------------------------------------------------
file: packages/addin-core/src/operations/buildExplores/utils.ts
line: 907
summary: Merging newUtils.ts in puts `getRowDimensions` (:355) and
  `getRowDimensionsNew` (:907), and `getColumnDimensions` (:382) and
  `getColumnDimensionsNew` (:891), side by side in one file as near-identical
  copies; both pairs are still called, and the copies already disagree about
  how to handle the same expression.
failure scenario / concrete cost: getRowDimensions/getRowDimensionsNew are ~25
  lines of identical dimensionGroup parent-walking; the only real difference is
  `table.getDimensionByValue` vs `table.getDimensionByAnyMatch`, so one function
  taking the lookup as a parameter replaces both. Both are live:
  buildExplores/index.js calls the old ones at :277, :593, :601, :605 and the New
  ones at :839, :847, :861, :903, :911, :919 — so a fix to the dimensionGroup walk
  has to be made twice and will be forgotten once. The copies have ALREADY
  drifted in their type handling: :357 uses a `!` non-null assertion while :909
  uses an `if (filter)`, and :930 still carries `// @ts-expect-error Table is yet
  not fully typed...` even though this PR added `Table.dimensionGroups`
  (packages/addin-core/src/entities/Table.ts:54) and deleted the other three
  @ts-expect-errors. `getColumnDimensionsNew` (:891) is literally
  `getColumnDimensions` (:382) plus a nullish drop. Both copies also repeat the
  no-op `const parentDimensionTexts = new Set(parentDimensionTextsSet);` (:372,
  :927). oxlint reports the identical `unicorn(prefer-array-some)` violation on
  both copies (utils.ts:375:16 and utils.ts:931:16). Separately, the
  `/** Copied directly from addin-components */` JSDoc at :353, :380 and :403 is
  now false — grep of apps/addin-components/src, apps/web-ui/src and
  packages/frontend-common-components/src for these names returns zero hits.
evidence:
  utils.ts:373-376   return table.dimensions.filter(({ text, value }) =>
                       !filters.find((dimension) => dimension.value === value) && !parentDimensionTexts.has(text));
  utils.ts:928-932   return table.dimensions.filter(({ text, value }) =>
                       // @ts-expect-error Table is yet not fully typed...
                       !filters.find((dimension) => dimension.value === value) && !parentDimensionTexts.has(text));
  oxlint: src/operations/buildExplores/utils.ts:375:16 and :931:16 unicorn(prefer-array-some)

--- C6 -------------------------------------------------------------------------
file: apps/gs-addin/src/client/utils/formulaUtils.ts
line: 22
summary: The diff edits BOTH forks of the Aleph-formula parser
  (ALEPH_FORMULA_REGEX / isAlephFormula / splitAlephFormula /
  buildAlephFormulasMap / RANGE_REGEX / parseAlephFormulaArraySize) instead of
  moving one copy into the shared package both apps already depend on — and in
  doing so it hand-syncs one change and diverges another.
failure scenario / concrete cost: identical definitions exist twice:
  ALEPH_FORMULA_REGEX at packages/addin-core/src/operations/buildExplores/utils.ts:45
  and apps/gs-addin/src/client/utils/formulaUtils.ts:22; isAlephFormula at
  utils.ts:52 / formulaUtils.ts:24; splitAlephFormula at utils.ts:78 /
  formulaUtils.ts:43 (bodies identical including the `quoteRegex` comment);
  buildAlephFormulasMap at utils.ts:836 / formulaUtils.ts:88; RANGE_REGEX at
  packages/addin-core/src/operations/buildExplores/index.js:55 /
  formulaUtils.ts:165; parseAlephFormulaArraySize at index.js:1047 /
  formulaUtils.ts:200. formulaUtils.ts:18 even keeps the commented-out
  `// import { buildAlephFormulasMap, isAlephFormula, splitAlephFormula } from './buildExplores/utils';`.
  This diff (a) DIVERGES the twins — gs-addin `isAlephFormula` becomes
  `Boolean(ALEPH_FORMULA_REGEX.test(s))` while addin-core keeps
  `Boolean(s.match(ALEPH_FORMULA_REGEX))`, and because the regex has the /g flag
  `.test()` advances `lastIndex` while `.match()` resets it, so the two copies
  now return different answers for the same formula on alternating calls; and
  (b) HAND-SYNCS the twins — formulaUtils.ts:213 is edited from
  `arg.replace(/\$/g,'')` to `arg.replaceAll('$','')`, which index.js:1060
  already said. apps/gs-addin/package.json has no @getaleph/addin-core dependency
  but DOES depend on @getaleph/global-common-utils (line 40), the package this PR
  is already adding connectUtils.ts to, so a shared home exists today.
  (Bonus in the same file: formulaUtils.ts:9-13 redeclares
  `type Sheet = { id; isActive; name }` locally while importing from
  '@getaleph/global-common-utils' on lines 3-7, which exports `Sheet` at
  packages/global-common-utils/src/types.ts:369.)
evidence:
  apps/gs-addin/src/client/utils/formulaUtils.ts:22-27
    const ALEPH_FORMULA_REGEX = /ALEPH(.*?)GET(INTEGRATION)?DATA\((.*?)\)/g;
    export function isAlephFormula(formula: string) {
      const s = formula.toString();
      return Boolean(ALEPH_FORMULA_REGEX.test(s));
    }
  packages/addin-core/src/operations/buildExplores/utils.ts:45,52-55
    const ALEPH_FORMULA_REGEX = /ALEPH(.*?)GET(INTEGRATION)?DATA\((.*?)\)/g;
    export function isAlephFormula(formula: string) {
      const s = formula.toString();
      return Boolean(s.match(ALEPH_FORMULA_REGEX));
    }
  apps/gs-addin/src/client/utils/formulaUtils.ts:18
    // import { buildAlephFormulasMap, isAlephFormula, splitAlephFormula } from './buildExplores/utils';

--- C7 -------------------------------------------------------------------------
file: packages/addin-core/src/operations/buildExplores/utils.ts
line: 4
summary: Three pairs of duplicate import statements from the same modules — the
  repo's own linter flags all three.
failure scenario / concrete cost: `pnpm -F @getaleph/addin-core run lint`
  (oxlint, rule `no-duplicates` = "warn" at .oxlintrc.json:132) emits three new
  warnings against this file:
    src/operations/buildExplores/utils.ts:4:46  import(no-duplicates)
    src/operations/buildExplores/utils.ts:35:8  import(no-duplicates)
    src/operations/buildExplores/utils.ts:38:37 import(no-duplicates) — Module '../connects'
  The package runs with `--max-warnings=520`, so these eat headroom in a shared
  budget and will flip CI red for an unrelated PR. Merge lines 4+5
  (@getaleph/global-common-utils), 30-35+36 (../../transformers) and 38+39
  (../connects).
evidence:
  utils.ts:4-5
    import { appendAndFilterToFilterGroup } from '@getaleph/global-common-utils';
    import { CellValue, Connect, Sheet, SheetWithoutActive } from '@getaleph/global-common-utils';
  utils.ts:35-36
    } from '../../transformers';
    import { SpreadsheetInput } from '../../transformers';
  utils.ts:38-39
    import { parseConnectMeasure } from '../connects';
    import { getConnectWithModifiedDimensionRanges } from '../connects';

--- C8 -------------------------------------------------------------------------
file: packages/addin-core/src/operations/buildExplores/utils.ts
line: 183
summary: `ExploreRecording` is a new local, weaker re-declaration of the
  recording/fixture shape that already exists in the module this file imports
  from; and the same "args must exist" guard is written twice because
  `ParsedAlephFormula` was left as a non-discriminated union.
failure scenario / concrete cost: packages/addin-core/src/transformers/explores.ts
  already declares `ExplorePullFixture` (:174-202) with exactly
  `{ app; operation; data: { input: { ...; spreadsheet: SpreadsheetInput } } }`
  plus the DeepPartial "recording under construction" variant
  `InputExplorePullFixture` (:210); utils.ts already imports `SpreadsheetInput`
  from that same module (utils.ts:36), and the real producer of the object is
  typed `FixtureRecording` from @getaleph/server-sdk
  (apps/addin-components/src/components/ActionButtons/ActionButtonsWrapper.tsx:24,
  built at apps/addin-components/src/hooks/useDrillDown.jsx:61-77 and mutated at
  :102-103). A fourth private slice in a leaf file means the `buildExplores`
  recording shape is documented nowhere near the other fixture types and will not
  be updated when they are; the local type also omits `data.input.table` and
  `data.output`, which useDrillDown.jsx:102-103 actually writes. It belongs in
  transformers/explores.ts as a `buildExplores` sibling of `ExplorePullFixture`.
  Related duplication in the same block: because `ParsedAlephFormula`
  (utils.ts:56-70) is not discriminated on `nFormulas`, the identical guard
  `if (!alephFormulaArgs) { throw new Error('Expected aleph formula to have
  args'); }` is written twice, at :134-136 and :170-172, in both cases for a
  branch that cannot happen at runtime. Making `splitAlephFormula` return a union
  discriminated on `nFormulas` deletes both.
evidence:
  utils.ts:183-189
    type ExploreRecording = { data: { input: { spreadsheet: SpreadsheetInput } } };
  packages/addin-core/src/transformers/explores.ts:184-193
    data: { input: { dateTime?; explore; exploreObjectProperties?; sheetName?;
      settings; table; spreadsheet: SpreadsheetInput }; ... }
  utils.ts:134-136 and :170-172
    if (!alephFormulaArgs) {
      throw new Error('Expected aleph formula to have args');
    }

================================================================================
REFUTED / NOT REPORTED (for the record)
================================================================================
- `SheetWithoutActive` (global-common-utils/src/types.ts:374): no pre-existing
  named partial-sheet type anywhere (grep for SheetRef/SheetInfo/SheetLike/
  PartialSheet returned zero). Correctly placed in the shared package. NOT a
  duplicate.
- `Table.dimensionGroups?: DimensionGroup[]` (entities/Table.ts:54): correctly
  imports the shared `DimensionGroup` from global-common-utils, matching
  `ServerTable.dimensionGroups` (types.ts:172). Good reuse.
- `getFilterGroup` (:408) vs `getFilterGroupNew` (:858): genuinely different
  algorithms (FilterGroup entity tree vs appendAndFilterToFilterGroup). Not a
  duplicate pair.
- `getVisibleDimensions` (:398): no existing "clone non-hidden dimensions" helper
  in the repo. Genuinely new.
- apps/server/src/operations/alephFunction.ts: a stricter, distinct parser
  (coerceArg / AlephFunctionParseException), not a third copy of
  splitAlephFormula. Sub-hypothesis refuted.
- workbook-utils `ServerTable`: useWorkbookTables.ts:10 only re-exports
  global-common-utils' `ServerTable` (types.ts:153), so retargeting
  `Connect['table']` is not a duplication problem.
- utils.ts:270-286 spread-in-reduce (oxlint no-accumulating-spread at :274, :282):
  only `groupBy` exists (global-common-utils/src/groupUtils.ts:19) and it returns
  a Map, so this is an efficiency/`Object.fromEntries` issue rather than a reuse
  one. Left to the efficiency angle.
